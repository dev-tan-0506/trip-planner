---
phase: 08-landing-auth-redesign
plan: "02"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - apps/web/app/page.tsx
  - apps/web/src/components/public-entry/landing-page.tsx
  - apps/web/src/components/public-entry/landing-sections.tsx
  - apps/web/src/components/public-entry/__tests__/landing-page.test.tsx
autonomous: true
requirements:
  - UI-01
must_haves:
  truths:
    - The landing route becomes a full Stitch-aligned public entry surface without becoming a dead-end marketing page.
    - Signed-out users see CTA links into auth, while signed-in users see CTA links into `/dashboard`.
    - Landing composition uses shared public-entry blocks and `@repo/ui` primitives rather than rebuilding the Phase 07 visual system inside `app/page.tsx`.
  artifacts:
    - path: apps/web/app/page.tsx
      contains: LandingPage
      min_lines: 10
    - path: apps/web/src/components/public-entry/landing-page.tsx
      contains: export function LandingPage
      min_lines: 80
    - path: apps/web/src/components/public-entry/__tests__/landing-page.test.tsx
      contains: describe
      min_lines: 40
  key_links:
    - from: apps/web/app/page.tsx
      to: apps/web/src/components/public-entry/landing-page.tsx
      via: the route becomes a thin consumer of the shared landing component.
      pattern: LandingPage
    - from: apps/web/src/components/public-entry/landing-page.tsx
      to: apps/web/src/components/public-entry/public-entry-cta.ts
      via: landing CTA destinations come from the shared signed-in/signed-out route helper.
      pattern: getLandingPrimaryHref
---

<objective>
Redesign the root landing route around the Stitch public-entry direction while preserving current entry behavior.

Purpose: Turn the current minimal home route into a real branded landing surface that still routes users decisively into login/register or dashboard.
Output: A thin `app/page.tsx` route, a shared landing composition component, supporting landing sections, and tests that lock CTA behavior for signed-in vs signed-out users.
</objective>

<execution_context>
@D:/code/trip-planner/.codex/get-shit-done/workflows/execute-plan.md
@D:/code/trip-planner/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-landing-auth-redesign/08-CONTEXT.md
@.planning/phases/08-landing-auth-redesign/08-RESEARCH.md
@.planning/phases/08-landing-auth-redesign/08-VALIDATION.md
@apps/web/app/page.tsx
@apps/web/src/store/useAuthStore.ts
@apps/web/src/components/public-entry/public-entry-cta.ts
@.planning/designs/stitch-v2/screens/landing-page/code.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move landing composition out of the route file and rebuild it as a Stitch-aligned shared component</name>
  <files>apps/web/app/page.tsx, apps/web/src/components/public-entry/landing-page.tsx, apps/web/src/components/public-entry/landing-sections.tsx</files>
  <read_first>
    apps/web/app/page.tsx
    apps/web/src/store/useAuthStore.ts
    apps/web/src/components/public-entry/public-entry-nav.tsx
    apps/web/src/components/public-entry/public-entry-cta.ts
    packages/ui/src/components/index.ts
    .planning/phases/08-landing-auth-redesign/08-CONTEXT.md
    .planning/designs/stitch-v2/screens/landing-page/code.html
  </read_first>
  <action>
    Rebuild the root landing experience as a shared public-entry component while preserving hydration-aware entry behavior.
    1. Replace `apps/web/app/page.tsx` with a thin route that reads the existing `useAuthStore` hydration state and renders a shared `LandingPage` component instead of owning the full layout directly.
    2. Create `apps/web/src/components/public-entry/landing-page.tsx` exporting `LandingPage`, and move the main public-entry composition there. Use the shared public-entry nav from Plan `08-01`, shared CTA helpers from `public-entry-cta.ts`, and Phase 07 primitives such as `Button`, `Card`, `Badge`, `HeroPanel`, and `SectionFrame`.
    3. Create `apps/web/src/components/public-entry/landing-sections.tsx` to hold the non-hero sections so the landing component does not collapse back into one oversized file. Include concrete sections for feature/value storytelling, safety/trust emphasis, social proof or community proof, and final CTA.
    4. Keep signed-out primary CTA destination as `/auth/login` and signed-out secondary CTA destination as `/auth/register`. Keep signed-in primary CTA destination as `/dashboard`; if a signed-in secondary action remains, make it non-destructive and route-safe rather than adding a new public branch.
    5. Remove dead-end placeholder links from the current route. Any top-nav links left in the landing surface must either scroll to real section anchors on the same page or route to existing app destinations.
  </action>
  <verify>npx vitest run src/components/public-entry/__tests__/landing-page.test.tsx</verify>
  <acceptance_criteria>
    - `apps/web/app/page.tsx` contains `LandingPage`
    - `apps/web/src/components/public-entry/landing-page.tsx` contains `export function LandingPage`
    - `apps/web/src/components/public-entry/landing-page.tsx` contains `getLandingPrimaryHref`
    - `apps/web/src/components/public-entry/landing-sections.tsx` exists
    - `apps/web/src/components/public-entry/landing-page.tsx` contains `/dashboard`
    - `apps/web/src/components/public-entry/landing-page.tsx` contains `/auth/login`
    - `apps/web/src/components/public-entry/landing-page.tsx` contains `/auth/register`
  </acceptance_criteria>
  <done>The landing route is now a shared, Stitch-aligned public entry surface with correct signed-in/signed-out routing behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add landing tests that lock signed-in and signed-out CTA behavior</name>
  <files>apps/web/src/components/public-entry/__tests__/landing-page.test.tsx</files>
  <read_first>
    apps/web/src/components/public-entry/landing-page.tsx
    apps/web/src/test/setup.tsx
    apps/web/src/store/useAuthStore.ts
    apps/web/src/components/public-entry/public-entry-cta.ts
    .planning/phases/08-landing-auth-redesign/08-VALIDATION.md
  </read_first>
  <action>
    Create focused landing tests through the `apps/web` harness so the redesigned route keeps its entry behavior.
    1. Add `apps/web/src/components/public-entry/__tests__/landing-page.test.tsx`.
    2. Mock the auth-store state so one test renders the signed-out landing and asserts links to `/auth/login` and `/auth/register`.
    3. Add a signed-in landing test that asserts the primary CTA targets `/dashboard`.
    4. Assert the landing renders Vietnamese-first hero copy or product branding, so the test locks that the route is still a real branded landing surface rather than a blank hydration placeholder.
  </action>
  <verify>npx vitest run src/components/public-entry/__tests__/landing-page.test.tsx</verify>
  <acceptance_criteria>
    - `apps/web/src/components/public-entry/__tests__/landing-page.test.tsx` exists
    - `apps/web/src/components/public-entry/__tests__/landing-page.test.tsx` contains `/auth/login`
    - `apps/web/src/components/public-entry/__tests__/landing-page.test.tsx` contains `/auth/register`
    - `apps/web/src/components/public-entry/__tests__/landing-page.test.tsx` contains `/dashboard`
    - `apps/web/src/components/public-entry/__tests__/landing-page.test.tsx` contains `Mình Đi Đâu Thế`
  </acceptance_criteria>
  <done>The landing redesign is covered by tests that pin its signed-in/signed-out routing contract.</done>
</task>

</tasks>

<verification>
- [ ] Run `npx vitest run src/components/public-entry/__tests__/landing-page.test.tsx`
- [ ] Run `npm run check-types`
- [ ] Confirm the root route is no longer a small one-card page
- [ ] Confirm top-nav links do not point to dead `#` placeholders unless they scroll to real landing sections
</verification>

<success_criteria>
- The landing page matches the Stitch direction more closely through a real multi-section entry surface
- CTA destinations stay correct for signed-in and signed-out users
- Landing composition is shared-component-driven rather than route-local one-off markup
</success_criteria>

<output>
After completion, create `.planning/phases/08-landing-auth-redesign/08-02-SUMMARY.md`
</output>
