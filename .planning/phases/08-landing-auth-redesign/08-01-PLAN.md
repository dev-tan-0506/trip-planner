---
phase: 08-landing-auth-redesign
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/public-entry/public-entry-cta.ts
  - apps/web/src/components/public-entry/public-entry-brand.tsx
  - apps/web/src/components/public-entry/public-entry-nav.tsx
  - apps/web/src/components/public-entry/public-entry-collage.tsx
  - apps/web/src/components/public-entry/auth-screen-frame.tsx
  - apps/web/src/components/public-entry/index.ts
  - apps/web/app/auth/layout.tsx
  - apps/web/src/components/public-entry/__tests__/public-entry-shell.test.tsx
autonomous: true
requirements:
  - UI-01
must_haves:
  truths:
    - Landing and auth routes share one reusable public-entry composition layer instead of duplicating the Stitch shell in each route file.
    - Signed-in vs signed-out CTA destinations are encoded in one helper so route behavior does not drift across landing and auth surfaces.
    - The auth layout provides a consistent expressive background and centered card frame without embedding login/register-specific business logic.
  artifacts:
    - path: apps/web/src/components/public-entry/public-entry-cta.ts
      contains: getLandingPrimaryHref
      min_lines: 10
    - path: apps/web/src/components/public-entry/auth-screen-frame.tsx
      contains: AuthScreenFrame
      min_lines: 30
    - path: apps/web/app/auth/layout.tsx
      contains: AuthScreenFrame
      min_lines: 8
    - path: apps/web/src/components/public-entry/__tests__/public-entry-shell.test.tsx
      contains: describe
      min_lines: 30
  key_links:
    - from: apps/web/app/auth/layout.tsx
      to: apps/web/src/components/public-entry/auth-screen-frame.tsx
      via: auth routes render inside one shared public-entry frame.
      pattern: AuthScreenFrame
    - from: apps/web/src/components/public-entry/public-entry-nav.tsx
      to: apps/web/src/components/public-entry/public-entry-cta.ts
      via: nav CTA destinations come from the shared route helper instead of hard-coded links.
      pattern: getLandingPrimaryHref
---

<objective>
Create the shared public-entry building blocks that both landing and auth screens will consume.

Purpose: Prevent Phase 08 from devolving into three separate route-level redesigns by establishing one reusable public-entry layer first.
Output: Shared CTA routing helpers, reusable public-entry shell components, an auth screen frame, and tests that lock the shared behavior contract before route rewrites begin.
</objective>

<execution_context>
@D:/code/trip-planner/.codex/get-shit-done/workflows/execute-plan.md
@D:/code/trip-planner/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-landing-auth-redesign/08-CONTEXT.md
@.planning/phases/08-landing-auth-redesign/08-RESEARCH.md
@.planning/phases/08-landing-auth-redesign/08-VALIDATION.md
@apps/web/app/layout.tsx
@apps/web/app/auth/layout.tsx
@apps/web/app/page.tsx
@packages/ui/src/components/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared public-entry routing helpers and shell components</name>
  <files>apps/web/src/components/public-entry/public-entry-cta.ts, apps/web/src/components/public-entry/public-entry-brand.tsx, apps/web/src/components/public-entry/public-entry-nav.tsx, apps/web/src/components/public-entry/public-entry-collage.tsx, apps/web/src/components/public-entry/auth-screen-frame.tsx, apps/web/src/components/public-entry/index.ts</files>
  <read_first>
    apps/web/app/page.tsx
    apps/web/app/auth/layout.tsx
    apps/web/app/layout.tsx
    packages/ui/src/components/index.ts
    .planning/phases/08-landing-auth-redesign/08-CONTEXT.md
    .planning/phases/08-landing-auth-redesign/08-RESEARCH.md
  </read_first>
  <action>
    Add a dedicated public-entry component slice under `apps/web/src/components/public-entry/` so the landing and auth redesigns consume shared blocks instead of route-local markup.
    1. Create `apps/web/src/components/public-entry/public-entry-cta.ts` with exact helper exports `getLandingPrimaryHref(isAuthenticated: boolean): "/dashboard" | "/auth/login"` and `getLandingSecondaryHref(isAuthenticated: boolean): "/dashboard" | "/auth/register"`. Keep the helper pure and route-only; do not read Zustand or browser APIs inside it.
    2. Create `apps/web/src/components/public-entry/public-entry-brand.tsx` that renders the product wordmark and Vietnamese tagline in Phase 07 typography tokens. Include the literal string `Mình Đi Đâu Thế?`.
    3. Create `apps/web/src/components/public-entry/public-entry-nav.tsx` that accepts `isAuthenticated`, renders the shared brand, and exposes CTA links derived from `getLandingPrimaryHref` and `getLandingSecondaryHref` instead of hard-coded route strings scattered across routes.
    4. Create `apps/web/src/components/public-entry/public-entry-collage.tsx` as the expressive background/collage layer for public-entry screens. Keep it presentation-only and do not mix business logic into the background component.
    5. Create `apps/web/src/components/public-entry/auth-screen-frame.tsx` that composes the collage/background layer with a centered card container for auth pages. Export it as `AuthScreenFrame`.
    6. Create `apps/web/src/components/public-entry/index.ts` and export the new helper and components from this barrel so the route plans can import from one shared public-entry entrypoint.
  </action>
  <verify>npx vitest run src/components/public-entry/__tests__/public-entry-shell.test.tsx</verify>
  <acceptance_criteria>
    - `apps/web/src/components/public-entry/public-entry-cta.ts` exists
    - `apps/web/src/components/public-entry/public-entry-cta.ts` contains `getLandingPrimaryHref`
    - `apps/web/src/components/public-entry/public-entry-cta.ts` contains `"/dashboard"`
    - `apps/web/src/components/public-entry/public-entry-nav.tsx` contains `getLandingSecondaryHref`
    - `apps/web/src/components/public-entry/auth-screen-frame.tsx` contains `AuthScreenFrame`
    - `apps/web/src/components/public-entry/public-entry-brand.tsx` contains `Mình Đi Đâu Thế?`
    - `apps/web/src/components/public-entry/index.ts` contains `public-entry-nav`
  </acceptance_criteria>
  <done>A reusable public-entry layer exists with shared CTA mapping, shared brand/nav composition, and a reusable auth screen frame.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the auth route layout around the shared frame and lock the shared contract with tests</name>
  <files>apps/web/app/auth/layout.tsx, apps/web/src/components/public-entry/__tests__/public-entry-shell.test.tsx</files>
  <read_first>
    apps/web/app/auth/layout.tsx
    apps/web/src/test/setup.tsx
    apps/web/vitest.config.ts
    apps/web/src/components/public-entry/auth-screen-frame.tsx
    apps/web/src/components/public-entry/public-entry-cta.ts
    .planning/phases/08-landing-auth-redesign/08-VALIDATION.md
  </read_first>
  <action>
    Convert the current auth layout into a thin consumer of the shared public-entry frame and add tests for the shared public-entry contract.
    1. Replace the custom blob-background markup in `apps/web/app/auth/layout.tsx` with a thin wrapper that imports and returns `AuthScreenFrame`.
    2. Create `apps/web/src/components/public-entry/__tests__/public-entry-shell.test.tsx` with test cases that assert `getLandingPrimaryHref(false)` returns `/auth/login`, `getLandingPrimaryHref(true)` returns `/dashboard`, `getLandingSecondaryHref(false)` returns `/auth/register`, and `getLandingSecondaryHref(true)` returns `/dashboard`.
    3. In the same test file, render `AuthScreenFrame` with simple children and assert the child content appears, so the test proves the shared auth frame can mount inside the `apps/web` harness.
  </action>
  <verify>npx vitest run src/components/public-entry/__tests__/public-entry-shell.test.tsx</verify>
  <acceptance_criteria>
    - `apps/web/app/auth/layout.tsx` contains `AuthScreenFrame`
    - `apps/web/src/components/public-entry/__tests__/public-entry-shell.test.tsx` exists
    - `apps/web/src/components/public-entry/__tests__/public-entry-shell.test.tsx` contains `/auth/login`
    - `apps/web/src/components/public-entry/__tests__/public-entry-shell.test.tsx` contains `/auth/register`
    - `apps/web/src/components/public-entry/__tests__/public-entry-shell.test.tsx` contains `/dashboard`
    - `apps/web/src/components/public-entry/__tests__/public-entry-shell.test.tsx` contains `AuthScreenFrame`
  </acceptance_criteria>
  <done>The auth layout is now a thin shared-frame consumer and the public-entry shell contract is locked with focused tests.</done>
</task>

</tasks>

<verification>
- [ ] Run `npx vitest run src/components/public-entry/__tests__/public-entry-shell.test.tsx`
- [ ] Run `npm run check-types`
- [ ] Confirm `apps/web/app/auth/layout.tsx` no longer contains the old blob-only background markup
</verification>

<success_criteria>
- Shared public-entry helpers and shell components exist under `apps/web/src/components/public-entry/`
- Auth layout is a thin consumer of `AuthScreenFrame`
- CTA destination rules are centralized and test-covered
</success_criteria>

<output>
After completion, create `.planning/phases/08-landing-auth-redesign/08-01-SUMMARY.md`
</output>
