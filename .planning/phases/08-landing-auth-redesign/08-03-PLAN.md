---
phase: 08-landing-auth-redesign
plan: "03"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - apps/web/app/auth/login/page.tsx
  - apps/web/app/auth/register/page.tsx
  - apps/web/src/components/public-entry/auth-page-shell.tsx
  - apps/web/src/components/public-entry/__tests__/auth-pages.test.tsx
autonomous: true
requirements:
  - UI-01
must_haves:
  truths:
    - Login and register keep their current auth API/store behavior while adopting the new shared public-entry visual system.
    - Auth pages use shared design-system primitives and a shared auth shell instead of two unrelated route-local visual implementations.
    - All malformed Vietnamese user-facing strings on login and register are corrected during the redesign.
  artifacts:
    - path: apps/web/src/components/public-entry/auth-page-shell.tsx
      contains: AuthPageShell
      min_lines: 30
    - path: apps/web/app/auth/login/page.tsx
      contains: authApi.login
      min_lines: 60
    - path: apps/web/app/auth/register/page.tsx
      contains: authApi.register
      min_lines: 70
    - path: apps/web/src/components/public-entry/__tests__/auth-pages.test.tsx
      contains: describe
      min_lines: 60
  key_links:
    - from: apps/web/app/auth/login/page.tsx
      to: apps/web/src/components/public-entry/auth-page-shell.tsx
      via: login uses the shared auth shell instead of custom route-local framing.
      pattern: AuthPageShell
    - from: apps/web/app/auth/register/page.tsx
      to: apps/web/src/components/public-entry/auth-page-shell.tsx
      via: register uses the shared auth shell instead of custom route-local framing.
      pattern: AuthPageShell
---

<objective>
Redesign login and register so they match the new public-entry direction without changing how auth actually works.

Purpose: Replace the current route-local auth UIs, fix broken Vietnamese copy, and keep trust/readability high through a shared auth shell and shared UI primitives.
Output: A reusable auth page shell, redesigned login/register pages, and focused tests for auth copy, links, and submission behavior.
</objective>

<execution_context>
@D:/code/trip-planner/.codex/get-shit-done/workflows/execute-plan.md
@D:/code/trip-planner/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-landing-auth-redesign/08-CONTEXT.md
@.planning/phases/08-landing-auth-redesign/08-RESEARCH.md
@.planning/phases/08-landing-auth-redesign/08-VALIDATION.md
@apps/web/app/auth/login/page.tsx
@apps/web/app/auth/register/page.tsx
@apps/web/src/lib/api-client.ts
@apps/web/src/store/useAuthStore.ts
@packages/ui/src/components/input.tsx
@packages/ui/src/button.tsx
@.planning/designs/stitch-v2/screens/login/code.html
@.planning/designs/stitch-v2/screens/register/code.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a shared auth page shell and rebuild login/register on top of it</name>
  <files>apps/web/src/components/public-entry/auth-page-shell.tsx, apps/web/app/auth/login/page.tsx, apps/web/app/auth/register/page.tsx</files>
  <read_first>
    apps/web/app/auth/login/page.tsx
    apps/web/app/auth/register/page.tsx
    apps/web/src/lib/api-client.ts
    apps/web/src/store/useAuthStore.ts
    packages/ui/src/components/input.tsx
    packages/ui/src/button.tsx
    .planning/phases/08-landing-auth-redesign/08-CONTEXT.md
    .planning/designs/stitch-v2/screens/login/code.html
    .planning/designs/stitch-v2/screens/register/code.html
  </read_first>
  <action>
    Rebuild the auth routes around a shared shell while preserving the existing successful auth flow.
    1. Create `apps/web/src/components/public-entry/auth-page-shell.tsx` exporting `AuthPageShell`. The shell should render the shared brand block, heading/subcopy area, supportive background/collage context, and a surface/card container for the form content.
    2. Update `apps/web/app/auth/login/page.tsx` to use `AuthPageShell` plus shared `@repo/ui/Input` and `Button` primitives where practical, while preserving the exact business flow: `await authApi.login(...)`, then `await checkAuth()`, then `router.push('/')`. Keep global error handling and submit disabled/loading states.
    3. Update `apps/web/app/auth/register/page.tsx` to use `AuthPageShell` plus shared UI primitives where practical, while preserving the exact business flow: `await authApi.register(...)`, then `await checkAuth()`, then `router.push('/')`.
    4. Replace all mojibake and malformed Vietnamese strings in both auth files with correctly encoded Vietnamese copy. Keep the auth tone playful-but-clear rather than as loud as the landing page.
    5. Preserve the cross-links between login and register with exact destinations `/auth/register` and `/auth/login`.
  </action>
  <verify>npx vitest run src/components/public-entry/__tests__/auth-pages.test.tsx</verify>
  <acceptance_criteria>
    - `apps/web/src/components/public-entry/auth-page-shell.tsx` exists
    - `apps/web/src/components/public-entry/auth-page-shell.tsx` contains `AuthPageShell`
    - `apps/web/app/auth/login/page.tsx` contains `authApi.login`
    - `apps/web/app/auth/login/page.tsx` contains `router.push('/')`
    - `apps/web/app/auth/register/page.tsx` contains `authApi.register`
    - `apps/web/app/auth/register/page.tsx` contains `router.push('/')`
    - `apps/web/app/auth/login/page.tsx` contains `Đăng nhập`
    - `apps/web/app/auth/register/page.tsx` contains `Đăng ký`
  </acceptance_criteria>
  <done>Login and register now share one visual shell, retain their current auth behavior, and use corrected Vietnamese-first copy.</done>
</task>

<task type="auto">
  <name>Task 2: Add auth tests for corrected copy, cross-links, and preserved submit flow</name>
  <files>apps/web/src/components/public-entry/__tests__/auth-pages.test.tsx</files>
  <read_first>
    apps/web/app/auth/login/page.tsx
    apps/web/app/auth/register/page.tsx
    apps/web/src/test/setup.tsx
    apps/web/src/lib/api-client.ts
    apps/web/src/store/useAuthStore.ts
    .planning/phases/08-landing-auth-redesign/08-VALIDATION.md
  </read_first>
  <action>
    Add focused auth-page tests through the existing `apps/web` Vitest harness.
    1. Create `apps/web/src/components/public-entry/__tests__/auth-pages.test.tsx`.
    2. Mock `authApi.login`, `authApi.register`, and the router/store hooks enough to assert the login and register pages still call the existing auth flow functions rather than bypassing them.
    3. Add assertions that the login page renders corrected Vietnamese labels and links to `/auth/register`.
    4. Add assertions that the register page renders corrected Vietnamese labels and links to `/auth/login`.
    5. Add one submit-path assertion per page so the tests prove each page still invokes its auth API handler with form data.
  </action>
  <verify>npx vitest run src/components/public-entry/__tests__/auth-pages.test.tsx</verify>
  <acceptance_criteria>
    - `apps/web/src/components/public-entry/__tests__/auth-pages.test.tsx` exists
    - `apps/web/src/components/public-entry/__tests__/auth-pages.test.tsx` contains `authApi.login`
    - `apps/web/src/components/public-entry/__tests__/auth-pages.test.tsx` contains `authApi.register`
    - `apps/web/src/components/public-entry/__tests__/auth-pages.test.tsx` contains `/auth/register`
    - `apps/web/src/components/public-entry/__tests__/auth-pages.test.tsx` contains `/auth/login`
    - `apps/web/src/components/public-entry/__tests__/auth-pages.test.tsx` contains `Đăng nhập`
    - `apps/web/src/components/public-entry/__tests__/auth-pages.test.tsx` contains `Đăng ký`
  </acceptance_criteria>
  <done>The auth redesign is protected by tests for corrected copy, cross-links, and preserved login/register submit behavior.</done>
</task>

</tasks>

<verification>
- [ ] Run `npx vitest run src/components/public-entry/__tests__/auth-pages.test.tsx`
- [ ] Run `npm run check-types`
- [ ] Run `npm run build`
- [ ] Confirm login and register no longer contain mojibake strings
- [ ] Confirm auth pages remain card-first and visually calmer than the landing page
</verification>

<success_criteria>
- Login and register match the new public-entry direction while preserving current auth behavior
- Shared public-entry/auth shell is reused instead of duplicated route-local framing
- Broken Vietnamese strings are removed from both auth routes
</success_criteria>

<output>
After completion, create `.planning/phases/08-landing-auth-redesign/08-03-SUMMARY.md`
</output>
