---
phase: 07-design-system-shared-shell-foundation
plan: "04"
type: execute
wave: 4
depends_on:
  - 07-03
files_modified:
  - packages/ui/src/card.tsx
  - packages/ui/src/components/dialog.tsx
  - packages/ui/src/components/tabs.tsx
  - packages/ui/src/components/sheet.tsx
  - packages/ui/src/components/skeleton.tsx
  - packages/ui/src/components/empty-state.tsx
  - packages/ui/src/components/error-state.tsx
  - packages/ui/src/components/index.ts
autonomous: true
requirements:
  - DSYS-02
  - DSYS-03
must_haves:
  truths:
    - `packages/ui` contains reusable container, overlay, and shared-state primitives instead of scaffold placeholders.
    - Loading, empty, error, disabled, and responsive-ready behavior is built into the shared primitive layer before route redesign begins.
    - Later shell-block plans can compose from shared card, overlay, and state primitives instead of embedding one-off state UIs in route files.
  artifacts:
    - path: packages/ui/src/card.tsx
      contains: "lifted"
      min_lines: 40
    - path: packages/ui/src/components/dialog.tsx
      contains: "Dialog"
      min_lines: 25
    - path: packages/ui/src/components/skeleton.tsx
      contains: "Skeleton"
      min_lines: 20
    - path: packages/ui/src/components/empty-state.tsx
      contains: "EmptyState"
      min_lines: 20
    - path: packages/ui/src/components/error-state.tsx
      contains: "ErrorState"
      min_lines: 20
    - path: packages/ui/src/components/index.ts
      contains: "error-state"
      min_lines: 18
  key_links:
    - from: packages/ui/src/components/index.ts
      to: packages/ui/src/components/empty-state.tsx
      via: barrel exports expose shared-state primitives through one shared import surface.
      pattern: empty-state
    - from: packages/ui/src/card.tsx
      to: packages/ui/src/styles/globals.css
      via: surface variants consume the semantic surface tokens rather than hardcoded card chrome.
      pattern: surface
---

<objective>
Build the shared surface, overlay, and state primitives that later shell blocks and pages will compose.

Purpose: Satisfy DSYS-03 and the heavier half of D-03 by separating stateful and overlay-capable primitives from the lighter core-controls plan, keeping this work executable without bundling shell chrome into the same prompt.
Output: Shared card, dialog, tabs, sheet, skeleton, empty-state, and error-state primitives exported from `packages/ui`.
</objective>

<execution_context>
@C:/Users/quoct/OneDrive/Desktop/code/designapp/trip-planner/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/quoct/OneDrive/Desktop/code/designapp/trip-planner/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-design-system-shared-shell-foundation/07-CONTEXT.md
@.planning/phases/07-design-system-shared-shell-foundation/07-RESEARCH.md
@.planning/phases/07-design-system-shared-shell-foundation/07-UI-SPEC.md
@.planning/designs/stitch-v2/screens/dashboard/DESIGN.md
@packages/ui/src/components/index.ts
@packages/ui/src/button.tsx
@packages/ui/src/card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the shared surface and overlay primitives</name>
  <files>packages/ui/src/card.tsx, packages/ui/src/components/dialog.tsx, packages/ui/src/components/tabs.tsx, packages/ui/src/components/sheet.tsx, packages/ui/src/components/index.ts</files>
  <read_first>
    packages/ui/src/card.tsx
    packages/ui/src/styles/globals.css
    packages/ui/src/components/index.ts
    .planning/phases/07-design-system-shared-shell-foundation/07-UI-SPEC.md
    .planning/designs/stitch-v2/screens/dashboard/DESIGN.md
  </read_first>
  <action>
    Build the reusable surface and overlay primitives that shell blocks will depend on.
    1. Replace the scaffold `packages/ui/src/card.tsx` link card with a true shared card primitive supporting at least base, lifted, and glass-capable surface variants, following the no-divider-by-default rule from D-06.
    2. Add `packages/ui/src/components/dialog.tsx`, `tabs.tsx`, and `sheet.tsx` using the existing `radix-nova` direction from D-05 rather than inventing a second primitive stack. Keep them styled to the shared token contract and mobile-safe per D-08 and D-09.
    3. Update the barrel exports so later shell-block plans can consume these primitives through `@repo/ui/components`.
    4. Keep this plan primitive-scoped. Do not add search, header, hero, status, or quick-action shell blocks here.
  </action>
  <verify>
    <automated>npm run check-types</automated>
  </verify>
  <acceptance_criteria>
    - `packages/ui/src/card.tsx` does not contain `utm_source=create-turbo`
    - `packages/ui/src/card.tsx` contains `lifted`
    - `packages/ui/src/components/dialog.tsx` exists
    - `packages/ui/src/components/tabs.tsx` exists
    - `packages/ui/src/components/sheet.tsx` exists
    - `packages/ui/src/components/index.ts` contains `sheet`
  </acceptance_criteria>
  <done>Shared surface and overlay primitives are available in `packages/ui` without mixing shell-block composition into the same plan.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the shared loading, empty, and error primitives</name>
  <files>packages/ui/src/components/skeleton.tsx, packages/ui/src/components/empty-state.tsx, packages/ui/src/components/error-state.tsx, packages/ui/src/components/index.ts</files>
  <read_first>
    packages/ui/src/button.tsx
    packages/ui/src/components/index.ts
    .planning/phases/07-design-system-shared-shell-foundation/07-UI-SPEC.md
  </read_first>
  <action>
    Implement the shared state primitives required by DSYS-03 before shell composition starts.
    1. Add `packages/ui/src/components/skeleton.tsx` for card, list, and shell-header placeholders that avoid layout shift.
    2. Add `packages/ui/src/components/empty-state.tsx` and `error-state.tsx` with the Vietnamese-first copy contract from `07-UI-SPEC.md`, including a clear primary action or retry path and fill-based error treatment.
    3. Update the component barrel so later shell and consumer-proof plans can import these state primitives through `@repo/ui/components`.
    4. Keep scope disciplined: no shell chrome, no hero sections, and no feature-specific domain cards in this plan.
  </action>
  <verify>
    <automated>npm run check-types</automated>
  </verify>
  <acceptance_criteria>
    - `packages/ui/src/components/skeleton.tsx` exists
    - `packages/ui/src/components/empty-state.tsx` contains `Chưa có gì để quẩy hết`
    - `packages/ui/src/components/error-state.tsx` contains `Có lỗi xảy ra rồi`
    - `packages/ui/src/components/index.ts` contains `error-state`
  </acceptance_criteria>
  <done>Shared loading, empty, and error primitives are exported from `packages/ui`, giving later plans a reusable state matrix instead of route-level one-offs.</done>
</task>

</tasks>

<verification>
- [ ] Run `npm run check-types`
- [ ] Confirm shared state and overlay primitives export from `packages/ui/src/components/index.ts`
- [ ] Confirm no task introduced shell chrome or feature-specific domain cards
</verification>

<success_criteria>
- Shared surface, overlay, and state primitives are reusable from `packages/ui`
- Loading, empty, error, disabled, and responsive-safe behavior exists in the primitive layer before shell plans start
- Phase 07 keeps shell composition separated from primitive/state implementation work
</success_criteria>

<output>
After completion, create `.planning/phases/07-design-system-shared-shell-foundation/07-04-SUMMARY.md`
</output>
