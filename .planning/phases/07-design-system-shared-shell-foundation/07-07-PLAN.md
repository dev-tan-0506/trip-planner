---
phase: 07-design-system-shared-shell-foundation
plan: "07"
type: execute
wave: 7
depends_on:
  - 07-06
files_modified:
  - apps/web/src/components/ui/phase-07-system-preview.tsx
  - apps/web/app/dev/design-system/page.tsx
  - apps/web/src/components/ui/__tests__/phase-07-system-preview.test.tsx
autonomous: true
requirements:
  - DSYS-01
  - DSYS-02
  - DSYS-03
must_haves:
  truths:
    - The web app can import and render the shared Phase 07 system without route-local one-off styling.
    - Tokens, primitives, and shell blocks render together in at least one thin consumer proof inside `apps/web`.
    - Automated tests lock the loading, empty, error, disabled, and responsive-safe states that later redesign phases depend on.
  artifacts:
    - path: apps/web/src/components/ui/phase-07-system-preview.tsx
      contains: "Phase07SystemPreview"
      min_lines: 60
    - path: apps/web/app/dev/design-system/page.tsx
      contains: "Phase07SystemPreview"
      min_lines: 10
    - path: apps/web/src/components/ui/__tests__/phase-07-system-preview.test.tsx
      contains: "describe"
      min_lines: 40
  key_links:
    - from: apps/web/app/dev/design-system/page.tsx
      to: apps/web/src/components/ui/phase-07-system-preview.tsx
      via: app route mounts the shared-system proof component for real consumer validation.
      pattern: Phase07SystemPreview
    - from: apps/web/src/components/ui/__tests__/phase-07-system-preview.test.tsx
      to: apps/web/src/components/ui/phase-07-system-preview.tsx
      via: UI tests verify shared states through the real consumer component.
      pattern: render
---

<objective>
Prove that the new design system actually works inside `apps/web` and lock its shared-state behavior with automated validation.

Purpose: Finish Phase 07 with a thin consumer proof and test gates, without drifting into a full landing, auth, or dashboard redesign.
Output: A web preview surface that renders shared tokens, primitives, and shell blocks together, plus UI tests that verify the required state matrix.
</objective>

<execution_context>
@C:/Users/quoct/OneDrive/Desktop/code/designapp/trip-planner/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/quoct/OneDrive/Desktop/code/designapp/trip-planner/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-design-system-shared-shell-foundation/07-CONTEXT.md
@.planning/phases/07-design-system-shared-shell-foundation/07-RESEARCH.md
@.planning/phases/07-design-system-shared-shell-foundation/07-UI-SPEC.md
@apps/web/vitest.config.ts
@apps/web/src/test/setup.tsx
@apps/web/app/page.tsx
@apps/web/app/dashboard/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a thin web consumer proof for the shared design system</name>
  <files>apps/web/src/components/ui/phase-07-system-preview.tsx, apps/web/app/dev/design-system/page.tsx</files>
  <read_first>
    apps/web/app/page.tsx
    apps/web/app/dashboard/page.tsx
    packages/ui/src/components/index.ts
    packages/ui/src/styles/globals.css
    .planning/phases/07-design-system-shared-shell-foundation/07-RESEARCH.md
    .planning/phases/07-design-system-shared-shell-foundation/07-UI-SPEC.md
  </read_first>
  <action>
    Create a dedicated consumer proof that exercises the shared system without redesigning production screens.
    1. Add `apps/web/src/components/ui/phase-07-system-preview.tsx` that renders a compact composition of the shared AppHeader, HeroPanel, SectionFrame, ActionPillRow, Button, Input/SearchField, Card, Badge, Avatar, StatusBanner, EmptyState, ErrorState, Skeleton, GlassNav, and FloatingActionSurface.
    2. Add `apps/web/app/dev/design-system/page.tsx` that mounts the preview component on a simple developer-facing route. Use the route only as a proof harness; do not replace `apps/web/app/page.tsx`, `apps/web/app/auth/login/page.tsx`, or `apps/web/app/dashboard/page.tsx`.
    3. Make the preview explicitly demonstrate the loading, empty, error, disabled, and mobile-safe states required by DSYS-03, with Vietnamese-first copy and no page-specific business logic.
  </action>
  <verify>
    <automated>npm run check-types</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/ui/phase-07-system-preview.tsx` exists
    - `apps/web/src/components/ui/phase-07-system-preview.tsx` contains `Phase07SystemPreview`
    - `apps/web/src/components/ui/phase-07-system-preview.tsx` contains `EmptyState`
    - `apps/web/src/components/ui/phase-07-system-preview.tsx` contains `ErrorState`
    - `apps/web/src/components/ui/phase-07-system-preview.tsx` contains `GlassNav`
    - `apps/web/app/dev/design-system/page.tsx` exists
    - `apps/web/app/dev/design-system/page.tsx` contains `Phase07SystemPreview`
  </acceptance_criteria>
  <done>The web app contains a dedicated preview route that proves the shared design system can be consumed end to end without touching production redesign phases early.</done>
</task>

<task type="auto">
  <name>Task 2: Add UI tests that lock the shared-system state matrix</name>
  <files>apps/web/src/components/ui/__tests__/phase-07-system-preview.test.tsx</files>
  <read_first>
    apps/web/vitest.config.ts
    apps/web/src/test/setup.tsx
    apps/web/src/components/ui/phase-07-system-preview.tsx
    .planning/phases/07-design-system-shared-shell-foundation/07-RESEARCH.md
  </read_first>
  <action>
    Add the validation gate recommended by research using the existing `apps/web` Vitest harness.
    1. Create `apps/web/src/components/ui/__tests__/phase-07-system-preview.test.tsx` with cases that assert the preview renders shared-system content and explicitly covers loading, empty, error, disabled, and mobile-safe shell states.
    2. Include at least one assertion that the preview renders a shared shell block plus a primitive together, so the test proves the package import surface and shared stylesheet assumptions are exercised by a real consumer.
    3. Keep tests consumer-driven through `apps/web`; do not add a separate `packages/ui` test runner in this phase.
  </action>
  <verify>
    <automated>npm run -w apps/web test:ui</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/ui/__tests__/phase-07-system-preview.test.tsx` exists
    - `apps/web/src/components/ui/__tests__/phase-07-system-preview.test.tsx` contains `loading`
    - `apps/web/src/components/ui/__tests__/phase-07-system-preview.test.tsx` contains `empty`
    - `apps/web/src/components/ui/__tests__/phase-07-system-preview.test.tsx` contains `error`
    - `apps/web/src/components/ui/__tests__/phase-07-system-preview.test.tsx` contains `disabled`
    - `apps/web/src/components/ui/__tests__/phase-07-system-preview.test.tsx` contains `GlassNav`
  </acceptance_criteria>
  <done>Automated UI tests exist in `apps/web` for the shared-system consumer proof and lock the DSYS-03 state matrix before Phase 08 starts.</done>
</task>

</tasks>

<verification>
- [ ] Run `npm run -w apps/web test:ui`
- [ ] Run `npm run check-types`
- [ ] Run `npm run build`
- [ ] Confirm the preview route proves tokens, primitives, and shell blocks together without replacing existing landing or dashboard pages
</verification>

<success_criteria>
- `apps/web` renders a thin shared-system proof route successfully
- UI tests cover loading, empty, error, disabled, and responsive-safe states
- Phase 07 ends with reusable design-system infrastructure, not a premature full-screen redesign
</success_criteria>

<output>
After completion, create `.planning/phases/07-design-system-shared-shell-foundation/07-07-SUMMARY.md`
</output>
