---
phase: 07-design-system-shared-shell-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/package.json
  - packages/ui/components.json
  - packages/ui/src/styles/globals.css
  - packages/ui/src/lib/utils.ts
  - packages/ui/src/components/index.ts
  - packages/ui/src/index.ts
autonomous: true
requirements:
  - DSYS-02
must_haves:
  truths:
    - The shared UI package exposes a stable import surface that matches the shadcn alias contract already configured in the repo.
    - A real shared stylesheet exists under packages/ui so later plans stop depending on a missing CSS entrypoint.
    - Later primitive and shell plans can add components without reworking package exports or alias wiring again.
  artifacts:
    - path: packages/ui/src/styles/globals.css
      contains: "@theme"
      min_lines: 20
    - path: packages/ui/src/lib/utils.ts
      contains: "export"
      min_lines: 5
    - path: packages/ui/src/components/index.ts
      contains: "export"
      min_lines: 5
    - path: packages/ui/package.json
      contains: "\"exports\""
      min_lines: 12
  key_links:
    - from: packages/ui/components.json
      to: packages/ui/src/styles/globals.css
      via: shadcn tailwind css path points at the real shared stylesheet.
      pattern: src/styles/globals\.css
    - from: packages/ui/package.json
      to: packages/ui/src/components/index.ts
      via: package exports expose the shared components barrel.
      pattern: components
---

<objective>
Stabilize the shared UI package boundary before any token or component work starts.

Purpose: Remove the broken alias and stylesheet assumptions identified in research so every later Phase 07 plan can build on one package contract.
Output: A real `packages/ui` style entrypoint, utility module, and explicit export surface for shared components and helpers.
</objective>

<execution_context>
@C:/Users/quoct/OneDrive/Desktop/code/designapp/trip-planner/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/quoct/OneDrive/Desktop/code/designapp/trip-planner/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-design-system-shared-shell-foundation/07-CONTEXT.md
@.planning/phases/07-design-system-shared-shell-foundation/07-RESEARCH.md
@.planning/phases/07-design-system-shared-shell-foundation/07-UI-SPEC.md
@packages/ui/package.json
@packages/ui/components.json
@apps/web/components.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the missing shared style and utility entrypoints</name>
  <files>packages/ui/src/styles/globals.css, packages/ui/src/lib/utils.ts, packages/ui/components.json</files>
  <read_first>
    packages/ui/components.json
    apps/web/components.json
    .planning/phases/07-design-system-shared-shell-foundation/07-CONTEXT.md
    .planning/phases/07-design-system-shared-shell-foundation/07-RESEARCH.md
    .planning/phases/07-design-system-shared-shell-foundation/07-UI-SPEC.md
  </read_first>
  <action>
    Create the foundational files that the repo already claims exist, before any primitive generation or hand-written component work begins.
    1. Add `packages/ui/src/styles/globals.css` as the authoritative shared stylesheet entrypoint referenced by both `components.json` files. Start with Tailwind v4 import, a small placeholder `@theme` scaffold, and a short comment noting that Plan `07-02` will replace placeholder values with the full Phase 07 token contract.
    2. Add `packages/ui/src/lib/utils.ts` with the shared `cn` helper expected by shadcn-generated and hand-written components. Use the standard `clsx` + `tailwind-merge` composition under the `@repo/ui/lib/utils` import contract instead of app-local helpers.
    3. Update `packages/ui/components.json` only if needed so the alias block resolves `components`, `ui`, `lib`, and `utils` to the files created above. Preserve the existing `radix-nova` preset per D-05; do not introduce a second component registry or a local `apps/web/@repo/*` workaround.
  </action>
  <verify>
    <automated>npm run check-types</automated>
  </verify>
  <acceptance_criteria>
    - `packages/ui/src/styles/globals.css` exists
    - `packages/ui/src/styles/globals.css` contains `@import "tailwindcss";`
    - `packages/ui/src/styles/globals.css` contains `@theme`
    - `packages/ui/src/lib/utils.ts` exists
    - `packages/ui/src/lib/utils.ts` contains `export`
    - `packages/ui/components.json` contains `"utils": "@repo/ui/lib/utils"`
    - `packages/ui/components.json` contains `"ui": "@repo/ui/components"`
  </acceptance_criteria>
  <done>The shared stylesheet and utility modules exist at the exact alias paths expected by the repo, with no remaining missing-file dependency for later plans.</done>
</task>

<task type="auto">
  <name>Task 2: Replace wildcard package exports with explicit shared-system entrypoints</name>
  <files>packages/ui/package.json, packages/ui/src/components/index.ts, packages/ui/src/index.ts</files>
  <read_first>
    packages/ui/package.json
    packages/ui/src/button.tsx
    packages/ui/src/card.tsx
    .planning/phases/07-design-system-shared-shell-foundation/07-RESEARCH.md
  </read_first>
  <action>
    Turn `@repo/ui` into an explicit package boundary instead of the current `./src/*.tsx` wildcard.
    1. Update `packages/ui/package.json` exports so the package exposes `./components`, `./lib/utils`, `./styles.css`, and the root `.` entrypoint that re-exports the components barrel. Keep the package private, but make the export map precise enough that later plans can add nested component files without changing import semantics again.
    2. Add `packages/ui/src/components/index.ts` as the shared barrel for future primitive and shell exports. Seed it with the existing scaffold components only as transitional placeholders to keep the package importable until Plans `07-03` and `07-04` replace them.
    3. Add `packages/ui/src/index.ts` that re-exports from `./components/index` and no app-specific modules. This plan is package-boundary work only; do not redesign any web route in this step.
  </action>
  <verify>
    <automated>npm run check-types</automated>
  </verify>
  <acceptance_criteria>
    - `packages/ui/package.json` contains `"./components"`
    - `packages/ui/package.json` contains `"./lib/utils"`
    - `packages/ui/package.json` contains `"./styles.css"`
    - `packages/ui/src/components/index.ts` exists
    - `packages/ui/src/components/index.ts` contains `button`
    - `packages/ui/src/components/index.ts` contains `card`
    - `packages/ui/src/index.ts` exists
    - `packages/ui/src/index.ts` contains `export * from "./components"`
  </acceptance_criteria>
  <done>`@repo/ui` exposes explicit, stable style, utility, and component entrypoints that later plans can build on without changing the package contract again.</done>
</task>

</tasks>

<verification>
- [ ] Run `npm run check-types`
- [ ] Confirm `packages/ui/src/styles/globals.css` exists and matches the CSS path already configured in both `components.json` files
- [ ] Confirm `packages/ui/package.json` no longer relies on the wildcard `./src/*.tsx` export pattern as the only package surface
</verification>

<success_criteria>
- `packages/ui` exposes `components`, `lib/utils`, and `styles.css` entrypoints
- The shared stylesheet path referenced by shadcn is real on disk
- Later Phase 07 plans can add shared components without revisiting package-boundary infrastructure
</success_criteria>

<output>
After completion, create `.planning/phases/07-design-system-shared-shell-foundation/07-01-SUMMARY.md`
</output>
