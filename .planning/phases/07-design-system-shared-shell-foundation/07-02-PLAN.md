---
phase: 07-design-system-shared-shell-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - packages/ui/src/styles/globals.css
  - apps/web/app/globals.css
  - apps/web/app/layout.tsx
autonomous: true
requirements:
  - DSYS-01
must_haves:
  truths:
    - The visual language for typography, color, spacing, radius, elevation, motion, and semantic surfaces is defined once in `packages/ui`.
    - The web app consumes the shared system by import, rather than keeping token authority inside app-local globals.
    - Semantic surface tiers are named and reusable, so later route redesign phases do not invent their own surface meanings.
  artifacts:
    - path: packages/ui/src/styles/globals.css
      contains: "--color"
      min_lines: 80
    - path: apps/web/app/globals.css
      contains: "@repo/ui/styles.css"
      min_lines: 5
    - path: apps/web/app/layout.tsx
      contains: "Be Vietnam Pro"
      min_lines: 10
  key_links:
    - from: apps/web/app/globals.css
      to: packages/ui/src/styles/globals.css
      via: app globals import the shared stylesheet instead of redefining the design language.
      pattern: @repo/ui/styles\.css
    - from: packages/ui/src/styles/globals.css
      to: apps/web/app/layout.tsx
      via: font variables and shared theme tokens are wired into the web root layout.
      pattern: font
---

<objective>
Move token authority into `packages/ui` and lock the shared semantic surface contract for Stitch-driven redesign work.

Purpose: Satisfy DSYS-01 and D-01/D-02 by making the shared system, not `apps/web`, the source of truth for typography, color, spacing, radius, elevation, motion, and semantic surface tiers.
Output: A shared stylesheet with the full Phase 07 token contract and a reduced web `globals.css` file that only wires shared styles into the app.
</objective>

<execution_context>
@C:/Users/quoct/OneDrive/Desktop/code/designapp/trip-planner/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/quoct/OneDrive/Desktop/code/designapp/trip-planner/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-design-system-shared-shell-foundation/07-CONTEXT.md
@.planning/phases/07-design-system-shared-shell-foundation/07-RESEARCH.md
@.planning/phases/07-design-system-shared-shell-foundation/07-UI-SPEC.md
@.planning/designs/stitch-v2/SCREEN-INVENTORY.md
@.planning/designs/stitch-v2/screens/dashboard/DESIGN.md
@packages/ui/src/styles/globals.css
@apps/web/app/globals.css
@apps/web/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Encode the Phase 07 token and semantic surface contract in the shared stylesheet</name>
  <files>packages/ui/src/styles/globals.css</files>
  <read_first>
    packages/ui/src/styles/globals.css
    .planning/phases/07-design-system-shared-shell-foundation/07-CONTEXT.md
    .planning/phases/07-design-system-shared-shell-foundation/07-RESEARCH.md
    .planning/phases/07-design-system-shared-shell-foundation/07-UI-SPEC.md
    .planning/designs/stitch-v2/screens/dashboard/DESIGN.md
  </read_first>
  <action>
    Replace the placeholder shared stylesheet with the real Phase 07 token authority per D-01, D-02, D-05, D-06, D-08, and D-09.
    1. Define explicit tokens for typography roles (`display`, `heading`, `body`, `label`) using `Be Vietnam Pro` for display/headings and `Plus Jakarta Sans` for body/labels.
    2. Define the spacing scale from the UI spec (`xs`, `sm`, `md`, `lg`, `xl`, `2xl`, `3xl`) and the radius/elevation/motion tokens from the contract, including the `44px` minimum target and large-shell/FAB radius values.
    3. Add semantic color and surface variables for app background, section surface, lifted card, high surface, glass overlay, CTA emphasis, urgency surface, success, discovery, destructive, and focus ring states. Use the exact Phase 07 palette values from `07-UI-SPEC.md`.
    4. Encode utility classes or CSS custom properties for the no-divider rule, glass blur, soft ambient shadows, and CTA gradient treatment so later component plans consume shared semantics instead of hardcoded hex values.
    5. Keep this stylesheet generic and reusable. Do not add landing-page-specific or dashboard-specific layout CSS in this step.
  </action>
  <verify>
    <automated>npm run check-types</automated>
  </verify>
  <acceptance_criteria>
    - `packages/ui/src/styles/globals.css` contains `--font-display`
    - `packages/ui/src/styles/globals.css` contains `--space-3xl`
    - `packages/ui/src/styles/globals.css` contains `--radius-hero`
    - `packages/ui/src/styles/globals.css` contains `--shadow-soft`
    - `packages/ui/src/styles/globals.css` contains `--surface-app`
    - `packages/ui/src/styles/globals.css` contains `--surface-section`
    - `packages/ui/src/styles/globals.css` contains `--surface-card`
    - `packages/ui/src/styles/globals.css` contains `--surface-glass`
    - `packages/ui/src/styles/globals.css` contains `--accent-primary: #a93000`
    - `packages/ui/src/styles/globals.css` contains `--status-discovery: #5a3bdd`
  </acceptance_criteria>
  <done>The shared stylesheet is the canonical home for the full Phase 07 design token and semantic surface contract, with no screen-specific layout logic mixed in.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce web globals to shared-style wiring and font integration</name>
  <files>apps/web/app/globals.css, apps/web/app/layout.tsx</files>
  <read_first>
    apps/web/app/globals.css
    apps/web/app/layout.tsx
    packages/ui/src/styles/globals.css
    .planning/phases/07-design-system-shared-shell-foundation/07-UI-SPEC.md
  </read_first>
  <action>
    Make the web app a consumer of the shared system rather than its owner.
    1. Update `apps/web/app/globals.css` so it imports `@repo/ui/styles.css` and keeps only app wiring or truly app-local overrides. Remove duplicated token authority from this file instead of maintaining two competing design systems.
    2. Update `apps/web/app/layout.tsx` so the root layout wires the shared typography contract with `Be Vietnam Pro` and `Plus Jakarta Sans` font variables applied at the app shell level.
    3. Preserve existing app boot behavior. This task is token migration work only, per D-05 through D-07; do not redesign `apps/web/app/page.tsx`, `apps/web/app/auth/login/page.tsx`, or `apps/web/app/dashboard/page.tsx` yet.
  </action>
  <verify>
    <automated>npm run check-types</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/globals.css` contains `@import "@repo/ui/styles.css";`
    - `apps/web/app/globals.css` does not contain `--color-brand-coral`
    - `apps/web/app/layout.tsx` contains `Be_Vietnam_Pro`
    - `apps/web/app/layout.tsx` contains `Plus_Jakarta_Sans`
    - `apps/web/app/layout.tsx` contains `variable`
  </acceptance_criteria>
  <done>The web app imports the shared design system and only keeps app-level wiring locally, while root typography is aligned to the Phase 07 font contract.</done>
</task>

</tasks>

<verification>
- [ ] Run `npm run check-types`
- [ ] Confirm the shared token values live in `packages/ui/src/styles/globals.css`
- [ ] Confirm `apps/web/app/globals.css` imports `@repo/ui/styles.css` and no longer owns the brand token definitions
</verification>

<success_criteria>
- Shared token authority lives in `packages/ui`
- Semantic surface tiers are named and reusable
- The web app consumes tokens by import instead of duplicating them locally
</success_criteria>

<output>
After completion, create `.planning/phases/07-design-system-shared-shell-foundation/07-02-SUMMARY.md`
</output>
