---
phase: 03-logistics-attendance
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/database/prisma/schema.prisma
  - apps/api/src/app.module.ts
  - apps/api/src/logistics/logistics.module.ts
  - apps/api/src/logistics/logistics.controller.ts
  - apps/api/src/logistics/logistics.service.ts
  - apps/api/src/logistics/dto/create-logistics-unit.dto.ts
  - apps/api/src/logistics/dto/reassign-logistics-member.dto.ts
  - apps/api/src/logistics/dto/self-join-logistics-slot.dto.ts
  - apps/api/src/logistics/dto/leave-logistics-slot.dto.ts
  - apps/api/src/logistics/logistics.service.spec.ts
  - apps/api/test/phase-03-logistics.e2e-spec.ts
  - apps/web/src/lib/api-client.ts
  - apps/web/src/components/trip/TripWorkspaceShell.tsx
  - apps/web/src/components/trip/LogisticsBoardTab.tsx
  - apps/web/src/components/trip/AllocationUnitCard.tsx
  - apps/web/src/components/trip/MemberChip.tsx
  - apps/web/src/components/trip/__tests__/logistics-board.test.tsx
autonomous: true
requirements:
  - LOGI-02
  - LOGI-03
user_setup: []
must_haves:
  truths:
    - Room and ride allocation is trip-scoped, capacity-aware, and enforced server-side in transactions.
    - Leaders can reassign anyone, while members can self-join open slots and leave their current slot without leader intervention.
    - The logistics UI appears as a first-class trip-planner tab and renders room/ride units as card-grid surfaces rather than hidden forms.
  artifacts:
    - path: packages/database/prisma/schema.prisma
      contains: model LogisticsUnit
      min_lines: 220
    - path: apps/api/src/logistics/logistics.service.ts
      contains: getAllocationSnapshot
      min_lines: 180
    - path: apps/web/src/components/trip/LogisticsBoardTab.tsx
      contains: Phân phòng/xe
      min_lines: 180
    - path: apps/api/test/phase-03-logistics.e2e-spec.ts
      contains: Phase 03 Logistics API
      min_lines: 80
  key_links:
    - from: apps/api/src/logistics/logistics.controller.ts
      to: apps/api/src/logistics/logistics.service.ts
      via: trip-scoped allocation endpoints delegate to the canonical allocation snapshot service.
      pattern: this\.logisticsService\.
    - from: apps/web/src/components/trip/TripWorkspaceShell.tsx
      to: apps/web/src/components/trip/LogisticsBoardTab.tsx
      via: the trip workspace mounts the logistics allocation tab as a first-class sibling in the planner.
      pattern: LogisticsBoardTab
---

<objective>
Create the Phase 3 allocation foundation for rooms and rides.

Purpose: Make allocation trustworthy before checklist and attendance work layer onto the same trip workspace.
Output: Prisma allocation models, NestJS allocation APIs, card-grid allocation UI, and tests for leader override plus member self-join.
</objective>

<execution_context>
@C:/Users/quoct/OneDrive/Desktop/code/inan/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/quoct/OneDrive/Desktop/code/inan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-logistics-attendance/03-CONTEXT.md
@.planning/phases/03-logistics-attendance/03-RESEARCH.md
@.planning/phases/03-logistics-attendance/03-VALIDATION.md
@.planning/phases/03-logistics-attendance/03-UI-SPEC.md
@apps/web/src/components/trip/TripWorkspaceShell.tsx
@apps/web/src/lib/api-client.ts
@apps/api/src/trips/trips.service.ts
@packages/database/prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add capacity-aware room and ride allocation models to Prisma</name>
  <files>packages/database/prisma/schema.prisma</files>
  <read_first>
    packages/database/prisma/schema.prisma
    .planning/phases/03-logistics-attendance/03-CONTEXT.md
    .planning/phases/03-logistics-attendance/03-RESEARCH.md
    .planning/phases/03-logistics-attendance/03-UI-SPEC.md
  </read_first>
  <action>
    Extend packages/database/prisma/schema.prisma with the Phase 3 allocation domain.
    1. Add enum LogisticsUnitType with ROOM and RIDE.
    2. Add enum LogisticsAssignmentSource with LEADER, SELF_JOIN, and AUTO_FILL.
    3. Add model LogisticsUnit with fields id String @id @default(cuid()), tripId String, type LogisticsUnitType, label String, capacity Int, sortOrder Int, note String?, createdAt DateTime @default(now()), updatedAt DateTime @updatedAt; relate it to Trip and AllocationAssignment; add @@index([tripId, type]) and @@unique([tripId, type, sortOrder]).
    4. Add model AllocationAssignment with fields id String @id @default(cuid()), tripId String, unitId String, tripMemberId String, createdByTripMemberId String?, source LogisticsAssignmentSource @default(LEADER), createdAt DateTime @default(now()), updatedAt DateTime @updatedAt; relate it to Trip, LogisticsUnit, TripMember as the assigned member, and TripMember as the creator when present.
    5. Enforce one occupancy per member per allocation type by storing unit type through the relation and adding a service-level transaction rule; in schema add @@unique([unitId, tripMemberId]) plus indexes on tripId and tripMemberId.
    6. Add relation fields on Trip and TripMember so Trip exposes logisticsUnits and allocationAssignments, while TripMember exposes logisticsAssignments and createdLogisticsAssignments.
  </action>
  <verify>npm exec prisma validate</verify>
  <acceptance_criteria>
    - packages/database/prisma/schema.prisma contains "enum LogisticsUnitType"
    - packages/database/prisma/schema.prisma contains "ROOM"
    - packages/database/prisma/schema.prisma contains "RIDE"
    - packages/database/prisma/schema.prisma contains "model LogisticsUnit"
    - packages/database/prisma/schema.prisma contains "capacity     Int"
    - packages/database/prisma/schema.prisma contains "model AllocationAssignment"
    - packages/database/prisma/schema.prisma contains "source       LogisticsAssignmentSource"
    - packages/database/prisma/schema.prisma contains "@@unique([tripId, type, sortOrder])"
  </acceptance_criteria>
  <done>The schema contains deterministic room/ride units and assignment records that can support leader override and member self-join.</done>
</task>

<task type="auto">
  <name>Task 2: Implement allocation APIs with leader override and member self-join rules</name>
  <files>apps/api/src/app.module.ts, apps/api/src/logistics/logistics.module.ts, apps/api/src/logistics/logistics.controller.ts, apps/api/src/logistics/logistics.service.ts, apps/api/src/logistics/dto/create-logistics-unit.dto.ts, apps/api/src/logistics/dto/reassign-logistics-member.dto.ts, apps/api/src/logistics/dto/self-join-logistics-slot.dto.ts, apps/api/src/logistics/dto/leave-logistics-slot.dto.ts</files>
  <read_first>
    apps/api/src/app.module.ts
    apps/api/src/trips/trips.service.ts
    packages/database/prisma/schema.prisma
    .planning/phases/03-logistics-attendance/03-CONTEXT.md
    .planning/phases/03-logistics-attendance/03-RESEARCH.md
  </read_first>
  <action>
    Add a new LogisticsModule and register it in apps/api/src/app.module.ts.
    1. In apps/api/src/logistics/logistics.controller.ts expose exactly these routes:
       - GET /trips/:tripId/logistics/allocations
       - POST /trips/:tripId/logistics/units
       - PATCH /trips/:tripId/logistics/units/:unitId
       - DELETE /trips/:tripId/logistics/units/:unitId
       - POST /trips/:tripId/logistics/assignments/self-join
       - POST /trips/:tripId/logistics/assignments/leave
       - POST /trips/:tripId/logistics/assignments/reassign
       - POST /trips/:tripId/logistics/auto-fill
    2. Build apps/api/src/logistics/logistics.service.ts around a getAllocationSnapshot(tripId, userId) method that returns room units, ride units, assignment occupancy counts, member role flags, and open-slot counts ready for card-grid rendering.
    3. Enforce exactly these permission rules in service methods:
       - only leaders can create, edit, delete, reassign, or auto-fill units
       - members can only self-join an existing unit with remaining capacity
       - members can leave their own current assignment for a given type through the leave endpoint
       - self-join must reject when the member already has an assignment in another unit of the same type
    4. Implement all assignment writes inside Prisma transactions so simultaneous self-join attempts cannot overbook capacity.
    5. The auto-fill endpoint must only distribute currently unassigned members for the requested type and must never remove existing manual placements.
    6. When a leader lowers a unit capacity below current occupancy, do not auto-eject members. Instead, keep assignments intact and mark the unit snapshot as overbooked with overCapacityBy and isOverbooked so the UI can render a warning state until the leader resolves it.
  </action>
  <verify>npm run test -w apps/api -- --runInBand</verify>
  <acceptance_criteria>
    - apps/api/src/app.module.ts contains "LogisticsModule"
    - apps/api/src/logistics/logistics.controller.ts contains "@Controller('trips/:tripId/logistics')"
    - apps/api/src/logistics/logistics.controller.ts contains "assignments/self-join"
    - apps/api/src/logistics/logistics.controller.ts contains "assignments/leave"
    - apps/api/src/logistics/logistics.controller.ts contains "assignments/reassign"
    - apps/api/src/logistics/logistics.service.ts contains "getAllocationSnapshot"
    - apps/api/src/logistics/logistics.service.ts contains "autoFill"
    - apps/api/src/logistics/logistics.service.ts contains "remainingCapacity"
    - apps/api/src/logistics/logistics.service.ts contains "isOverbooked"
    - apps/api/src/logistics/logistics.service.ts contains "SELF_JOIN"
  </acceptance_criteria>
  <done>The API exposes a canonical allocation snapshot and enforces leader-first authority plus member self-join boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: Build the card-grid allocation tab and its tests in the trip workspace</name>
  <files>apps/web/src/lib/api-client.ts, apps/web/src/components/trip/TripWorkspaceShell.tsx, apps/web/src/components/trip/LogisticsBoardTab.tsx, apps/web/src/components/trip/AllocationUnitCard.tsx, apps/web/src/components/trip/MemberChip.tsx, apps/web/src/components/trip/__tests__/logistics-board.test.tsx, apps/api/src/logistics/logistics.service.spec.ts, apps/api/test/phase-03-logistics.e2e-spec.ts</files>
  <read_first>
    apps/web/src/components/trip/TripWorkspaceShell.tsx
    apps/web/src/lib/api-client.ts
    apps/web/app/globals.css
    .planning/phases/03-logistics-attendance/03-UI-SPEC.md
    .planning/phases/03-logistics-attendance/03-VALIDATION.md
    apps/api/src/logistics/logistics.service.ts
  </read_first>
  <action>
    Extend the trip workspace so Phase 03 becomes visible to users.
    1. In apps/web/src/lib/api-client.ts add typed contracts and methods for the allocation snapshot and the three write actions: create/update/delete unit, self-join, reassign, and auto-fill.
    2. In apps/web/src/components/trip/TripWorkspaceShell.tsx add the first new logistics tab labeled exactly "Phân phòng/xe" and mount a new LogisticsBoardTab component instead of hiding allocation behind a deep link.
    3. Build apps/web/src/components/trip/LogisticsBoardTab.tsx to render room and ride units as a card grid:
       - cards show label, type badge, occupancy count, note, and member chips
       - leaders see "Tạo chỗ mới" and "Chia nhanh" actions
       - members see "Vào chỗ này" only on units with remaining capacity
       - members see a leave action on the unit they currently occupy
       - full units render a disabled occupancy state
       - touch-safe fallback actions such as "Chuyển chỗ" exist in addition to drag affordances
       - overbooked units render a dedicated warning state such as coral emphasis, "Đang quá số chỗ", and a visible over-capacity count
    4. Build AllocationUnitCard and MemberChip as reusable presentational pieces so later checklist and attendance work can reuse status-chip styling.
    5. Add apps/api/src/logistics/logistics.service.spec.ts coverage for capacity checks, duplicate-assignment prevention, self-join, self-leave, leader override, and overbooked-capacity warnings; add apps/api/test/phase-03-logistics.e2e-spec.ts for the leader-create plus member-self-join-and-leave flow; add apps/web/src/components/trip/__tests__/logistics-board.test.tsx for card-grid rendering, overbooked state rendering, and role-based actions.
  </action>
  <verify>npm run test -w apps/api -- --runInBand && npm run lint -w apps/web && npm run check-types -w apps/web && npm run test:ui -w apps/web</verify>
  <acceptance_criteria>
    - apps/web/src/components/trip/TripWorkspaceShell.tsx contains "Phân phòng/xe"
    - apps/web/src/components/trip/TripWorkspaceShell.tsx contains "LogisticsBoardTab"
    - apps/web/src/components/trip/LogisticsBoardTab.tsx contains "Tạo chỗ mới"
    - apps/web/src/components/trip/LogisticsBoardTab.tsx contains "Chia nhanh"
    - apps/web/src/components/trip/LogisticsBoardTab.tsx contains "Vào chỗ này"
    - apps/web/src/components/trip/LogisticsBoardTab.tsx contains "Rời chỗ này"
    - apps/web/src/components/trip/LogisticsBoardTab.tsx contains "Đang quá số chỗ"
    - apps/api/src/logistics/logistics.service.spec.ts contains "describe('LogisticsService'"
    - apps/api/test/phase-03-logistics.e2e-spec.ts contains "describe('Phase 03 Logistics API'"
    - apps/web/src/components/trip/__tests__/logistics-board.test.tsx contains "Phân phòng/xe"
  </acceptance_criteria>
  <done>The trip workspace exposes a first-class allocation tab with the card-grid UX and validation coverage promised in the UI and research contracts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `npm exec prisma validate`
- [ ] Run `npm run test -w apps/api -- --runInBand`
- [ ] Run `npm run lint -w apps/web && npm run check-types -w apps/web && npm run test:ui -w apps/web`
- [ ] Confirm leader and member role branching is visible directly in the allocation tab
</verification>

<success_criteria>
- Room and ride units render as capacity-aware cards inside the trip workspace
- Members can self-join only valid open slots and can also leave their own slot
- Leaders can override assignments and use an explicit auto-fill helper
- Overbooked states are explicit and recoverable instead of silently breaking the UI
- Allocation rules are covered by backend and UI tests before later logistics features build on them
</success_criteria>

<output>
After completion, create `.planning/phases/03-logistics-attendance/03-01-SUMMARY.md`
</output>
