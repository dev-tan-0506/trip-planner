---
phase: 03-logistics-attendance
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - packages/database/prisma/schema.prisma
  - apps/api/src/app.module.ts
  - apps/api/src/attendance/attendance.module.ts
  - apps/api/src/attendance/attendance.controller.ts
  - apps/api/src/attendance/attendance.service.ts
  - apps/api/src/attendance/attendance.gateway.ts
  - apps/api/src/attendance/proof-storage.service.ts
  - apps/api/src/attendance/dto/create-attendance-session.dto.ts
  - apps/api/src/attendance/dto/create-attendance-submission.dto.ts
  - apps/api/src/attendance/attendance.service.spec.ts
  - apps/api/test/phase-03-attendance.e2e-spec.ts
  - apps/web/src/lib/api-client.ts
  - apps/web/src/components/trip/TripWorkspaceShell.tsx
  - apps/web/src/components/trip/AttendanceTab.tsx
  - apps/web/src/components/trip/AttendanceMapPanel.tsx
  - apps/web/src/components/trip/AttendanceMemberList.tsx
  - apps/web/src/components/trip/CheckInCaptureSheet.tsx
  - apps/web/src/components/trip/__tests__/attendance-panel.test.tsx
autonomous: true
requirements:
  - LOGI-04
  - LOGI-05
user_setup: []
must_haves:
  truths:
    - Attendance is session-based: leader opens a check-in round for a point and time window, not an always-on tracker.
    - Members submit selfie proof and may attach browser location when available, without location being a hard blocker.
    - Attendance proof storage uses an explicit first-pass strategy rather than leaving image persistence undefined.
    - The leader dashboard prioritizes map awareness first, then detailed member status below it, with realtime refresh semantics.
  artifacts:
    - path: packages/database/prisma/schema.prisma
      contains: model AttendanceSession
      min_lines: 300
    - path: apps/api/src/attendance/attendance.service.ts
      contains: getCurrentAttendanceSnapshot
      min_lines: 200
    - path: apps/api/src/attendance/proof-storage.service.ts
      contains: saveProofImage
      min_lines: 40
    - path: apps/api/src/attendance/attendance.gateway.ts
      contains: attendance.snapshot
      min_lines: 60
    - path: apps/web/src/components/trip/AttendanceTab.tsx
      contains: Mở check-in
      min_lines: 220
  key_links:
    - from: apps/api/src/attendance/attendance.controller.ts
      to: apps/api/src/attendance/attendance.service.ts
      via: check-in session and proof routes flow through one attendance snapshot service.
      pattern: this\.attendanceService\.
    - from: apps/api/src/attendance/attendance.gateway.ts
      to: apps/api/src/attendance/attendance.service.ts
      via: websocket reconnect and broadcast use fresh snapshot reads rather than optimistic deltas only.
      pattern: getCurrentAttendanceSnapshot
---

<objective>
Create the Phase 3 attendance and realtime dashboard flow.

Purpose: Give the leader a trustworthy live picture of who has reached the meeting point, backed by selfie proof and optional location.
Output: Attendance session models, check-in APIs plus websocket gateway, map-first attendance UI, capture sheet, and automated coverage for proof submission.
</objective>

<execution_context>
@C:/Users/quoct/OneDrive/Desktop/code/inan/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/quoct/OneDrive/Desktop/code/inan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-logistics-attendance/03-CONTEXT.md
@.planning/phases/03-logistics-attendance/03-RESEARCH.md
@.planning/phases/03-logistics-attendance/03-VALIDATION.md
@.planning/phases/03-logistics-attendance/03-UI-SPEC.md
@packages/database/prisma/schema.prisma
@apps/api/src/votes/votes.gateway.ts
@apps/web/src/components/trip/TripWorkspaceShell.tsx
@apps/web/src/lib/api-client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add attendance session and submission models to Prisma</name>
  <files>packages/database/prisma/schema.prisma</files>
  <read_first>
    packages/database/prisma/schema.prisma
    .planning/phases/03-logistics-attendance/03-CONTEXT.md
    .planning/phases/03-logistics-attendance/03-RESEARCH.md
    .planning/phases/03-logistics-attendance/03-UI-SPEC.md
  </read_first>
  <action>
    Extend packages/database/prisma/schema.prisma with the attendance domain.
    1. Add enum AttendanceSessionStatus with OPEN and CLOSED.
    2. Add enum AttendanceLocationStatus with GRANTED, DENIED, and UNAVAILABLE.
    3. Add model AttendanceSession with fields id String @id @default(cuid()), tripId String, createdByTripMemberId String, title String, meetingLabel String, meetingAddress String, lat Float?, lng Float?, opensAt DateTime, closesAt DateTime, status AttendanceSessionStatus @default(OPEN), createdAt DateTime @default(now()), updatedAt DateTime @updatedAt; relate to Trip, TripMember, and AttendanceSubmission.
    4. Add model AttendanceSubmission with fields id String @id @default(cuid()), sessionId String, tripMemberId String, photoUrl String, submittedAt DateTime @default(now()), lat Float?, lng Float?, accuracyMeters Float?, locationStatus AttendanceLocationStatus @default(UNAVAILABLE), createdAt DateTime @default(now()), updatedAt DateTime @updatedAt; relate to AttendanceSession and TripMember; add @@unique([sessionId, tripMemberId]) so one latest proof per member per round exists.
    5. Add relation fields on Trip and TripMember so Trip exposes attendanceSessions while TripMember exposes createdAttendanceSessions and attendanceSubmissions.
  </action>
  <verify>npm exec prisma validate</verify>
  <acceptance_criteria>
    - packages/database/prisma/schema.prisma contains "enum AttendanceSessionStatus"
    - packages/database/prisma/schema.prisma contains "enum AttendanceLocationStatus"
    - packages/database/prisma/schema.prisma contains "model AttendanceSession"
    - packages/database/prisma/schema.prisma contains "meetingAddress String"
    - packages/database/prisma/schema.prisma contains "model AttendanceSubmission"
    - packages/database/prisma/schema.prisma contains "photoUrl String"
    - packages/database/prisma/schema.prisma contains "@@unique([sessionId, tripMemberId])"
  </acceptance_criteria>
  <done>The schema can represent a check-in round and one latest proof per member inside that round.</done>
</task>

<task type="auto">
  <name>Task 2: Implement attendance APIs and websocket snapshot broadcasting</name>
  <files>apps/api/src/app.module.ts, apps/api/src/attendance/attendance.module.ts, apps/api/src/attendance/attendance.controller.ts, apps/api/src/attendance/attendance.service.ts, apps/api/src/attendance/attendance.gateway.ts, apps/api/src/attendance/proof-storage.service.ts, apps/api/src/attendance/dto/create-attendance-session.dto.ts, apps/api/src/attendance/dto/create-attendance-submission.dto.ts, apps/api/src/attendance/attendance.service.spec.ts, apps/api/test/phase-03-attendance.e2e-spec.ts</files>
  <read_first>
    apps/api/src/app.module.ts
    apps/api/src/votes/votes.gateway.ts
    apps/api/src/trips/trips.service.ts
    packages/database/prisma/schema.prisma
    .planning/phases/03-logistics-attendance/03-RESEARCH.md
    .planning/phases/03-logistics-attendance/03-VALIDATION.md
  </read_first>
  <action>
    Add a new AttendanceModule and register it in apps/api/src/app.module.ts.
    1. In apps/api/src/attendance/attendance.controller.ts expose exactly these routes:
       - GET /trips/:tripId/attendance/sessions/current
       - POST /trips/:tripId/attendance/sessions
       - POST /attendance/sessions/:sessionId/submissions
       - POST /attendance/sessions/:sessionId/close
    2. Add apps/api/src/attendance/proof-storage.service.ts that implements a first-pass proof strategy for Phase 3: accept a browser-generated data URL or uploaded file payload, persist it as a deterministic local proof asset under an attendance-proof directory inside the app workspace, and return a stable relative proof URL/path that can later be swapped for cloud storage without changing the service contract. Do not leave proof storage implicit.
    3. Build apps/api/src/attendance/attendance.service.ts around getCurrentAttendanceSnapshot(tripId, userId) that returns the active session, meeting point, leader/member flags, counts for arrived/missing/partial-location, map-ready submissions, and detailed member rows ordered with missing members first.
    3. Enforce exactly these rules:
       - only leaders can open or close a session
       - members can submit only when a session is OPEN and current time is before closesAt
       - location is optional; denied or unavailable location still stores a valid submission with a clear locationStatus
       - a later submission from the same member replaces the previous proof for that same session through the sessionId+tripMemberId unique key
    4. Implement apps/api/src/attendance/attendance.gateway.ts by reusing the vote gateway pattern:
       - namespace /attendance
       - join event subscribes the client to an attendance-session room
       - join always emits a fresh attendance.snapshot payload
       - broadcast attendance.updated after submissions or close actions
       - keep the attendance socket client isolated from the existing /votes socket flow by using separate namespace-specific connect helpers and explicit cleanup on unmount
    5. Add apps/api/src/attendance/attendance.service.spec.ts for session window, optional location, proof-storage integration, namespace-safe snapshot broadcasting, and resubmission logic; add apps/api/test/phase-03-attendance.e2e-spec.ts for leader-open plus member-submit flow.
  </action>
  <verify>npm run test -w apps/api -- --runInBand</verify>
  <acceptance_criteria>
    - apps/api/src/app.module.ts contains "AttendanceModule"
    - apps/api/src/attendance/attendance.controller.ts contains "@Controller('trips/:tripId/attendance')"
    - apps/api/src/attendance/attendance.controller.ts contains "sessions/current"
    - apps/api/src/attendance/attendance.service.ts contains "getCurrentAttendanceSnapshot"
    - apps/api/src/attendance/proof-storage.service.ts contains "saveProofImage"
    - apps/api/src/attendance/attendance.service.ts contains "AttendanceLocationStatus"
    - apps/api/src/attendance/attendance.gateway.ts contains "namespace: '/attendance'"
    - apps/api/src/attendance/attendance.gateway.ts contains "attendance.snapshot"
    - apps/api/src/attendance/attendance.service.spec.ts contains "describe('AttendanceService'"
    - apps/api/test/phase-03-attendance.e2e-spec.ts contains "describe('Phase 03 Attendance API'"
  </acceptance_criteria>
  <done>The attendance backend can open rounds, store proof submissions with optional location, and broadcast fresh snapshots to connected dashboards.</done>
</task>

<task type="auto">
  <name>Task 3: Build the map-first attendance tab, proof capture sheet, and UI tests</name>
  <files>apps/web/src/lib/api-client.ts, apps/web/src/components/trip/TripWorkspaceShell.tsx, apps/web/src/components/trip/AttendanceTab.tsx, apps/web/src/components/trip/AttendanceMapPanel.tsx, apps/web/src/components/trip/AttendanceMemberList.tsx, apps/web/src/components/trip/CheckInCaptureSheet.tsx, apps/web/src/components/trip/__tests__/attendance-panel.test.tsx</files>
  <read_first>
    apps/web/src/components/trip/TripWorkspaceShell.tsx
    apps/web/src/lib/api-client.ts
    apps/web/app/globals.css
    .planning/phases/03-logistics-attendance/03-UI-SPEC.md
    apps/api/src/attendance/attendance.service.ts
  </read_first>
  <action>
    Add the third logistics tab labeled exactly "Check-in" to the trip workspace and implement the full proof flow.
    1. In apps/web/src/lib/api-client.ts add typed attendance snapshot contracts, isolated socket helpers for /attendance, and methods for session creation, proof submission, and close session. Keep attendance socket lifecycle separate from the existing /votes client so reconnect and teardown behavior do not leak between namespaces.
    2. Build apps/web/src/components/trip/AttendanceTab.tsx to follow the map-first contract:
       - top summary with meeting point and time window
       - status counts for "Đã đến", "Chưa đến", and "Thiếu vị trí"
       - AttendanceMapPanel as the first major content block
       - AttendanceMemberList below the map with missing members shown first
    3. Build CheckInCaptureSheet so members can:
       - open a clear primary CTA labeled "Chụp ảnh check-in" or "Tải ảnh check-in"
       - prepare a browser photo or file-upload fallback
       - request geolocation after proof intent
       - submit even when location is denied or unavailable
       - see explicit success state after submission
    4. The payload sent from CheckInCaptureSheet must match the first-pass storage strategy above: either uploaded file content or a browser-captured data URL is handed to the backend storage service, and the UI must not assume cloud-hosted URLs already exist.
    5. Keep overlays stable: the capture sheet must not auto-close while the user is interacting with camera, upload preparation, or location capture.
    6. Add apps/web/src/components/trip/__tests__/attendance-panel.test.tsx for dashboard-state rendering, missing-vs-arrived ordering, camera/upload fallback states, and attendance socket setup/cleanup behavior.
  </action>
  <verify>npm run lint -w apps/web && npm run check-types -w apps/web && npm run test:ui -w apps/web</verify>
  <acceptance_criteria>
    - apps/web/src/components/trip/TripWorkspaceShell.tsx contains "Check-in"
    - apps/web/src/components/trip/TripWorkspaceShell.tsx contains "AttendanceTab"
    - apps/web/src/components/trip/AttendanceTab.tsx contains "Mở check-in"
    - apps/web/src/components/trip/AttendanceTab.tsx contains "Đã đến"
    - apps/web/src/components/trip/AttendanceTab.tsx contains "Chưa đến"
    - apps/web/src/components/trip/AttendanceMapPanel.tsx contains "map"
    - apps/web/src/components/trip/CheckInCaptureSheet.tsx contains "Chụp ảnh check-in"
    - apps/web/src/components/trip/CheckInCaptureSheet.tsx contains "Tải ảnh check-in"
    - apps/web/src/components/trip/__tests__/attendance-panel.test.tsx contains "Check-in"
  </acceptance_criteria>
  <done>The attendance UX matches the UI contract: map first for leaders, lightweight proof capture for members, and live session feedback after submission.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `npm exec prisma validate`
- [ ] Run `npm run test -w apps/api -- --runInBand`
- [ ] Run `npm run lint -w apps/web && npm run check-types -w apps/web && npm run test:ui -w apps/web`
- [ ] Confirm the leader dashboard shows the map block before the member list and updates after a submission without a manual page reload
</verification>

<success_criteria>
- Leaders can open and close attendance rounds with a defined place and time window
- Members can submit selfie proof with camera or upload fallback and optional location
- Proof-image persistence is explicitly handled by a first-pass local storage service instead of an undefined future upload path
- The attendance dashboard is map-first and feels live enough for coordination use
- Attendance proof and dashboard rules are covered by backend and UI tests
</success_criteria>

<output>
After completion, create `.planning/phases/03-logistics-attendance/03-03-SUMMARY.md`
</output>
