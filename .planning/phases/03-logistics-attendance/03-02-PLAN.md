---
phase: 03-logistics-attendance
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - packages/database/prisma/schema.prisma
  - apps/api/src/app.module.ts
  - apps/api/src/checklists/checklists.module.ts
  - apps/api/src/checklists/checklists.controller.ts
  - apps/api/src/checklists/checklists.service.ts
  - apps/api/src/checklists/dto/create-checklist-group.dto.ts
  - apps/api/src/checklists/dto/create-checklist-item.dto.ts
  - apps/api/src/checklists/dto/update-checklist-item.dto.ts
  - apps/api/src/checklists/checklists.service.spec.ts
  - apps/web/src/lib/api-client.ts
  - apps/web/src/components/trip/TripWorkspaceShell.tsx
  - apps/web/src/components/trip/ChecklistTab.tsx
  - apps/web/src/components/trip/ChecklistCategoryAccordion.tsx
  - apps/web/src/components/trip/ChecklistItemRow.tsx
  - apps/web/src/components/trip/__tests__/checklist-panel.test.tsx
autonomous: true
requirements:
  - LOGI-01
user_setup: []
must_haves:
  truths:
    - Checklist state supports both shared categories and personal tasks in the same trip-scoped feature.
    - Leaders can create and assign checklist items, while members can clearly see, filter, and complete their own responsibilities.
    - The checklist UI follows the mixed-board contract: accordion categories above, personal task area below.
  artifacts:
    - path: packages/database/prisma/schema.prisma
      contains: model ChecklistGroup
      min_lines: 260
    - path: apps/api/src/checklists/checklists.service.ts
      contains: getChecklistSnapshot
      min_lines: 160
    - path: apps/web/src/components/trip/ChecklistTab.tsx
      contains: Checklist còn trống
      min_lines: 180
  key_links:
    - from: apps/api/src/checklists/checklists.controller.ts
      to: apps/api/src/checklists/checklists.service.ts
      via: checklist routes flow through one trip-scoped checklist snapshot service.
      pattern: this\.checklistsService\.
    - from: apps/web/src/components/trip/TripWorkspaceShell.tsx
      to: apps/web/src/components/trip/ChecklistTab.tsx
      via: the trip workspace surfaces checklist as its own top-level logistics tab.
      pattern: ChecklistTab
---

<objective>
Create the Phase 3 checklist and packing coordination layer.

Purpose: Turn trip logistics into visible group responsibility, not ad-hoc chat reminders.
Output: Checklist groups and items in Prisma, checklist APIs, mixed-board checklist UI, and tests for ownership plus completion behavior.
</objective>

<execution_context>
@C:/Users/quoct/OneDrive/Desktop/code/inan/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/quoct/OneDrive/Desktop/code/inan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-logistics-attendance/03-CONTEXT.md
@.planning/phases/03-logistics-attendance/03-RESEARCH.md
@.planning/phases/03-logistics-attendance/03-VALIDATION.md
@.planning/phases/03-logistics-attendance/03-UI-SPEC.md
@packages/database/prisma/schema.prisma
@apps/web/src/components/trip/TripWorkspaceShell.tsx
@apps/web/src/lib/api-client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add combined checklist and personal-task models to Prisma</name>
  <files>packages/database/prisma/schema.prisma</files>
  <read_first>
    packages/database/prisma/schema.prisma
    .planning/phases/03-logistics-attendance/03-CONTEXT.md
    .planning/phases/03-logistics-attendance/03-RESEARCH.md
    .planning/phases/03-logistics-attendance/03-UI-SPEC.md
  </read_first>
  <action>
    Extend packages/database/prisma/schema.prisma with the checklist domain.
    1. Add enum ChecklistGroupKind with SHARED_CATEGORY and PERSONAL_TASKS.
    2. Add enum ChecklistItemStatus with TODO and DONE.
    3. Add model ChecklistGroup with fields id String @id @default(cuid()), tripId String, title String, kind ChecklistGroupKind, sortOrder Int, createdAt DateTime @default(now()), updatedAt DateTime @updatedAt; relate to Trip and ChecklistItem; add @@unique([tripId, kind, sortOrder]).
    4. Add model ChecklistItem with fields id String @id @default(cuid()), tripId String, groupId String, title String, notes String?, assigneeTripMemberId String?, status ChecklistItemStatus @default(TODO), sortOrder Int, completedAt DateTime?, createdAt DateTime @default(now()), updatedAt DateTime @updatedAt; relate to Trip, ChecklistGroup, and TripMember as assignee; add @@index([tripId, status]) and @@unique([groupId, sortOrder]).
    5. Add relation fields on Trip and TripMember so Trip exposes checklistGroups and checklistItems while TripMember exposes assignedChecklistItems.
  </action>
  <verify>npm exec prisma validate</verify>
  <acceptance_criteria>
    - packages/database/prisma/schema.prisma contains "enum ChecklistGroupKind"
    - packages/database/prisma/schema.prisma contains "SHARED_CATEGORY"
    - packages/database/prisma/schema.prisma contains "PERSONAL_TASKS"
    - packages/database/prisma/schema.prisma contains "model ChecklistGroup"
    - packages/database/prisma/schema.prisma contains "model ChecklistItem"
    - packages/database/prisma/schema.prisma contains "status       ChecklistItemStatus"
    - packages/database/prisma/schema.prisma contains "@@unique([groupId, sortOrder])"
  </acceptance_criteria>
  <done>The schema can represent both shared packing categories and assigned personal tasks in one trip-scoped checklist system.</done>
</task>

<task type="auto">
  <name>Task 2: Implement checklist APIs and snapshot logic</name>
  <files>apps/api/src/app.module.ts, apps/api/src/checklists/checklists.module.ts, apps/api/src/checklists/checklists.controller.ts, apps/api/src/checklists/checklists.service.ts, apps/api/src/checklists/dto/create-checklist-group.dto.ts, apps/api/src/checklists/dto/create-checklist-item.dto.ts, apps/api/src/checklists/dto/update-checklist-item.dto.ts, apps/api/src/checklists/checklists.service.spec.ts</files>
  <read_first>
    apps/api/src/app.module.ts
    apps/api/src/trips/trips.service.ts
    packages/database/prisma/schema.prisma
    .planning/phases/03-logistics-attendance/03-RESEARCH.md
    .planning/phases/03-logistics-attendance/03-VALIDATION.md
  </read_first>
  <action>
    Add a new ChecklistsModule and register it in apps/api/src/app.module.ts.
    1. In apps/api/src/checklists/checklists.controller.ts expose exactly these routes:
       - GET /trips/:tripId/checklists
       - POST /trips/:tripId/checklists/groups
       - PATCH /trips/:tripId/checklists/groups/:groupId
       - POST /trips/:tripId/checklists/items
       - PATCH /trips/:tripId/checklists/items/:itemId
       - POST /trips/:tripId/checklists/items/:itemId/toggle
       - DELETE /trips/:tripId/checklists/items/:itemId
    2. Build apps/api/src/checklists/checklists.service.ts around getChecklistSnapshot(tripId, userId) that returns shared groups ordered by sortOrder, personal-task items grouped separately, assignee labels, completion counts, and flags such as isLeader and canToggleSelf.
    3. Enforce leader-only creation and structure editing for groups and items, but allow members to toggle completion for checklist items assigned to themselves.
    4. When toggling to DONE, persist completedAt with the current timestamp; when toggling back to TODO, clear completedAt.
    5. Add apps/api/src/checklists/checklists.service.spec.ts coverage for group ordering, assignment ownership, and completion toggling.
  </action>
  <verify>npm run test -w apps/api -- --runInBand</verify>
  <acceptance_criteria>
    - apps/api/src/app.module.ts contains "ChecklistsModule"
    - apps/api/src/checklists/checklists.controller.ts contains "@Controller('trips/:tripId/checklists')"
    - apps/api/src/checklists/checklists.controller.ts contains "items/:itemId/toggle"
    - apps/api/src/checklists/checklists.service.ts contains "getChecklistSnapshot"
    - apps/api/src/checklists/checklists.service.ts contains "canToggleSelf"
    - apps/api/src/checklists/checklists.service.spec.ts contains "describe('ChecklistsService'"
    - apps/api/src/checklists/checklists.service.spec.ts contains "toggle"
  </acceptance_criteria>
  <done>The API exposes a mixed shared-and-personal checklist snapshot with the ownership rules defined in the context.</done>
</task>

<task type="auto">
  <name>Task 3: Build the mixed-board checklist tab and UI tests</name>
  <files>apps/web/src/lib/api-client.ts, apps/web/src/components/trip/TripWorkspaceShell.tsx, apps/web/src/components/trip/ChecklistTab.tsx, apps/web/src/components/trip/ChecklistCategoryAccordion.tsx, apps/web/src/components/trip/ChecklistItemRow.tsx, apps/web/src/components/trip/__tests__/checklist-panel.test.tsx</files>
  <read_first>
    apps/web/src/components/trip/TripWorkspaceShell.tsx
    apps/web/src/lib/api-client.ts
    apps/web/app/globals.css
    .planning/phases/03-logistics-attendance/03-UI-SPEC.md
    apps/api/src/checklists/checklists.service.ts
  </read_first>
  <action>
    Extend the trip workspace with the second logistics tab labeled exactly "Checklist".
    1. In apps/web/src/lib/api-client.ts add typed checklist snapshot contracts and methods for group creation, item creation, item update, and item toggle.
    2. Build apps/web/src/components/trip/ChecklistTab.tsx to follow the mixed-board contract:
       - top section renders shared categories as accordions
       - bottom section renders personal tasks in a dedicated container
       - leaders see "Thêm vật dụng" and item assignment controls
       - members see their assigned items highlighted clearly
       - members can toggle a filter labeled exactly "Chỉ xem việc của tôi" to reduce noise in larger groups
       - completed items stay visible with muted styling and a success accent
    3. Build ChecklistCategoryAccordion and ChecklistItemRow as reusable pieces with owner chips, status chips, and inline toggle affordances.
    4. Add apps/web/src/components/trip/__tests__/checklist-panel.test.tsx to cover empty states, shared category rendering, personal-task separation, the "Chỉ xem việc của tôi" filter, and role-based actions.
  </action>
  <verify>npm run lint -w apps/web && npm run check-types -w apps/web && npm run test:ui -w apps/web</verify>
  <acceptance_criteria>
    - apps/web/src/components/trip/TripWorkspaceShell.tsx contains "Checklist"
    - apps/web/src/components/trip/TripWorkspaceShell.tsx contains "ChecklistTab"
    - apps/web/src/components/trip/ChecklistTab.tsx contains "Checklist còn trống"
    - apps/web/src/components/trip/ChecklistTab.tsx contains "Thêm vật dụng"
    - apps/web/src/components/trip/ChecklistTab.tsx contains "Chỉ xem việc của tôi"
    - apps/web/src/components/trip/ChecklistTab.tsx contains "Giấy tờ"
    - apps/web/src/components/trip/__tests__/checklist-panel.test.tsx contains "Checklist"
  </acceptance_criteria>
  <done>The checklist UI reflects the combined shared-category plus personal-task model and is visible as its own top-level logistics tab.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `npm exec prisma validate`
- [ ] Run `npm run test -w apps/api -- --runInBand`
- [ ] Run `npm run lint -w apps/web && npm run check-types -w apps/web && npm run test:ui -w apps/web`
- [ ] Confirm assigned items remain readable in both shared categories and personal-task sections
</verification>

<success_criteria>
- Leaders can create shared categories and assign owners to checklist items
- Members can quickly identify and complete their own responsibilities, including through a focused self-only filter
- Checklist UI follows the mixed-board contract from the UI-SPEC
- Checklist ownership and completion rules are covered by automated tests
</success_criteria>

<output>
After completion, create `.planning/phases/03-logistics-attendance/03-02-SUMMARY.md`
</output>
