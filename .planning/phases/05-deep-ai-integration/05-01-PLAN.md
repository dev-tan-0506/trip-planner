---
phase: 05-deep-ai-integration
plan: "01"
type: execute
wave: 1
depends_on:
  - "05-00"
files_modified:
  - apps/api/src/itinerary/itinerary.controller.ts
  - apps/api/src/itinerary/itinerary.service.ts
  - apps/api/src/itinerary/dto/request-culinary-route.dto.ts
  - apps/api/src/itinerary/dto/apply-culinary-route.dto.ts
  - apps/api/src/itinerary/itinerary.service.spec.ts
  - apps/api/test/phase-05-deep-ai-integration.e2e-spec.ts
  - apps/web/src/lib/api-client.ts
  - apps/web/src/components/trip/TripWorkspaceShell.tsx
  - apps/web/src/components/trip/TimelineDaySection.tsx
  - apps/web/src/components/trip/AiAssistTab.tsx
  - apps/web/src/components/trip/CulinaryRouteCard.tsx
  - apps/web/src/components/trip/HealthConflictBadge.tsx
  - apps/web/src/components/trip/__tests__/phase-05-ai.test.tsx
autonomous: true
requirements:
  - PLAN-04
  - PLAN-05
user_setup: []
must_haves:
  truths:
    - TripWorkspaceShell remains the main Phase 5 entry point, and AI is introduced as a trip-scoped surface instead of a separate product area.
    - Health profile matching is advisory and inline on itinerary items; it never blocks normal editing or silently rewrites trip data.
    - Culinary routing returns an ordered suggestion with short reasoning and a visible review step before the leader applies it to canonical itinerary order.
    - Confidence and severity states are visible in both API payloads and UI copy so low-confidence AI output is not presented as certainty.
  artifacts:
    - path: apps/api/src/itinerary/itinerary.service.ts
      contains: requestCulinaryRoute
      min_lines: 260
    - path: apps/web/src/components/trip/AiAssistTab.tsx
      contains: Goi y lo trinh an uong
      min_lines: 120
    - path: apps/web/src/components/trip/HealthConflictBadge.tsx
      contains: Nguy co cao
      min_lines: 60
    - path: apps/web/src/components/trip/TimelineDaySection.tsx
      contains: HealthConflictBadge
      min_lines: 220
  key_links:
    - from: apps/api/src/itinerary/itinerary.controller.ts
      to: apps/api/src/itinerary/itinerary.service.ts
      via: itinerary AI endpoints delegate route suggestion, route apply, and health warning assembly to the itinerary service.
      pattern: this\.itineraryService\.
    - from: apps/web/src/components/trip/AiAssistTab.tsx
      to: apps/web/src/lib/api-client.ts
      via: the AI trip tab loads route suggestions through explicit request and apply API calls.
      pattern: requestCulinaryRoute
    - from: apps/web/src/components/trip/TimelineDaySection.tsx
      to: apps/web/src/components/trip/HealthConflictBadge.tsx
      via: itinerary cards render severity-coded health conflict warnings inline under item metadata.
      pattern: HealthConflictBadge
---

<objective>
Create the Phase 5 foundation inside the trip workspace and deliver the first two trust-sensitive AI flows: culinary routing and health-profile conflict matching.

Purpose: Anchor Phase 5 in the existing trip workspace while proving the product contract that AI can warn and suggest without silently mutating canonical trip data.
Output: AI workspace tab, culinary route suggestion/apply flow, inline health conflict warnings, and Phase 5 test scaffolding.
</objective>

<execution_context>
@C:/Users/tantr/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tantr/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-deep-ai-integration/05-CONTEXT.md
@.planning/phases/05-deep-ai-integration/05-RESEARCH.md
@.planning/phases/05-deep-ai-integration/05-VALIDATION.md
@apps/web/src/components/trip/TripWorkspaceShell.tsx
@apps/web/src/components/trip/TimelineDaySection.tsx
@apps/web/src/components/trip/TripMapScreen.tsx
@apps/web/src/lib/api-client.ts
@apps/api/src/itinerary/itinerary.service.ts
@packages/database/prisma/schema.prisma
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add itinerary AI endpoints for health conflict warnings and culinary route suggestion/apply</name>
  <files>apps/api/src/itinerary/itinerary.controller.ts, apps/api/src/itinerary/itinerary.service.ts, apps/api/src/itinerary/dto/request-culinary-route.dto.ts, apps/api/src/itinerary/dto/apply-culinary-route.dto.ts, apps/api/src/itinerary/itinerary.service.spec.ts, apps/api/test/phase-05-deep-ai-integration.e2e-spec.ts</files>
  <read_first>
    apps/api/src/itinerary/itinerary.controller.ts
    apps/api/src/itinerary/itinerary.service.ts
    apps/api/test/phase-02-itinerary.e2e-spec.ts
    .planning/phases/05-deep-ai-integration/05-CONTEXT.md
    .planning/phases/05-deep-ai-integration/05-RESEARCH.md
    .planning/phases/05-deep-ai-integration/05-VALIDATION.md
    packages/database/prisma/schema.prisma
  </read_first>
  <action>
    Extend the itinerary backend so Phase 5 can analyze and suggest without silently mutating the trip.
    1. In apps/api/src/itinerary/dto/request-culinary-route.dto.ts add RequestCulinaryRouteDto with itemIds string[] and optional travelMode string defaulting to "WALKING".
    2. In apps/api/src/itinerary/dto/apply-culinary-route.dto.ts add ApplyCulinaryRouteDto with orderedItemIds string[] and sourceSuggestionId string.
    3. In apps/api/src/itinerary/itinerary.controller.ts expose POST /trips/:tripId/itinerary/culinary-route/suggest and POST /trips/:tripId/itinerary/culinary-route/apply in the itinerary controller instead of creating a separate AI-only controller.
    4. In apps/api/src/itinerary/itinerary.service.ts add requestCulinaryRoute(tripId, userId, dto) that validates all requested items belong to the trip, sorts selected food stops by a deterministic distance heuristic using item lat/lng, and returns ordered items plus per-hop reason text, totalEstimatedMinutes, confidenceLabel, and a suggestionId token.
    5. In apps/api/src/itinerary/itinerary.service.ts extend the existing snapshot builder so the itinerary snapshot includes healthWarnings: each warning row must contain itemId, severity, title, message, confidenceLabel, and affectedMemberIds. Parse User.healthProfile as JSON when possible, fall back to plain-string keyword matching when parsing fails, and downgrade ambiguous matches to confidenceLabel "Can xem lai" instead of presenting certainty.
    6. Use exactly these backend severity values for health warnings: "LUU_Y", "CAN_XEM_LAI", and "NGUY_CO_CAO". Use exactly these confidence labels in the route response: "Goi y", "Uoc luong", and "Can xem lai".
    7. Implement applyCulinaryRoute(tripId, userId, dto) so only the trip leader can rewrite the selected items' sortOrder values and the route is applied only when orderedItemIds is sent in an explicit second call.
    8. Add tests in itinerary.service.spec.ts and apps/api/test/phase-05-deep-ai-integration.e2e-spec.ts for ambiguous health-profile parsing, leader-only route apply, and the guarantee that suggest does not mutate itinerary order.
  </action>
  <verify>npx jest --config apps/api/test/jest-e2e.json --runInBand apps/api/test/phase-05-deep-ai-integration.e2e-spec.ts --testNamePattern "culinary route|health|non-mutation"</verify>
  <acceptance_criteria>
    - apps/api/src/itinerary/itinerary.controller.ts contains "culinary-route/suggest"
    - apps/api/src/itinerary/itinerary.controller.ts contains "culinary-route/apply"
    - apps/api/src/itinerary/itinerary.service.ts contains "requestCulinaryRoute"
    - apps/api/src/itinerary/itinerary.service.ts contains "applyCulinaryRoute"
    - apps/api/src/itinerary/itinerary.service.ts contains "healthWarnings"
    - apps/api/src/itinerary/itinerary.service.ts contains "NGUY_CO_CAO"
    - apps/api/src/itinerary/itinerary.service.ts contains "Can xem lai"
    - apps/api/test/phase-05-deep-ai-integration.e2e-spec.ts contains "culinary route"
  </acceptance_criteria>
  <done>The itinerary API can surface severity-coded health warnings and return a reviewable culinary route suggestion before any canonical reorder happens.</done>
</task>

<task type="auto">
  <name>Task 2: Add the AI trip tab, route review UI, and inline health warning rendering</name>
  <files>apps/web/src/lib/api-client.ts, apps/web/src/components/trip/TripWorkspaceShell.tsx, apps/web/src/components/trip/TimelineDaySection.tsx, apps/web/src/components/trip/AiAssistTab.tsx, apps/web/src/components/trip/CulinaryRouteCard.tsx, apps/web/src/components/trip/HealthConflictBadge.tsx, apps/web/src/components/trip/__tests__/phase-05-ai.test.tsx</files>
  <read_first>
    apps/web/src/lib/api-client.ts
    apps/web/src/components/trip/TripWorkspaceShell.tsx
    apps/web/src/components/trip/TimelineDaySection.tsx
    apps/web/src/components/trip/TripMapScreen.tsx
    .planning/phases/05-deep-ai-integration/05-CONTEXT.md
    .planning/phases/05-deep-ai-integration/05-RESEARCH.md
    apps/api/src/itinerary/itinerary.service.ts
  </read_first>
  <action>
    Surface Phase 5 inside the trip workspace and keep trust signals visible.
    1. In apps/web/src/lib/api-client.ts extend ItinerarySnapshot with a healthWarnings array matching the backend contract, and add requestCulinaryRoute(tripId, body) plus applyCulinaryRoute(tripId, body).
    2. In apps/web/src/components/trip/TripWorkspaceShell.tsx add a new tab key and label "Tro ly AI", mount a new AiAssistTab component, and keep it alongside the existing trip tabs rather than routing users into a new top-level app area.
    3. Create apps/web/src/components/trip/AiAssistTab.tsx that loads itinerary context, offers a primary CTA labeled exactly "Goi y lo trinh an uong", lists selected food stops, and renders route suggestions through CulinaryRouteCard. When the current member is not the leader, render the route suggestion read-only without the apply CTA.
    4. Create apps/web/src/components/trip/CulinaryRouteCard.tsx that shows ordered stops, one short reason per hop, totalEstimatedMinutes, and a confidence chip using exactly one of these labels: "Goi y", "Uoc luong", or "Can xem lai". The apply button must be labeled exactly "Ap dung vao lich trinh".
    5. Create apps/web/src/components/trip/HealthConflictBadge.tsx and update apps/web/src/components/trip/TimelineDaySection.tsx so itinerary cards render inline warnings under the item metadata with exact visible severity copy "Luu y", "Can xem lai", or "Nguy co cao".
    6. Make AiAssistTab explain that the route suggestion is a draft and that itinerary order changes only after the leader presses "Ap dung vao lich trinh".
    7. Add apps/web/src/components/trip/__tests__/phase-05-ai.test.tsx coverage for the AI tab label, confidence chips, route apply gating, and inline health warning rendering.
  </action>
  <verify>npx vitest run --config apps/web/vitest.config.ts apps/web/src/components/trip/__tests__/phase-05-ai.test.tsx --testNamePattern "culinary|route"</verify>
  <acceptance_criteria>
    - apps/web/src/components/trip/TripWorkspaceShell.tsx contains "Tro ly AI"
    - apps/web/src/components/trip/TripWorkspaceShell.tsx contains "AiAssistTab"
    - apps/web/src/components/trip/AiAssistTab.tsx contains "Goi y lo trinh an uong"
    - apps/web/src/components/trip/CulinaryRouteCard.tsx contains "Ap dung vao lich trinh"
    - apps/web/src/components/trip/CulinaryRouteCard.tsx contains "Uoc luong"
    - apps/web/src/components/trip/HealthConflictBadge.tsx contains "Nguy co cao"
    - apps/web/src/components/trip/TimelineDaySection.tsx contains "HealthConflictBadge"
    - apps/web/src/components/trip/__tests__/phase-05-ai.test.tsx contains "Tro ly AI"
  </acceptance_criteria>
  <done>The trip workspace now exposes a Vietnamese-first AI entry point and inline health warnings without treating draft AI output as confirmed trip state.</done>
</task>
</tasks>

<verification>
- [ ] Run `npx jest --config apps/api/test/jest-e2e.json --runInBand apps/api/test/phase-05-deep-ai-integration.e2e-spec.ts --testNamePattern "culinary route|health|non-mutation"`
- [ ] Run `npx vitest run --config apps/web/vitest.config.ts apps/web/src/components/trip/__tests__/phase-05-ai.test.tsx --testNamePattern "culinary|route"`
- [ ] Confirm the culinary route suggestion does not reorder the timeline until the leader presses the apply button
- [ ] Confirm ambiguous health-profile matches render "Can xem lai" instead of a false-certain danger state
</verification>

<success_criteria>
- Phase 5 has a first-class AI entry point inside the existing trip workspace
- PLAN-04 warnings appear inline on itinerary items with visible severity states
- PLAN-05 route suggestions remain reviewable drafts until explicitly applied by the leader
</success_criteria>

<output>
After completion, create `.planning/phases/05-deep-ai-integration/05-01-SUMMARY.md`
</output>
