---
phase: 05-deep-ai-integration
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-00"
  - "05-01"
files_modified:
  - packages/database/prisma/schema.prisma
  - apps/api/src/app.module.ts
  - apps/api/src/booking-import/booking-import.module.ts
  - apps/api/src/booking-import/booking-import.controller.ts
  - apps/api/src/booking-import/booking-import.service.ts
  - apps/api/src/booking-import/dto/create-booking-import-draft.dto.ts
  - apps/api/src/booking-import/dto/confirm-booking-import.dto.ts
  - apps/api/src/booking-import/booking-import.service.spec.ts
  - apps/api/test/phase-05-deep-ai-integration.e2e-spec.ts
  - apps/web/src/lib/api-client.ts
  - apps/web/src/components/trip/AiAssistTab.tsx
  - apps/web/src/components/trip/BookingImportCard.tsx
  - apps/web/src/components/trip/BookingImportReviewSheet.tsx
  - apps/web/src/components/trip/__tests__/phase-05-ai.test.tsx
autonomous: true
requirements:
  - AIX-01
user_setup: []
must_haves:
  truths:
    - The trip exposes a trip-specific forwarding address and a manual paste fallback in the same AI surface so booking import does not depend on hidden operator steps.
    - Parsed booking content is stored as a reviewable draft first; nothing is inserted into the canonical itinerary until the user confirms.
    - Ambiguous or partial parsing results visibly communicate uncertainty and preserve manual edit controls before import.
    - Booking import output targets the existing itinerary model instead of inventing a parallel source of truth for bookings.
  artifacts:
    - path: packages/database/prisma/schema.prisma
      contains: model BookingImportDraft
      min_lines: 420
    - path: apps/api/src/booking-import/booking-import.service.ts
      contains: confirmBookingImportDraft
      min_lines: 180
    - path: apps/web/src/components/trip/BookingImportCard.tsx
      contains: Dia chi chuyen tiep
      min_lines: 80
    - path: apps/web/src/components/trip/BookingImportReviewSheet.tsx
      contains: Nhap vao lich trinh
      min_lines: 100
  key_links:
    - from: apps/api/src/booking-import/booking-import.controller.ts
      to: apps/api/src/booking-import/booking-import.service.ts
      via: booking import routes create drafts first and call a second confirm path to materialize itinerary items.
      pattern: this\.bookingImportService\.
    - from: apps/web/src/components/trip/AiAssistTab.tsx
      to: apps/web/src/components/trip/BookingImportCard.tsx
      via: the AI tab shows the forwarding address, draft status, and review entry point for booking imports.
      pattern: BookingImportCard
    - from: apps/web/src/components/trip/BookingImportReviewSheet.tsx
      to: apps/web/src/lib/api-client.ts
      via: the review sheet confirms a selected draft into itinerary items only after explicit user action.
      pattern: confirmBookingImportDraft
---

<objective>
Deliver the booking email parsing flow as a draft-first import system with explicit review and confirmation.

Purpose: Satisfy the strongest ingestion requirement in Phase 5 without violating the rule that AI cannot silently mutate canonical trip data.
Output: booking import draft persistence, inbound/manual ingestion routes, forwarding-address UI, editable review sheet, and tests.
</objective>

<execution_context>
@C:/Users/tantr/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/tantr/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-deep-ai-integration/05-CONTEXT.md
@.planning/phases/05-deep-ai-integration/05-RESEARCH.md
@.planning/phases/05-deep-ai-integration/05-VALIDATION.md
@.planning/phases/05-deep-ai-integration/05-01-PLAN.md
@apps/web/src/components/trip/AiAssistTab.tsx
@apps/web/src/lib/api-client.ts
@apps/api/src/itinerary/itinerary.service.ts
@packages/database/prisma/schema.prisma
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add booking import draft persistence and confirm-only itinerary insertion</name>
  <files>packages/database/prisma/schema.prisma, apps/api/src/app.module.ts, apps/api/src/booking-import/booking-import.module.ts, apps/api/src/booking-import/booking-import.controller.ts, apps/api/src/booking-import/booking-import.service.ts, apps/api/src/booking-import/dto/create-booking-import-draft.dto.ts, apps/api/src/booking-import/dto/confirm-booking-import.dto.ts, apps/api/src/booking-import/booking-import.service.spec.ts, apps/api/test/phase-05-deep-ai-integration.e2e-spec.ts</files>
  <read_first>
    packages/database/prisma/schema.prisma
    apps/api/src/app.module.ts
    apps/api/src/itinerary/itinerary.service.ts
    .planning/phases/05-deep-ai-integration/05-CONTEXT.md
    .planning/phases/05-deep-ai-integration/05-RESEARCH.md
    .planning/phases/05-deep-ai-integration/05-VALIDATION.md
    .planning/phases/05-deep-ai-integration/05-01-PLAN.md
  </read_first>
  <action>
    Build booking import as a draft persistence flow instead of a direct auto-insert pipeline.
    1. In packages/database/prisma/schema.prisma add enum BookingImportDraftStatus with DRAFT, CONFIRMED, and REJECTED.
    2. Add model BookingImportDraft with fields id String @id @default(cuid()), tripId String, createdByTripMemberId String?, reviewedByTripMemberId String?, sourceChannel String, forwardingAddress String, sourceMessageId String?, sourceSender String?, sourceSubject String?, rawContent String, confidenceLabel String, status BookingImportDraftStatus @default(DRAFT), parseSummary String, parsedItems Json, createdAt DateTime @default(now()), updatedAt DateTime @updatedAt; add @@index([tripId, status]) and relations to Trip and TripMember.
    3. Register a new BookingImportModule in apps/api/src/app.module.ts and create controller routes GET /trips/:tripId/booking-import/config, GET /trips/:tripId/booking-import/drafts, POST /trips/:tripId/booking-import/drafts, POST /booking-import/inbound, and POST /trips/:tripId/booking-import/drafts/:draftId/confirm.
    4. In booking-import.service.ts generate a forwarding address string exactly in the shape "booking+{joinCode}@minhdidauthe.local", return it from getImportConfig(tripId), and support manual raw-content ingestion through createDraftFromRawContent(tripId, userId, dto).
    5. Parse booking text into parsedItems rows with title, locationName, startTime, endTime, bookingCode, and missingFields arrays. When parsing confidence is incomplete, set confidenceLabel to "Can xem lai" and preserve all raw excerpt text in the draft.
    6. Implement confirmBookingImportDraft(tripId, draftId, userId, dto) so leader confirmation remains the default structural authority path: only the trip leader can create itinerary items from a draft through the existing itinerary service, and the draft stays editable/reviewable for non-leader members until the leader confirms; after success, mark the draft status CONFIRMED and store reviewedByTripMemberId.
    7. Add tests covering forwarding-address generation, manual draft creation, inbound webhook draft creation, low-confidence parsing, and the guarantee that draft creation alone does not create itinerary items.
  </action>
  <verify>npx jest --config apps/api/test/jest-e2e.json --runInBand apps/api/test/phase-05-deep-ai-integration.e2e-spec.ts --testNamePattern "booking import|forwarding"</verify>
  <acceptance_criteria>
    - packages/database/prisma/schema.prisma contains "model BookingImportDraft"
    - packages/database/prisma/schema.prisma contains "enum BookingImportDraftStatus"
    - apps/api/src/app.module.ts contains "BookingImportModule"
    - apps/api/src/booking-import/booking-import.controller.ts contains "booking-import/config"
    - apps/api/src/booking-import/booking-import.controller.ts contains "booking-import/drafts/:draftId/confirm"
    - apps/api/src/booking-import/booking-import.service.ts contains "booking+"
    - apps/api/src/booking-import/booking-import.service.ts contains "Can xem lai"
    - apps/api/src/booking-import/booking-import.service.ts contains "confirmBookingImportDraft"
  </acceptance_criteria>
  <done>Booking emails now land as persisted drafts with explicit forwarding metadata and a second confirmation step before the itinerary changes.</done>
</task>

<task type="auto">
  <name>Task 2: Expose the booking import review flow in the AI tab</name>
  <files>apps/web/src/lib/api-client.ts, apps/web/src/components/trip/AiAssistTab.tsx, apps/web/src/components/trip/BookingImportCard.tsx, apps/web/src/components/trip/BookingImportReviewSheet.tsx, apps/web/src/components/trip/__tests__/phase-05-ai.test.tsx</files>
  <read_first>
    apps/web/src/lib/api-client.ts
    apps/web/src/components/trip/AiAssistTab.tsx
    apps/web/src/components/trip/CulinaryRouteCard.tsx
    .planning/phases/05-deep-ai-integration/05-CONTEXT.md
    .planning/phases/05-deep-ai-integration/05-RESEARCH.md
    apps/api/src/booking-import/booking-import.service.ts
  </read_first>
  <action>
    Add a draft-first booking import surface to the existing AI tab.
    1. In apps/web/src/lib/api-client.ts add getBookingImportConfig(tripId), listBookingImportDrafts(tripId), createBookingImportDraft(tripId, body), and confirmBookingImportDraft(tripId, draftId, body). Add interfaces for BookingImportConfig and BookingImportDraft that mirror the backend fields including confidenceLabel, parseSummary, parsedItems, and missingFields.
    2. Create apps/web/src/components/trip/BookingImportCard.tsx that displays the exact visible label "Dia chi chuyen tiep", shows the forwarding address, includes a manual paste CTA, and lists recent drafts with confidence chips.
    3. Create apps/web/src/components/trip/BookingImportReviewSheet.tsx that renders editable parsed rows and a primary confirm button labeled exactly "Nhap vao lich trinh". Missing or ambiguous fields must stay editable before confirm.
    4. Update apps/web/src/components/trip/AiAssistTab.tsx so the booking import card sits alongside culinary routing in the same trip-scoped AI surface and includes helper copy explaining that nothing is imported until the draft is reviewed and the leader confirms it.
    5. When confidenceLabel is "Can xem lai", render a visible caution block in the review sheet and keep the raw excerpt accessible for comparison.
    6. Extend apps/web/src/components/trip/__tests__/phase-05-ai.test.tsx with cases for forwarding-address rendering, draft list rendering, low-confidence caution UI, and the confirm CTA label.
  </action>
  <verify>npx vitest run --config apps/web/vitest.config.ts apps/web/src/components/trip/__tests__/phase-05-ai.test.tsx --testNamePattern "booking import|review sheet"</verify>
  <acceptance_criteria>
    - apps/web/src/components/trip/BookingImportCard.tsx contains "Dia chi chuyen tiep"
    - apps/web/src/components/trip/BookingImportCard.tsx contains "booking+"
    - apps/web/src/components/trip/BookingImportReviewSheet.tsx contains "Nhap vao lich trinh"
    - apps/web/src/components/trip/BookingImportReviewSheet.tsx contains "Can xem lai"
    - apps/web/src/components/trip/AiAssistTab.tsx contains "BookingImportCard"
    - apps/web/src/components/trip/AiAssistTab.tsx contains "khong duoc dua vao lich trinh"
    - apps/web/src/components/trip/__tests__/phase-05-ai.test.tsx contains "Dia chi chuyen tiep"
  </acceptance_criteria>
  <done>The AI tab can show the forwarding address, accept a manual raw booking paste fallback, and confirm parsed drafts into the timeline only after visible review.</done>
</task>
</tasks>

<verification>
- [ ] Run `npx jest --config apps/api/test/jest-e2e.json --runInBand apps/api/test/phase-05-deep-ai-integration.e2e-spec.ts --testNamePattern "booking import|forwarding"`
- [ ] Run `npx vitest run --config apps/web/vitest.config.ts apps/web/src/components/trip/__tests__/phase-05-ai.test.tsx --testNamePattern "booking import|review sheet"`
- [ ] Confirm draft creation does not add itinerary items until the confirm action runs
- [ ] Confirm low-confidence parsing leaves editable fields and raw excerpt visible in the review sheet
</verification>

<success_criteria>
- AIX-01 is satisfied through a trip-specific forwarding address plus a visible reviewable draft flow
- Booking import follows the same trust model as other Phase 5 AI flows: analyze first, confirm before mutation
</success_criteria>

<output>
After completion, create `.planning/phases/05-deep-ai-integration/05-02-SUMMARY.md`
</output>
