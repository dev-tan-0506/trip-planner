---
phase: 06-post-trip-memories
plan: "03"
type: execute
wave: 3
depends_on:
  - "06-02"
files_modified:
  - packages/database/prisma/schema.prisma
  - apps/api/src/memories/memories.controller.ts
  - apps/api/src/memories/memories.service.ts
  - apps/api/src/memories/dto/submit-anonymous-feedback.dto.ts
  - apps/api/src/memories/memories.service.spec.ts
  - apps/api/test/phase-06-post-trip-memories.e2e-spec.ts
  - apps/web/src/lib/api-client.ts
  - apps/web/src/components/trip/MemoriesTab.tsx
  - apps/web/src/components/trip/AnonymousFeedbackPanel.tsx
  - apps/web/src/components/trip/__tests__/post-trip-memories.test.tsx
autonomous: true
requirements:
  - MEMO-02
user_setup: []
must_haves:
  truths:
    - Anonymous feedback cannot be traced back through the leader-facing snapshot or API contract.
    - Members can submit exactly one end-of-trip response, but the system enforces that limit through a receipt record separated from anonymous content rows.
    - The poll feels lightweight and playful in Vietnamese instead of like a heavy enterprise survey.
    - The memories tab stays the single Phase 6 home rather than adding a separate feedback route.
  artifacts:
    - path: packages/database/prisma/schema.prisma
      contains: model AnonymousFeedbackSubmission
      min_lines: 520
    - path: packages/database/prisma/schema.prisma
      contains: model FeedbackSubmissionReceipt
      min_lines: 520
    - path: apps/api/src/memories/memories.service.ts
      contains: submitAnonymousFeedback
      min_lines: 260
    - path: apps/web/src/components/trip/AnonymousFeedbackPanel.tsx
      contains: Góp ý ẩn danh
      min_lines: 120
    - path: apps/web/src/components/trip/MemoriesTab.tsx
      contains: AnonymousFeedbackPanel
      min_lines: 120
  key_links:
    - from: apps/api/src/memories/memories.service.ts
      to: packages/database/prisma/schema.prisma
      via: anonymous content rows and receipt rows are persisted separately to preserve one-response-per-member without exposing identity.
      pattern: FeedbackSubmissionReceipt
    - from: apps/web/src/components/trip/MemoriesTab.tsx
      to: apps/web/src/components/trip/AnonymousFeedbackPanel.tsx
      via: the shared memories host mounts the anonymous feedback experience inline.
      pattern: AnonymousFeedbackPanel
---

<objective>
Deliver MEMO-02: a fun, anonymous post-trip feedback poll that preserves privacy by design.

Purpose: Capture honest member reflections at the end of a trip without turning the feature into a public or traceable complaint log.
Output: anonymous-feedback data model, one-response-per-member enforcement, aggregate leader snapshot, and a member-friendly feedback panel inside the memories tab.
</objective>

<execution_context>
@C:/Users/quoct/OneDrive/Desktop/code/designapp/trip-planner/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/quoct/OneDrive/Desktop/code/designapp/trip-planner/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-post-trip-memories/06-RESEARCH.md
@.planning/phases/06-post-trip-memories/06-VALIDATION.md
@.planning/phases/06-post-trip-memories/06-01-PLAN.md
@.planning/phases/06-post-trip-memories/06-02-PLAN.md
@packages/database/prisma/schema.prisma
@apps/api/src/memories/memories.service.ts
@apps/web/src/components/trip/MemoriesTab.tsx
@apps/web/src/lib/api-client.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add anonymous feedback models with receipt-based privacy boundaries</name>
  <files>packages/database/prisma/schema.prisma</files>
  <read_first>
    packages/database/prisma/schema.prisma
    .planning/phases/06-post-trip-memories/06-RESEARCH.md
    .planning/phases/06-post-trip-memories/06-01-PLAN.md
    .planning/phases/06-post-trip-memories/06-02-PLAN.md
  </read_first>
  <action>
    Extend the schema for anonymous end-of-trip feedback.
    1. Add enum FeedbackPollStatus with LOCKED, OPEN, and CLOSED.
    2. Add model TripFeedbackPoll with fields id String @id @default(cuid()), tripId String @unique, status FeedbackPollStatus @default(LOCKED), openedAt DateTime?, closedAt DateTime?, createdAt DateTime @default(now()), updatedAt DateTime @updatedAt; relate it to Trip.
    3. Add model AnonymousFeedbackSubmission with fields id String @id @default(cuid()), tripId String, pollId String, moodScore Int, highlight String, wishNextTime String, createdAt DateTime @default(now()); relate it to Trip and TripFeedbackPoll; add @@index([tripId, createdAt]).
    4. Add model FeedbackSubmissionReceipt with fields id String @id @default(cuid()), tripId String, pollId String, tripMemberId String, submissionId String @unique, submittedAt DateTime @default(now()); relate it to Trip, TripFeedbackPoll, TripMember, and AnonymousFeedbackSubmission; add @@unique([pollId, tripMemberId]).
    5. Add relation fields on Trip and TripMember so the feedback poll and receipts can be queried without exposing receipt data to the public snapshot contract.
  </action>
  <verify>npm exec prisma validate</verify>
  <acceptance_criteria>
    - packages/database/prisma/schema.prisma contains "model TripFeedbackPoll"
    - packages/database/prisma/schema.prisma contains "model AnonymousFeedbackSubmission"
    - packages/database/prisma/schema.prisma contains "model FeedbackSubmissionReceipt"
    - packages/database/prisma/schema.prisma contains "@@unique([pollId, tripMemberId])"
    - packages/database/prisma/schema.prisma contains "FeedbackPollStatus"
  </acceptance_criteria>
  <done>The schema can enforce one anonymous response per member without storing identity in the leader-facing submission rows.</done>
</task>

<task type="auto">
  <name>Task 2: Implement anonymous feedback APIs, privacy-safe snapshots, and coverage</name>
  <files>apps/api/src/memories/memories.controller.ts, apps/api/src/memories/memories.service.ts, apps/api/src/memories/dto/submit-anonymous-feedback.dto.ts, apps/api/src/memories/memories.service.spec.ts, apps/api/test/phase-06-post-trip-memories.e2e-spec.ts</files>
  <read_first>
    apps/api/src/memories/memories.controller.ts
    apps/api/src/memories/memories.service.ts
    packages/database/prisma/schema.prisma
    .planning/phases/06-post-trip-memories/06-RESEARCH.md
    .planning/phases/06-post-trip-memories/06-VALIDATION.md
  </read_first>
  <action>
    Add end-of-trip anonymous feedback routes inside the existing memories module.
    1. In apps/api/src/memories/memories.controller.ts add exactly these routes for Phase 6.2: GET /trips/:tripId/memories/feedback, POST /trips/:tripId/memories/feedback/submissions, and POST /trips/:tripId/memories/feedback/close.
    2. Create SubmitAnonymousFeedbackDto with moodScore number constrained to 1-5, highlight string, and wishNextTime string.
    3. In apps/api/src/memories/memories.service.ts implement getFeedbackSnapshot(tripId, userId) that auto-opens the poll when `now >= trip.endDate`, returns member-facing eligibility plus leader-facing aggregate summaries, and never includes `tripMemberId` or receipt identifiers in the leader-visible response.
    4. Implement submitAnonymousFeedback(tripId, userId, dto) so the service verifies the caller is a trip member, ensures the poll is open, creates an anonymous submission row, then writes a receipt row for one-response-per-member enforcement.
    5. Implement closeFeedbackPoll(tripId, userId) so only the trip leader can close the poll and freeze new submissions.
    6. Add unit and e2e coverage for poll auto-open timing, duplicate-submission blocking, leader-only close permission, and the guarantee that leader snapshots expose only aggregates plus anonymous text rows.
  </action>
  <verify>npx jest --config apps/api/test/jest-e2e.json --runInBand apps/api/test/phase-06-post-trip-memories.e2e-spec.ts --testNamePattern "feedback|anonymous"</verify>
  <acceptance_criteria>
    - apps/api/src/memories/memories.controller.ts contains "memories/feedback"
    - apps/api/src/memories/memories.controller.ts contains "feedback/submissions"
    - apps/api/src/memories/memories.controller.ts contains "feedback/close"
    - apps/api/src/memories/memories.service.ts contains "getFeedbackSnapshot"
    - apps/api/src/memories/memories.service.ts contains "submitAnonymousFeedback"
    - apps/api/src/memories/memories.service.ts contains "FeedbackSubmissionReceipt"
    - apps/api/test/phase-06-post-trip-memories.e2e-spec.ts contains "anonymous"
  </acceptance_criteria>
  <done>The backend now supports an actually anonymous, end-of-trip feedback flow with aggregate leader insights.</done>
</task>

<task type="auto">
  <name>Task 3: Add the anonymous feedback panel inside MemoriesTab</name>
  <files>apps/web/src/lib/api-client.ts, apps/web/src/components/trip/MemoriesTab.tsx, apps/web/src/components/trip/AnonymousFeedbackPanel.tsx, apps/web/src/components/trip/__tests__/post-trip-memories.test.tsx</files>
  <read_first>
    apps/web/src/lib/api-client.ts
    apps/web/src/components/trip/MemoriesTab.tsx
    apps/web/src/components/trip/AiAssistTab.tsx
    apps/api/src/memories/memories.service.ts
    .planning/phases/06-post-trip-memories/06-RESEARCH.md
  </read_first>
  <action>
    Surface the feedback experience inside the shared memories host.
    1. In apps/web/src/lib/api-client.ts add typed contracts and methods for getFeedbackSnapshot(tripId), submitAnonymousFeedback(tripId, body), and closeFeedbackPoll(tripId).
    2. Create apps/web/src/components/trip/AnonymousFeedbackPanel.tsx that renders a member submission card with a visible title exactly "Góp ý ẩn danh", a 1-5 mood selector, two short text prompts, and a confirmation message after successful submit.
    3. When the current user is leader, the panel must render aggregate counts and anonymous content only; it must not show member names, avatars, or any identity chips.
    4. Update apps/web/src/components/trip/MemoriesTab.tsx so the feedback panel appears under the vault section once the poll is open or the trip has ended.
    5. Keep the copy playful and lightweight with visible cues that the response is anonymous and only gửi một lần.
    6. Add UI tests for member submit state, leader aggregate rendering, and the absence of identity labels in the leader view.
  </action>
  <verify>npx vitest run --config apps/web/vitest.config.ts apps/web/src/components/trip/__tests__/post-trip-memories.test.tsx --testNamePattern "ẩn danh|feedback"</verify>
  <acceptance_criteria>
    - apps/web/src/components/trip/AnonymousFeedbackPanel.tsx contains "Góp ý ẩn danh"
    - apps/web/src/components/trip/AnonymousFeedbackPanel.tsx contains "một lần"
    - apps/web/src/components/trip/AnonymousFeedbackPanel.tsx contains "Mức vui"
    - apps/web/src/components/trip/MemoriesTab.tsx contains "AnonymousFeedbackPanel"
    - apps/web/src/components/trip/__tests__/post-trip-memories.test.tsx contains "ẩn danh"
  </acceptance_criteria>
  <done>The memories tab now includes a Vietnamese-first anonymous feedback poll with distinct member and leader states.</done>
</task>
</tasks>

<verification>
- [ ] Run `npm exec prisma validate`
- [ ] Run `npx jest --config apps/api/test/jest-e2e.json --runInBand apps/api/test/phase-06-post-trip-memories.e2e-spec.ts --testNamePattern "feedback|anonymous"`
- [ ] Run `npx vitest run --config apps/web/vitest.config.ts apps/web/src/components/trip/__tests__/post-trip-memories.test.tsx --testNamePattern "ẩn danh|feedback"`
- [ ] Confirm the leader-facing snapshot cannot map feedback rows back to trip members
</verification>

<success_criteria>
- MEMO-02 is enforced as one response per member without identity leakage
- Members can submit a playful anonymous response inside the trip workspace
- Leaders only see aggregate insights and anonymous text content
</success_criteria>

<output>
After completion, create `.planning/phases/06-post-trip-memories/06-03-SUMMARY.md`
</output>
