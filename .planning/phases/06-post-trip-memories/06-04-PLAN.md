---
phase: 06-post-trip-memories
plan: "04"
type: execute
wave: 4
depends_on:
  - "06-02"
  - "06-03"
files_modified:
  - apps/api/package.json
  - packages/database/prisma/schema.prisma
  - apps/api/src/memories/memories.module.ts
  - apps/api/src/memories/memories.service.ts
  - apps/api/src/memories/reunion-invite-mailer.service.ts
  - apps/api/src/memories/dto/respond-reunion-availability.dto.ts
  - apps/api/src/memories/dto/finalize-reunion-invite.dto.ts
  - apps/api/src/memories/memories.service.spec.ts
  - apps/api/test/phase-06-post-trip-memories.e2e-spec.ts
autonomous: true
requirements:
  - MEMO-03
  - MEMO-04
user_setup: []
must_haves:
  truths:
    - The backend exposes final-day souvenir reminder data tied to destination and departure timing, including concrete authentic market/shop/area suggestions rather than generic shopping copy.
    - Reunion Organizer auto-sends a real e-invite at the `trip.endDate + 7 days` threshold through a concrete SMTP-backed delivery path with persisted send status.
    - Reunion coordination is based on member availability responses stored in-app, and the feature can finalize a recommended reunion date from collected responses instead of stopping at raw availability capture.
    - The backend exposes souvenir eligibility, reunion unlock state, delivery status, and a recommended date so the follow-up UI plan can render a truthful post-trip experience.
  artifacts:
    - path: apps/api/package.json
      contains: nodemailer
      min_lines: 20
    - path: packages/database/prisma/schema.prisma
      contains: model ReunionInvite
      min_lines: 560
    - path: packages/database/prisma/schema.prisma
      contains: model ReunionAvailability
      min_lines: 560
    - path: packages/database/prisma/schema.prisma
      contains: model ReunionInviteDelivery
      min_lines: 560
    - path: apps/api/src/memories/memories.service.ts
      contains: getSouvenirSnapshot
      min_lines: 340
    - path: apps/api/src/memories/memories.service.ts
      contains: getReunionSnapshot
      min_lines: 340
    - path: apps/api/src/memories/memories.service.ts
      contains: dispatchDueReunionInvites
      min_lines: 340
    - path: apps/api/src/memories/memories.service.ts
      contains: recommendedDate
      min_lines: 340
    - path: apps/api/src/memories/reunion-invite-mailer.service.ts
      contains: sendInviteEmail
      min_lines: 60
    - path: apps/api/test/phase-06-post-trip-memories.e2e-spec.ts
      contains: reunion
      min_lines: 120
  key_links:
    - from: apps/api/src/memories/memories.service.ts
      to: packages/database/prisma/schema.prisma
      via: reunion invite eligibility, delivery receipts, and member availability are persisted in dedicated trip-scoped models.
      pattern: ReunionInvite
    - from: apps/api/src/memories/memories.service.ts
      to: apps/api/src/memories/reunion-invite-mailer.service.ts
      via: the memories service hands off actual reunion email delivery to a dedicated mailer service and persists SENT or FAILED outcomes.
      pattern: sendInviteEmail
---

<objective>
Deliver MEMO-03 and MEMO-04: final-day souvenir reminders plus a lightweight reunion organizer that unlocks one week after the trip ends.

Purpose: Build the backend-heavy retention engine first so the later integration plan can stay focused on routes, client contracts, and UI.
Output: final-day souvenir reminder snapshot contract, reunion invite plus delivery and availability models, scheduled SMTP-backed invite dispatch, and finalized date selection from member responses.
</objective>

<execution_context>
@C:/Users/quoct/OneDrive/Desktop/code/designapp/trip-planner/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/quoct/OneDrive/Desktop/code/designapp/trip-planner/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-post-trip-memories/06-RESEARCH.md
@.planning/phases/06-post-trip-memories/06-VALIDATION.md
@.planning/phases/06-post-trip-memories/06-01-PLAN.md
@.planning/phases/06-post-trip-memories/06-02-PLAN.md
@.planning/phases/06-post-trip-memories/06-03-PLAN.md
@packages/database/prisma/schema.prisma
@apps/api/src/memories/memories.service.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add reunion persistence and time-gated souvenir/reunion service logic</name>
  <files>apps/api/package.json, packages/database/prisma/schema.prisma, apps/api/src/memories/memories.module.ts, apps/api/src/memories/memories.service.ts, apps/api/src/memories/reunion-invite-mailer.service.ts, apps/api/src/memories/dto/respond-reunion-availability.dto.ts, apps/api/src/memories/dto/finalize-reunion-invite.dto.ts, apps/api/src/memories/memories.service.spec.ts, apps/api/test/phase-06-post-trip-memories.e2e-spec.ts</files>
  <read_first>
    packages/database/prisma/schema.prisma
    apps/api/src/memories/memories.service.ts
    apps/api/src/daily-podcast/daily-podcast.service.ts
    .planning/phases/06-post-trip-memories/06-RESEARCH.md
  </read_first>
  <action>
    Extend the memories backend for the retention flows.
    1. In apps/api/package.json add `nodemailer` and the matching TypeScript types needed for direct SMTP delivery from Nest services.
    2. Add model ReunionInvite with fields id String @id @default(cuid()), tripId String @unique, status String @default("PENDING_SEND"), title String, message String, suggestedDateOptions String[], recommendedDate String?, finalizedDate String?, unlockedAt DateTime, sentAt DateTime?, createdAt DateTime @default(now()), updatedAt DateTime @updatedAt; relate it to Trip.
    3. Add model ReunionAvailability with fields id String @id @default(cuid()), reunionInviteId String, tripMemberId String, selectedDates String[], note String?, createdAt DateTime @default(now()), updatedAt DateTime @updatedAt; relate it to ReunionInvite and TripMember; add @@unique([reunionInviteId, tripMemberId]).
    4. Add model ReunionInviteDelivery with fields id String @id @default(cuid()), reunionInviteId String, userId String, recipientEmail String, status String @default("PENDING"), subjectLine String, sentAt DateTime?, errorMessage String?, createdAt DateTime @default(now()), updatedAt DateTime @updatedAt; relate it to ReunionInvite and User; add @@unique([reunionInviteId, userId]).
    5. Create apps/api/src/memories/reunion-invite-mailer.service.ts with sendInviteEmail(recipientEmail, subjectLine, htmlBody) that reads SMTP configuration from env, sends a simple HTML reunion invitation, and returns SENT or FAILED metadata without using background workers.
    6. Update apps/api/src/memories/memories.module.ts so it explicitly provides and exports any dependencies needed for the reunion flow, including ReunionInviteMailerService, and so MemoriesService can inject and use that mailer through normal Nest DI.
    7. In apps/api/src/memories/memories.service.ts implement getSouvenirSnapshot(tripId, userId) that returns reminder cards only when the current date falls on the trip's final day in local trip time; the payload should include destinationLabel, reminderLabel, and 2-4 concise authentic-location suggestions where each suggestion contains `locationName`, `locationType`, `areaLabel`, `reason`, and a short souvenir hint so MEMO-04 covers both reminders and where to buy.
    8. In apps/api/src/memories/memories.service.ts add a real scheduled delivery mechanism instead of fetch-triggered sending: implement a `dispatchDueReunionInvites()` routine plus a module-lifecycle timer or scheduler hook that runs inside the API process, scans for trips whose `trip.endDate + 7 days <= now` and whose reunion invite has not been sent yet, creates the ReunionInvite if missing, sends the initial e-invite to all trip members through ReunionInviteMailerService, and persists one ReunionInviteDelivery row per recipient with SENT or FAILED status.
    9. Implement getReunionSnapshot(tripId, userId) so it reports `eligible: false` before the threshold, and after unlock it returns the persisted reunion invite, delivery status, existing member availability, and a computed `recommendedDate` derived from overlapping or most-supported `selectedDates` across ReunionAvailability rows without being responsible for the actual timed send.
    10. Create RespondReunionAvailabilityDto with selectedDates string[] and optional note string, plus FinalizeReunionInviteDto with one required `finalizedDate` string.
    11. Implement respondReunionAvailability(tripId, userId, dto) so each trip member can save or replace their own availability once the reunion invite is unlocked.
    12. Implement finalizeReunionInvite(tripId, userId, dto) so only the trip leader can choose a final reunion date, the chosen date must be one of the suggested or recommended options, and the snapshot exposes the finalized invite artifact after responses arrive.
    13. Add service and e2e coverage for final-day gating, one-week-after-trip unlock timing, scheduled invite dispatch at the due threshold, persisted SENT or FAILED delivery rows, `recommendedDate` computation from member availability overlaps, upsert-style member availability submission, and leader-only invite finalization.
  </action>
  <verify>npx jest apps/api/src/memories/memories.service.spec.ts --runInBand --testNamePattern "souvenir|reunion|dispatch|final day"</verify>
  <acceptance_criteria>
    - apps/api/package.json contains "nodemailer"
    - packages/database/prisma/schema.prisma contains "model ReunionInvite"
    - packages/database/prisma/schema.prisma contains "model ReunionAvailability"
    - packages/database/prisma/schema.prisma contains "model ReunionInviteDelivery"
    - packages/database/prisma/schema.prisma contains "@@unique([reunionInviteId, tripMemberId])"
    - apps/api/src/memories/memories.module.ts contains "ReunionInviteMailerService"
    - apps/api/src/memories/reunion-invite-mailer.service.ts contains "sendInviteEmail"
    - apps/api/src/memories/memories.service.ts contains "getSouvenirSnapshot"
    - apps/api/src/memories/memories.service.ts contains "locationName"
    - apps/api/src/memories/memories.service.ts contains "getReunionSnapshot"
    - apps/api/src/memories/memories.service.ts contains "dispatchDueReunionInvites"
    - apps/api/src/memories/memories.service.ts contains "recommendedDate"
    - apps/api/src/memories/memories.service.ts contains "respondReunionAvailability"
    - apps/api/src/memories/memories.service.ts contains "finalizeReunionInvite"
  </acceptance_criteria>
  <done>The backend can expose final-day souvenir prompts and a reunion organizer that both auto-sends an initial e-invite and finalizes a recommended reunion date from collected availability.</done>
</task>
</tasks>

<verification>
- [ ] Run `npx jest apps/api/src/memories/memories.service.spec.ts --runInBand --testNamePattern "souvenir|reunion|dispatch|final day"`
- [ ] Confirm reunion stays locked before `trip.endDate + 7 days`
- [ ] Confirm `recommendedDate` is derived from overlapping member availability before leader finalization
</verification>

<success_criteria>
- MEMO-04 backend exposes final-day souvenir reminders plus authentic location suggestions
- MEMO-03 backend auto-sends e-invites after the trip and derives a concrete recommended reunion date from member responses
- The next plan can integrate these backend contracts into the memories UI without reopening persistence or scheduling design
</success_criteria>

<output>
After completion, create `.planning/phases/06-post-trip-memories/06-04-SUMMARY.md`
</output>
