---
phase: 06-post-trip-memories
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/database/prisma/schema.prisma
  - apps/api/src/app.module.ts
  - apps/api/src/memories/memories.module.ts
  - apps/api/src/memories/memories.controller.ts
  - apps/api/src/memories/memories.service.ts
  - apps/api/src/memories/vault-storage.service.ts
  - apps/api/src/memories/dto/upload-vault-document.dto.ts
  - apps/api/src/memories/dto/review-vault-document.dto.ts
  - apps/api/src/memories/memories.service.spec.ts
  - apps/api/test/phase-06-post-trip-memories.e2e-spec.ts
autonomous: true
requirements:
  - MEMO-01
user_setup: []
must_haves:
  truths:
    - Digital Vault is clearly temporary and trip-scoped, supports browser-friendly image or PDF uploads, and reuses the repo's existing proof-storage style instead of assuming cloud storage.
    - Members can upload and review their own sensitive documents, while the leader gets a group-ready overview for check-in coordination without exposing documents through a public route.
    - The backend contract exposes supported file types, review status, and temporary-retention metadata so the follow-up UI plan can stay honest and bounded.
  artifacts:
    - path: packages/database/prisma/schema.prisma
      contains: model VaultDocument
      min_lines: 480
    - path: apps/api/src/memories/memories.service.ts
      contains: getVaultSnapshot
      min_lines: 180
    - path: apps/api/src/memories/vault-storage.service.ts
      contains: application/pdf
      min_lines: 40
  key_links:
    - from: apps/api/src/memories/memories.controller.ts
      to: apps/api/src/memories/memories.service.ts
      via: trip-scoped memories routes delegate vault permission checks and snapshot assembly to the memories service.
      pattern: this\.memoriesService\.
---

<objective>
Create the backend foundation for MEMO-01: a temporary Digital Vault for IDs and tickets.

Purpose: Give Phase 6 a reliable storage and API base before the workspace UI and later post-trip flows are layered on top.
Output: Prisma vault models, local document storage service for image/PDF uploads, and trip-scoped memories routes with permission-safe snapshots.
</objective>

<execution_context>
@C:/Users/quoct/OneDrive/Desktop/code/designapp/trip-planner/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/quoct/OneDrive/Desktop/code/designapp/trip-planner/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-post-trip-memories/06-RESEARCH.md
@.planning/phases/06-post-trip-memories/06-VALIDATION.md
@packages/database/prisma/schema.prisma
@apps/api/src/checklists/checklist-proof-storage.service.ts
@apps/api/src/attendance/proof-storage.service.ts
@apps/api/src/app.module.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add temporary vault models and browser-friendly storage support</name>
  <files>packages/database/prisma/schema.prisma, apps/api/src/memories/vault-storage.service.ts</files>
  <read_first>
    packages/database/prisma/schema.prisma
    apps/api/src/checklists/checklist-proof-storage.service.ts
    apps/api/src/attendance/proof-storage.service.ts
    .planning/phases/06-post-trip-memories/06-RESEARCH.md
  </read_first>
  <action>
    Extend the schema and storage layer for trip-scoped sensitive documents.
    1. In packages/database/prisma/schema.prisma add enum VaultDocumentKind with ID_CARD, PASSPORT, FLIGHT_TICKET, HOTEL_BOOKING, and OTHER.
    2. Add enum VaultDocumentStatus with PENDING_REVIEW, READY_FOR_CHECK_IN, and ARCHIVED.
    3. Add model VaultDocument with fields id String @id @default(cuid()), tripId String, uploadedByTripMemberId String, reviewedByTripMemberId String?, kind VaultDocumentKind, status VaultDocumentStatus @default(PENDING_REVIEW), fileName String, mimeType String, fileUrl String, note String?, expiresAt DateTime, createdAt DateTime @default(now()), updatedAt DateTime @updatedAt; relate it to Trip and TripMember; add @@index([tripId, status]) and @@index([tripId, uploadedByTripMemberId]).
    4. Add relation fields on Trip and TripMember so Trip exposes vaultDocuments and TripMember exposes uploadedVaultDocuments plus reviewedVaultDocuments.
    5. Create apps/api/src/memories/vault-storage.service.ts that accepts exactly two payload families: `data:image/...;base64,...` and `data:application/pdf;base64,...`, rejects anything else, writes files under a trip-scoped memories/vault directory, and returns a relative asset path.
    6. Use explicit file extensions `.jpg`, `.png`, `.webp`, or `.pdf` based on MIME type and keep the service implementation deterministic like the checklist/attendance proof storage pattern.
  </action>
  <verify>npm exec prisma validate</verify>
  <acceptance_criteria>
    - packages/database/prisma/schema.prisma contains "model VaultDocument"
    - packages/database/prisma/schema.prisma contains "enum VaultDocumentKind"
    - packages/database/prisma/schema.prisma contains "enum VaultDocumentStatus"
    - packages/database/prisma/schema.prisma contains "FLIGHT_TICKET"
    - packages/database/prisma/schema.prisma contains "READY_FOR_CHECK_IN"
    - apps/api/src/memories/vault-storage.service.ts contains "application/pdf"
    - apps/api/src/memories/vault-storage.service.ts contains "data:image/"
    - apps/api/src/memories/vault-storage.service.ts contains ".pdf"
  </acceptance_criteria>
  <done>The database and storage layer can persist temporary trip documents with explicit type, status, and expiry metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Implement trip-scoped Digital Vault APIs and permission-safe snapshots</name>
  <files>apps/api/src/app.module.ts, apps/api/src/memories/memories.module.ts, apps/api/src/memories/memories.controller.ts, apps/api/src/memories/memories.service.ts, apps/api/src/memories/dto/upload-vault-document.dto.ts, apps/api/src/memories/dto/review-vault-document.dto.ts, apps/api/src/memories/memories.service.spec.ts, apps/api/test/phase-06-post-trip-memories.e2e-spec.ts</files>
  <read_first>
    apps/api/src/app.module.ts
    apps/api/src/checklists/checklists.service.ts
    packages/database/prisma/schema.prisma
    .planning/phases/06-post-trip-memories/06-RESEARCH.md
    .planning/phases/06-post-trip-memories/06-VALIDATION.md
  </read_first>
  <action>
    Add a MemoriesModule and trip-scoped vault endpoints.
    1. Register MemoriesModule in apps/api/src/app.module.ts.
    2. Create apps/api/src/memories/memories.controller.ts with exactly these routes for Phase 6.1: GET /trips/:tripId/memories/vault, POST /trips/:tripId/memories/vault/documents, and PATCH /trips/:tripId/memories/vault/documents/:documentId/review.
    3. Create UploadVaultDocumentDto with documentKind string, fileName string, mimeType string, fileDataUrl string, and optional note string.
    4. Create ReviewVaultDocumentDto with one required `status` field limited to exactly `READY_FOR_CHECK_IN` or `ARCHIVED`.
    5. In apps/api/src/memories/memories.service.ts implement getVaultSnapshot(tripId, userId) that returns tripId, isLeader, retentionLabel, supportedMimeTypes, and document rows. Leaders receive all documents for the trip. Non-leaders only receive their own documents.
    6. Implement uploadVaultDocument(tripId, userId, dto) so only joined trip members can upload, uploads go through VaultStorageService, expiresAt defaults to trip.endDate plus 7 days, and new rows start at status PENDING_REVIEW.
    7. Implement reviewVaultDocument(tripId, userId, documentId, dto) so only the trip leader can set the exact target status from ReviewVaultDocumentDto, and the returned snapshot includes the updated status and reviewed timestamp.
    8. Add service and e2e coverage for leader/member visibility boundaries, image/PDF acceptance, expiry defaulting, review authorization, and deterministic review-status transitions.
  </action>
  <verify>npx jest --config apps/api/test/jest-e2e.json --runInBand apps/api/test/phase-06-post-trip-memories.e2e-spec.ts --testNamePattern "vault|memory tab"</verify>
  <acceptance_criteria>
    - apps/api/src/app.module.ts contains "MemoriesModule"
    - apps/api/src/memories/memories.controller.ts contains "@Controller('trips/:tripId/memories')"
    - apps/api/src/memories/memories.controller.ts contains "vault/documents"
    - apps/api/src/memories/memories.controller.ts contains "documents/:documentId/review"
    - apps/api/src/memories/dto/review-vault-document.dto.ts contains "READY_FOR_CHECK_IN"
    - apps/api/src/memories/memories.service.ts contains "getVaultSnapshot"
    - apps/api/src/memories/memories.service.ts contains "READY_FOR_CHECK_IN"
    - apps/api/src/memories/memories.service.ts contains "supportedMimeTypes"
    - apps/api/test/phase-06-post-trip-memories.e2e-spec.ts contains "vault"
  </acceptance_criteria>
  <done>The API exposes a permission-safe Digital Vault snapshot and upload/review workflow for trip documents.</done>
</task>

</tasks>

<verification>
- [ ] Run `npm exec prisma validate`
- [ ] Run `npx jest --config apps/api/test/jest-e2e.json --runInBand apps/api/test/phase-06-post-trip-memories.e2e-spec.ts --testNamePattern "vault|memory tab"`
- [ ] Confirm members cannot read other members' vault documents from the returned snapshot
</verification>

<success_criteria>
- MEMO-01 backend supports temporary image/PDF document collection for one trip
- Leader/member visibility stays bounded and explicit
- The API exposes enough retention and status metadata for a truthful UI in the next plan
</success_criteria>

<output>
After completion, create `.planning/phases/06-post-trip-memories/06-01-SUMMARY.md`
</output>
