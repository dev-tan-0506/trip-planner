---
phase: 02-the-itinerary-engine
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/database/prisma/schema.prisma
  - apps/api/src/app.module.ts
  - apps/api/src/itinerary/itinerary.module.ts
  - apps/api/src/itinerary/itinerary.controller.ts
  - apps/api/src/itinerary/itinerary.service.ts
  - apps/api/src/itinerary/dto/create-itinerary-item.dto.ts
  - apps/api/src/itinerary/dto/update-itinerary-item.dto.ts
  - apps/api/src/itinerary/dto/reorder-itinerary.dto.ts
  - apps/api/src/proposals/proposals.module.ts
  - apps/api/src/proposals/proposals.controller.ts
  - apps/api/src/proposals/proposals.service.ts
  - apps/api/src/proposals/dto/create-proposal.dto.ts
  - apps/api/src/itinerary/itinerary.service.spec.ts
  - apps/api/src/proposals/proposals.service.spec.ts
  - apps/api/test/phase-02-itinerary.e2e-spec.ts
autonomous: true
requirements:
  - PLAN-01
  - PLAN-02
user_setup: []
must_haves:
  truths:
    - Canonical itinerary state is stored once per trip with deterministic ordering by dayIndex and sortOrder.
    - Leader-only structural writes are enforced on the server, while members can only read the snapshot and submit proposals.
    - The itinerary API returns one server-computed snapshot that includes grouped days, overlap warnings, proposal counts, and map-ready items.
  artifacts:
    - path: packages/database/prisma/schema.prisma
      contains: model ItineraryItem
      min_lines: 120
    - path: apps/api/src/itinerary/itinerary.service.ts
      contains: getTripItinerarySnapshot
      min_lines: 180
    - path: apps/api/src/proposals/proposals.service.ts
      contains: markOutdatedProposals
      min_lines: 120
    - path: apps/api/test/phase-02-itinerary.e2e-spec.ts
      contains: Phase 02 Itinerary API
      min_lines: 80
  key_links:
    - from: apps/api/src/itinerary/itinerary.controller.ts
      to: apps/api/src/itinerary/itinerary.service.ts
      via: REST itinerary endpoints delegate to the canonical snapshot service.
      pattern: this\.itineraryService\.
    - from: apps/api/src/proposals/proposals.controller.ts
      to: apps/api/src/proposals/proposals.service.ts
      via: Proposal review routes flow through the proposal service and stale-proposal handling.
      pattern: this\.proposalsService\.
---

<objective>
Create the Phase 2 backend foundation for itinerary and proposal workflows.

Purpose: Make the timeline the only source of truth before any UI, voting, or template work builds on top of it.
Output: Prisma models, trip-scoped itinerary/proposal REST APIs, and backend tests that lock canonical ordering and permissions in place.
</objective>

<execution_context>
@C:/Users/quoct/OneDrive/Desktop/code/inan/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/quoct/OneDrive/Desktop/code/inan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-the-itinerary-engine/02-CONTEXT.md
@.planning/phases/02-the-itinerary-engine/02-RESEARCH.md
@.planning/phases/02-the-itinerary-engine/02-VALIDATION.md
@.planning/phases/01-foundation-onboarding/01-CONTEXT.md
@apps/api/src/app.module.ts
@apps/api/src/trips/trips.module.ts
@apps/api/src/trips/trips.controller.ts
@apps/api/src/trips/trips.service.ts
@packages/database/prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the Phase 2 persistence contract in Prisma</name>
  <files>packages/database/prisma/schema.prisma</files>
  <read_first>
    packages/database/prisma/schema.prisma
    .planning/phases/02-the-itinerary-engine/02-CONTEXT.md
    .planning/phases/02-the-itinerary-engine/02-RESEARCH.md
  </read_first>
  <action>
    Update packages/database/prisma/schema.prisma to support the full Phase 2 domain in one schema pass so later wave-3 plans do not need to touch Prisma in parallel.
    1. Add Trip.timeZone as String with @default("Asia/Ho_Chi_Minh") so existing trips remain valid and itinerary progress can be evaluated in trip-local time.
    2. Add model ItineraryItem with fields id String @id @default(cuid()), tripId String, dayIndex Int, sortOrder Int, startMinute Int?, title String, locationName String?, locationAddress String?, placeId String?, lat Float?, lng Float?, shortNote String?, version Int @default(1), createdAt DateTime @default(now()), updatedAt DateTime @updatedAt; add relation to Trip and @@index([tripId, dayIndex]) plus @@unique([tripId, dayIndex, sortOrder]).
    3. Add enum ItineraryProposalType with ADD_ITEM, UPDATE_TIME, UPDATE_LOCATION, UPDATE_NOTE and enum ItineraryProposalStatus with PENDING, ACCEPTED, REJECTED, OUTDATED.
    4. Add model ItineraryProposal with fields id, tripId, proposerId, targetItemId String?, type ItineraryProposalType, payload Json, baseVersion Int?, status ItineraryProposalStatus @default(PENDING), reviewedById String?, reviewedAt DateTime?, createdAt DateTime @default(now()), updatedAt DateTime @updatedAt; relate proposer/reviewer to User and targetItem to ItineraryItem.
    5. Add the vote persistence models now so vote and template plans can execute in parallel later: VoteSession, VoteOption, VoteBallot plus enums VoteSessionMode (NEW_OPTION, REPLACE_ITEM, TIE_BREAK), VoteSessionStatus (PENDING_APPROVAL, OPEN, CLOSED, LEADER_DECISION_REQUIRED), VoteOptionStatus (PENDING_APPROVAL, ACTIVE, REJECTED, WINNER, RUNNER_UP). VoteSession must include tripId, createdById String, targetItemId String?, targetDayIndex Int?, targetInsertAfterItemId String?, deadline DateTime, parentSessionId String?, approvedById String?, tieBreakRound Int @default(0), and explicit Prisma relation names for createdBy, approvedBy, parentSession, and childSessions so tie-break ancestry is unambiguous. Use concrete self-relation wiring such as @relation("VoteSessionParentChild", fields: [parentSessionId], references: [id]) on parentSession and the matching childSessions relation on the model. Add model VoteSessionOutcome with fields id, voteSessionId String @unique, winningOptionId String, tripId String, targetItemId String?, targetDayIndex Int?, targetInsertAfterItemId String?, createdItemId String?, replacementProposalId String?, payload Json, createdAt DateTime @default(now()), updatedAt DateTime @updatedAt, and explicit relations back to VoteSession and Trip plus optional relations to the created itinerary item and replacement proposal when those follow-up records exist. NEW_OPTION winners must persist a VoteSessionOutcome row that captures the winning payload plus insertion target and later records createdItemId if the insertion is applied; REPLACE_ITEM winners must persist a VoteSessionOutcome row that captures the winning payload plus targetItemId and later records replacementProposalId when the leader-review proposal is created. VoteBallot must have unique constraint on [voteSessionId, userId] so only the latest active vote counts.
    6. Add model CommunityTemplate with id, sourceTripId, publishedById, title, destinationLabel, summary String?, coverNote String?, daysCount Int, cloneCount Int @default(0), sanitizedSnapshot Json, status String @default("PUBLISHED"), createdAt DateTime @default(now()), updatedAt DateTime @updatedAt.
    7. Add all necessary relation fields on Trip and User so Trip has itineraryItems, itineraryProposals, voteSessions, voteSessionOutcomes, communityTemplates and User has proposedItineraryChanges, reviewedItineraryChanges, createdVoteSessions, approvedVoteSessions, publishedTemplates, voteBallots.
  </action>
  <verify>npm exec prisma validate</verify>
  <acceptance_criteria>
    - packages/database/prisma/schema.prisma contains "timeZone    String       @default(\"Asia/Ho_Chi_Minh\")"
    - packages/database/prisma/schema.prisma contains "model ItineraryItem"
    - packages/database/prisma/schema.prisma contains "@@unique([tripId, dayIndex, sortOrder])"
    - packages/database/prisma/schema.prisma contains "model ItineraryProposal"
    - packages/database/prisma/schema.prisma contains "model VoteSession"
    - packages/database/prisma/schema.prisma contains "createdById String"
    - packages/database/prisma/schema.prisma contains "@relation(\"VoteSessionParentChild\""
    - packages/database/prisma/schema.prisma contains "model VoteSessionOutcome"
    - packages/database/prisma/schema.prisma contains "replacementProposalId String?"
    - packages/database/prisma/schema.prisma contains "@@unique([voteSessionId, userId])"
    - packages/database/prisma/schema.prisma contains "model CommunityTemplate"
    - packages/database/prisma/schema.prisma contains "sanitizedSnapshot Json"
  </acceptance_criteria>
  <done>All Phase 2 persistence models exist in schema.prisma with the canonical ordering, proposal, vote creator/ancestry, durable vote-outcome, and template fields named above.</done>
</task>

<task type="auto">
  <name>Task 2: Implement itinerary and proposal REST modules around a canonical snapshot</name>
  <files>apps/api/src/app.module.ts, apps/api/src/itinerary/itinerary.module.ts, apps/api/src/itinerary/itinerary.controller.ts, apps/api/src/itinerary/itinerary.service.ts, apps/api/src/itinerary/dto/create-itinerary-item.dto.ts, apps/api/src/itinerary/dto/update-itinerary-item.dto.ts, apps/api/src/itinerary/dto/reorder-itinerary.dto.ts, apps/api/src/proposals/proposals.module.ts, apps/api/src/proposals/proposals.controller.ts, apps/api/src/proposals/proposals.service.ts, apps/api/src/proposals/dto/create-proposal.dto.ts</files>
  <read_first>
    apps/api/src/app.module.ts
    apps/api/src/trips/trips.module.ts
    apps/api/src/trips/trips.controller.ts
    apps/api/src/trips/trips.service.ts
    packages/database/prisma/schema.prisma
    .planning/phases/02-the-itinerary-engine/02-CONTEXT.md
    .planning/phases/02-the-itinerary-engine/02-RESEARCH.md
  </read_first>
  <action>
    Add two new NestJS feature modules, ItineraryModule and ProposalsModule, and register both in apps/api/src/app.module.ts.
    1. In apps/api/src/itinerary/itinerary.controller.ts expose exactly these trip-scoped routes:
       - GET /trips/:tripId/itinerary
       - POST /trips/:tripId/itinerary/items
       - PATCH /trips/:tripId/itinerary/items/:itemId
       - DELETE /trips/:tripId/itinerary/items/:itemId
       - POST /trips/:tripId/itinerary/reorder
    2. In apps/api/src/proposals/proposals.controller.ts expose exactly these routes:
       - GET /trips/:tripId/proposals
       - POST /trips/:tripId/proposals
       - POST /trips/:tripId/proposals/:proposalId/accept
       - POST /trips/:tripId/proposals/:proposalId/reject
    3. Build apps/api/src/itinerary/itinerary.service.ts around a getTripItinerarySnapshot(tripId, userId) method that returns days grouped by dayIndex, each item ordered by sortOrder, plus permissions, proposal counts, overlap warnings, and mapItems filtered to records with non-null lat/lng.
    4. Enforce leader-only structural mutations for create, update, delete, and reorder. The reorder DTO must accept an ordered array of itemId/dayIndex/sortOrder tuples and normalize sortOrder to 1..N inside a single Prisma transaction.
    5. Expose startTime to the web as an HH:mm string while persisting startMinute Int? in Prisma. Derived progress states must be computed on read as one of "sap toi", "dang di", "da di", or "chua chot gio". Untimed items must never be forced into timed progress states.
    6. In the create/update DTOs support quick mode fields title, dayIndex, insertAfterItemId, startTime, locationName, shortNote and detailed-mode fields locationAddress, placeId, lat, lng. Saving an overlap must remain allowed, but the snapshot must include overlapWarnings for the conflicting same-day items.
    7. In apps/api/src/proposals/proposals.service.ts allow members to create proposal types ADD_ITEM, UPDATE_TIME, UPDATE_LOCATION, UPDATE_NOTE, and require leaders to accept or reject them one by one. Accepting a proposal must update the itinerary item or create a new item, increment the target item version, and mark older proposals with the same targetItemId and mismatched baseVersion as OUTDATED.
  </action>
  <verify>npm run test -w apps/api -- --runInBand</verify>
  <acceptance_criteria>
    - apps/api/src/app.module.ts contains "ItineraryModule"
    - apps/api/src/app.module.ts contains "ProposalsModule"
    - apps/api/src/itinerary/itinerary.controller.ts contains "@Controller('trips/:tripId/itinerary')"
    - apps/api/src/itinerary/itinerary.controller.ts contains "reorder"
    - apps/api/src/itinerary/itinerary.service.ts contains "getTripItinerarySnapshot"
    - apps/api/src/itinerary/itinerary.service.ts contains "\"chua chot gio\""
    - apps/api/src/itinerary/itinerary.service.ts contains "overlapWarnings"
    - apps/api/src/proposals/proposals.controller.ts contains "@Controller('trips/:tripId/proposals')"
    - apps/api/src/proposals/proposals.service.ts contains "markOutdatedProposals"
    - apps/api/src/proposals/proposals.service.ts contains "ItineraryProposalStatus.OUTDATED"
  </acceptance_criteria>
  <done>The API exposes the itinerary/proposal routes above and every mutation flows back through a canonical snapshot response.</done>
</task>

<task type="auto">
  <name>Task 3: Add backend tests that freeze ordering, permissions, and stale-proposal behavior</name>
  <files>apps/api/src/itinerary/itinerary.service.spec.ts, apps/api/src/proposals/proposals.service.spec.ts, apps/api/test/phase-02-itinerary.e2e-spec.ts</files>
  <read_first>
    apps/api/package.json
    apps/api/src/trips/trips.service.ts
    apps/api/src/itinerary/itinerary.service.ts
    apps/api/src/proposals/proposals.service.ts
    .planning/phases/02-the-itinerary-engine/02-VALIDATION.md
  </read_first>
  <action>
    Create the Phase 2 backend test baseline required by VALIDATION.md.
    1. Add apps/api/src/itinerary/itinerary.service.spec.ts with cases for move-between-days reorder normalization, overlap warning generation, untimed item state remaining "chua chot gio", and leader-only structural mutations.
    2. Add apps/api/src/proposals/proposals.service.spec.ts with cases for member proposal creation, leader accept/reject flows, and stale proposal invalidation when target item version changes.
    3. Add apps/api/test/phase-02-itinerary.e2e-spec.ts with an end-to-end flow covering: leader creates trip, leader creates itinerary items, member fetches read-only itinerary, member submits proposal, leader accepts proposal, and reorder persists in later GET /trips/:tripId/itinerary responses.
    4. If the apps/api test bootstrap is missing reusable helpers, add only the minimal helper wiring needed by the new spec files and keep the suite runnable through the existing "test" and "test:e2e" scripts.
  </action>
  <verify>npm run test -w apps/api -- --runInBand</verify>
  <acceptance_criteria>
    - apps/api/src/itinerary/itinerary.service.spec.ts contains "describe('ItineraryService'"
    - apps/api/src/itinerary/itinerary.service.spec.ts contains "move-between-days"
    - apps/api/src/proposals/proposals.service.spec.ts contains "describe('ProposalsService'"
    - apps/api/src/proposals/proposals.service.spec.ts contains "OUTDATED"
    - apps/api/test/phase-02-itinerary.e2e-spec.ts contains "describe('Phase 02 Itinerary API'"
    - apps/api/test/phase-02-itinerary.e2e-spec.ts contains "POST /trips/:tripId/itinerary/reorder"
  </acceptance_criteria>
  <done>Backend tests exist for the itinerary and proposal service rules that every later Phase 2 plan relies on.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `npm exec prisma validate`
- [ ] Run `npm run test -w apps/api -- --runInBand`
- [ ] Confirm the itinerary snapshot includes grouped days, progress states, overlap warnings, proposal counts, and mapItems from the same service response
</verification>

<success_criteria>
- All itinerary ordering is deterministic through `dayIndex + sortOrder`
- Members cannot mutate structural itinerary data through the REST API
- Proposal acceptance updates itinerary state and marks stale sibling proposals as `OUTDATED`
- Backend tests cover ordering, permission, and stale-proposal rules before UI work starts
</success_criteria>

<output>
After completion, create `.planning/phases/02-the-itinerary-engine/02-01-SUMMARY.md`
</output>
