---
phase: 02-the-itinerary-engine
plan: "05"
type: execute
wave: 5
depends_on:
  - "02-02"
files_modified:
  - apps/web/package.json
  - apps/web/src/components/trip/TimelineDaySection.tsx
  - apps/web/src/components/trip/TripWorkspaceShell.tsx
  - apps/web/src/components/trip/__tests__/trip-workspace.test.tsx
autonomous: false
requirements:
  - PLAN-01
  - PLAN-02
gap_closure: true
must_haves:
  truths:
    - Leaders can drag an itinerary card within the same day or into another day and the reordered timeline still persists after refresh.
    - Members still see the itinerary as read-only and do not get drag handles or sortable interaction.
    - The reorder regression is covered by an automated UI test that exercises the drag flow instead of only checking static controls.
  artifacts:
    - path: apps/web/src/components/trip/TimelineDaySection.tsx
      provides: Browser-stable sortable day sections and drop targets for itinerary cards
      min_lines: 220
    - path: apps/web/src/components/trip/TripWorkspaceShell.tsx
      provides: Canonical reorder persistence path from drag result to itineraryApi.reorder
      min_lines: 300
    - path: apps/web/src/components/trip/__tests__/trip-workspace.test.tsx
      provides: Regression coverage for leader reorder behavior and member read-only guardrails
      min_lines: 220
    - path: apps/web/package.json
      provides: Stable sortable interaction dependency set for the web workspace
      contains: "@dnd-kit"
  key_links:
    - from: apps/web/src/components/trip/TimelineDaySection.tsx
      to: apps/web/src/components/trip/TripWorkspaceShell.tsx
      via: Sortable drag end resolves target day/index and calls onReorderItem with canonical coordinates.
      pattern: onReorderItem
    - from: apps/web/src/components/trip/TripWorkspaceShell.tsx
      to: apps/web/src/lib/api-client.ts
      via: handleReorderItem posts the normalized payload to itineraryApi.reorder and refreshes snapshot state.
      pattern: itineraryApi\.reorder
    - from: apps/web/src/components/trip/__tests__/trip-workspace.test.tsx
      to: apps/web/src/components/trip/TripWorkspaceShell.tsx
      via: The test simulates leader drag interaction and asserts reorder API payload/persisted render order.
      pattern: reorder
---

<objective>
Close the remaining Phase 2 browser gap in timeline reordering.

Purpose: The only diagnosed Phase 2 failure is that leaders cannot actually drag itinerary cards even though the reorder API exists and manual move buttons do.
Output: A stable sortable timeline interaction, a regression test that covers the broken path, and a blocking browser verification for the reported drag-and-drop issue.
</objective>

<execution_context>
@C:/Users/quoct/OneDrive/Desktop/code/designapp/trip-planner/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/quoct/OneDrive/Desktop/code/designapp/trip-planner/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-the-itinerary-engine/02-CONTEXT.md
@.planning/phases/02-the-itinerary-engine/02-RESEARCH.md
@.planning/phases/02-the-itinerary-engine/02-VERIFICATION.md
@.planning/phases/02-the-itinerary-engine/02-HUMAN-UAT.md
@.planning/phases/02-the-itinerary-engine/02-02-SUMMARY.md
@apps/web/package.json
@apps/web/src/lib/api-client.ts
@apps/web/src/components/trip/TimelineDaySection.tsx
@apps/web/src/components/trip/TripWorkspaceShell.tsx
@apps/web/src/components/trip/__tests__/trip-workspace.test.tsx

<interfaces>
From apps/web/src/lib/api-client.ts:
```ts
export interface ItineraryItem {
  id: string;
  tripId: string;
  dayIndex: number;
  sortOrder: number;
  startMinute: number | null;
  startTime: string | null;
  title: string;
  locationName: string | null;
  locationAddress: string | null;
  placeId: string | null;
  lat: number | null;
  lng: number | null;
  shortNote: string | null;
  version: number;
  progress: ProgressState;
  proposalCount: number;
  createdAt: string;
  updatedAt: string;
}

export interface DayGroup {
  dayIndex: number;
  items: ItineraryItem[];
}

export interface ItinerarySnapshot {
  tripId: string;
  days: DayGroup[];
  overlapWarnings: OverlapWarning[];
  mapItems: MapItem[];
  totalItems: number;
  isLeader: boolean;
  canEdit: boolean;
}

export const itineraryApi = {
  async reorder(tripId: string, body: ReorderPayload): Promise<ItinerarySnapshot>;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace native HTML drag events with a sortable leader-only timeline interaction</name>
  <files>apps/web/package.json, apps/web/src/components/trip/TimelineDaySection.tsx, apps/web/src/components/trip/TripWorkspaceShell.tsx</files>
  <read_first>
    apps/web/package.json
    apps/web/src/lib/api-client.ts
    apps/web/src/components/trip/TimelineDaySection.tsx
    apps/web/src/components/trip/TripWorkspaceShell.tsx
    .planning/phases/02-the-itinerary-engine/02-CONTEXT.md
    .planning/phases/02-the-itinerary-engine/02-VERIFICATION.md
    .planning/phases/02-the-itinerary-engine/02-HUMAN-UAT.md
  </read_first>
  <action>
    Replace the current native `draggable` + `onDragStart/onDrop` flow in TimelineDaySection with a browser-stable sortable implementation that satisfies D-03 and keeps the timeline as the source of truth.
    1. Add `@dnd-kit/core`, `@dnd-kit/sortable`, and `@dnd-kit/utilities` to apps/web/package.json. Do not introduce any second reorder source or local-only ordering model.
    2. In apps/web/src/components/trip/TimelineDaySection.tsx, remove native HTML drag handlers and build sortable item cards with explicit drag handles for leaders only. Use a pointer sensor with an activation distance of 8 pixels so simple taps on edit/delete/add buttons do not start a drag.
    3. Support both same-day and cross-day drops per D-03 by making each day section a droppable container and each card a sortable item. Empty days must accept a dropped card at index 0.
    4. Keep ArrowUp and ArrowDown button fallbacks intact for keyboard/mouse precision, but do not treat them as the primary fix. The primary interaction must be true drag-and-drop.
    5. In apps/web/src/components/trip/TripWorkspaceShell.tsx, keep `handleReorderItem` as the single persistence path, but adjust the optimistic list logic if needed so dropping into a new day preserves the moved item's `dayIndex` and sends a fully normalized `items` payload to `itineraryApi.reorder`.
    6. Preserve PLAN-02 and the existing member guardrails: members must not initialize sortable sensors, must not see drag handles, and must still only see proposal actions.
    7. Keep the existing progress chips, proposal badges, map links, and playful Vietnamese-first presentation intact; this task is only about fixing the broken reorder interaction and its visual drop affordances.
  </action>
  <verify>
    <automated>npm run -w apps/web check-types</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/package.json contains "@dnd-kit/core"
    - apps/web/package.json contains "@dnd-kit/sortable"
    - apps/web/package.json contains "@dnd-kit/utilities"
    - apps/web/src/components/trip/TimelineDaySection.tsx does not contain "draggable={canEdit}"
    - apps/web/src/components/trip/TimelineDaySection.tsx contains "PointerSensor"
    - apps/web/src/components/trip/TimelineDaySection.tsx contains "SortableContext"
    - apps/web/src/components/trip/TimelineDaySection.tsx contains "useSortable"
    - apps/web/src/components/trip/TimelineDaySection.tsx contains "distance: 8"
    - apps/web/src/components/trip/TripWorkspaceShell.tsx contains "itineraryApi.reorder"
    - apps/web/src/components/trip/TripWorkspaceShell.tsx contains "dayIndex: day.dayIndex"
  </acceptance_criteria>
  <done>The leader can perform real drag-and-drop reorder within and across days, and every drop still persists through the existing reorder API path.</done>
</task>

<task type="auto">
  <name>Task 2: Add a regression test that proves reorder behavior instead of static presence</name>
  <files>apps/web/src/components/trip/__tests__/trip-workspace.test.tsx</files>
  <read_first>
    apps/web/src/components/trip/__tests__/trip-workspace.test.tsx
    apps/web/src/components/trip/TripWorkspaceShell.tsx
    apps/web/src/components/trip/TimelineDaySection.tsx
    apps/web/src/lib/api-client.ts
    .planning/phases/02-the-itinerary-engine/02-VERIFICATION.md
  </read_first>
  <action>
    Extend the existing workspace regression suite so it fails if the drag interaction stops producing real reorder requests again.
    1. Add a leader-focused same-day reorder test that renders at least three itinerary items in one day, simulates a drag from one card to another drop position, and asserts `itineraryApi.reorder` is called with an `items` payload containing updated `dayIndex` and `sortOrder` values for the same day.
    2. Add a separate leader-focused cross-day move test that renders at least two days, drags one item into a different day container, and asserts `itineraryApi.reorder` is called with the moved item's updated `dayIndex` plus normalized `sortOrder` values in both affected days.
    3. Mock the reorder response with a changed snapshot and assert the new item order is what the user sees after the promise resolves for both same-day and cross-day paths. This makes the test cover the same user-visible behavior as the UAT gap.
    4. Add a member-focused assertion in the same suite that drag handles are absent when `canEdit` is false so the new DnD dependency does not weaken PLAN-02.
    5. Keep the test file in the existing `trip-workspace.test.tsx` suite unless the file becomes unmanageably large. Do not add a shallow unit test that only checks component props.
  </action>
  <verify>
    <automated>npx vitest run src/components/trip/__tests__/trip-workspace.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/trip/__tests__/trip-workspace.test.tsx contains "itineraryApi.reorder"
    - apps/web/src/components/trip/__tests__/trip-workspace.test.tsx contains "sortOrder"
    - apps/web/src/components/trip/__tests__/trip-workspace.test.tsx contains "dayIndex"
    - apps/web/src/components/trip/__tests__/trip-workspace.test.tsx contains "drag"
    - apps/web/src/components/trip/__tests__/trip-workspace.test.tsx contains "same-day"
    - apps/web/src/components/trip/__tests__/trip-workspace.test.tsx contains "cross-day"
    - apps/web/src/components/trip/__tests__/trip-workspace.test.tsx contains "canEdit: false"
  </acceptance_criteria>
  <done>The UI suite now fails if leader reorder stops emitting the canonical payload or if member read-only mode accidentally exposes drag controls.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the repaired drag-and-drop flow in a real browser</name>
  <files>.planning/phases/02-the-itinerary-engine/02-HUMAN-UAT.md, .planning/phases/02-the-itinerary-engine/02-VERIFICATION.md</files>
  <read_first>
    .planning/phases/02-the-itinerary-engine/02-HUMAN-UAT.md
    .planning/phases/02-the-itinerary-engine/02-VERIFICATION.md
    apps/web/src/components/trip/TimelineDaySection.tsx
    apps/web/src/components/trip/TripWorkspaceShell.tsx
  </read_first>
  <action>
    Run the exact browser check that failed in UAT after Tasks 1 and 2 are complete, then record whether the leader can drag across real rendered cards and whether member read-only mode stayed intact.
  </action>
  <what-built>Leader-facing drag-and-drop reorder now uses a sortable interaction model instead of native HTML drag events, with the same persistence path and regression coverage.</what-built>
  <how-to-verify>
    1. Open a trip workspace as the leader on the timeline tab.
    2. Ensure there are at least three itinerary items across one or more days.
    3. Drag one item by its handle to a new position inside the same day, then refresh the page and confirm the new order persists.
    4. Drag one item into a different day, then refresh the page and confirm it stays in the new day and slot.
    5. Open the same trip as a non-leader member and confirm the timeline remains visible but no drag handle appears.
  </how-to-verify>
  <resume-signal>Type "approved" if all five checks pass, or describe the failing step precisely.</resume-signal>
  <verify>
    <automated>npm run -w apps/web check-types && npx vitest run src/components/trip/__tests__/trip-workspace.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - 02-HUMAN-UAT.md gap 1 can be marked pass after the browser run
    - leader can reorder within the same day and across days
    - refresh preserves the new order
    - member timeline remains read-only
  </acceptance_criteria>
  <done>The previously failed human test for drag-and-drop reorder passes in an actual browser session and Phase 2 no longer has open drag-and-drop verification debt.</done>
</task>

</tasks>

<verification>
- Run `npm run -w apps/web check-types`
- Run `npx vitest run src/components/trip/__tests__/trip-workspace.test.tsx`
- Complete the blocking browser verification for same-day reorder, cross-day reorder, refresh persistence, and member read-only behavior
</verification>

<success_criteria>
- The diagnosed Phase 2 gap "hiện tại vẫn chưa kéo thả được" is closed with a browser-stable drag interaction
- Reorder persists through the existing canonical API path instead of a separate client-only state model
- PLAN-02 stays intact while fixing PLAN-01
- The remaining manual debt is reduced to a single explicit browser checkpoint and can be marked passed once approved
</success_criteria>

<output>
After completion, create `.planning/phases/02-the-itinerary-engine/02-05-SUMMARY.md`
</output>
