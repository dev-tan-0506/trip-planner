---
phase: 02-the-itinerary-engine
plan: "04"
type: execute
wave: 4
depends_on:
  - "02-01"
  - "02-02"
  - "02-03"
files_modified:
  - apps/api/src/app.module.ts
  - apps/api/src/templates/templates.module.ts
  - apps/api/src/templates/templates.controller.ts
  - apps/api/src/templates/templates.service.ts
  - apps/api/src/templates/dto/publish-template.dto.ts
  - apps/api/src/templates/dto/clone-template.dto.ts
  - apps/api/src/templates/templates.service.spec.ts
  - apps/api/test/phase-02-itinerary.e2e-spec.ts
  - apps/web/src/lib/api-client.ts
  - apps/web/app/dashboard/page.tsx
  - apps/web/app/templates/page.tsx
  - apps/web/app/templates/[templateId]/page.tsx
  - apps/web/src/components/templates/TemplateLibraryPage.tsx
  - apps/web/src/components/templates/TemplatePreviewCard.tsx
  - apps/web/src/components/templates/CloneTemplateDialog.tsx
  - apps/web/src/components/templates/PublishTemplateDialog.tsx
  - apps/web/src/components/templates/__tests__/template-library.test.tsx
autonomous: true
requirements:
  - TRIP-03
user_setup: []
must_haves:
  truths:
    - Publishing a community template strips personal trip data and stores a sanitized itinerary snapshot with no live link back to the source trip.
    - Cloning a template creates a new trip plus deep-copied itinerary items remapped from relative Day 1, Day 2, and onward to the new trip dates.
    - Template browsing and cloning are one-click approachable on the web, but only leaders can publish.
  artifacts:
    - path: apps/api/src/templates/templates.service.ts
      contains: sanitizedSnapshot
      min_lines: 180
    - path: apps/web/app/templates/page.tsx
      contains: TemplateLibraryPage
      min_lines: 30
    - path: apps/web/src/components/templates/CloneTemplateDialog.tsx
      contains: startDate
      min_lines: 70
    - path: apps/web/src/components/templates/__tests__/template-library.test.tsx
      contains: clone
      min_lines: 50
  key_links:
    - from: apps/api/src/templates/templates.controller.ts
      to: apps/api/src/templates/templates.service.ts
      via: Publish and clone endpoints delegate to the template snapshot service.
      pattern: this\.templatesService\.
    - from: apps/web/src/lib/api-client.ts
      to: apps/web/src/components/templates/CloneTemplateDialog.tsx
      via: The clone dialog submits the new trip payload through typed template client methods.
      pattern: templatesApi
    - from: apps/web/app/templates/page.tsx
      to: apps/web/src/components/templates/TemplateLibraryPage.tsx
      via: The templates route renders the library page component.
      pattern: TemplateLibraryPage
---

<objective>
Ship community template publishing and deep-copy cloning.

Purpose: Let users bootstrap a trip from reusable community itineraries without sharing live state or personal data.
Output: Template publish/clone APIs, a template library UI, leader-only publish flow, and tests that prove cloning creates a separate trip copy.
</objective>

<execution_context>
@C:/Users/quoct/OneDrive/Desktop/code/inan/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/quoct/OneDrive/Desktop/code/inan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-the-itinerary-engine/02-CONTEXT.md
@.planning/phases/02-the-itinerary-engine/02-RESEARCH.md
@.planning/phases/02-the-itinerary-engine/02-VALIDATION.md
@apps/api/src/app.module.ts
@apps/api/src/trips/trips.service.ts
@apps/web/src/lib/api-client.ts
@apps/web/app/dashboard/page.tsx
@packages/database/prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement sanitized template publish and deep-copy clone APIs</name>
  <files>apps/api/src/app.module.ts, apps/api/src/templates/templates.module.ts, apps/api/src/templates/templates.controller.ts, apps/api/src/templates/templates.service.ts, apps/api/src/templates/dto/publish-template.dto.ts, apps/api/src/templates/dto/clone-template.dto.ts</files>
  <read_first>
    apps/api/src/app.module.ts
    apps/api/src/trips/trips.service.ts
    packages/database/prisma/schema.prisma
    .planning/phases/02-the-itinerary-engine/02-CONTEXT.md
    .planning/phases/02-the-itinerary-engine/02-RESEARCH.md
  </read_first>
  <action>
    Add a TemplatesModule that turns itinerary snapshots into reusable community templates and back into new trips.
    1. Register TemplatesModule in apps/api/src/app.module.ts.
    2. In apps/api/src/templates/templates.controller.ts expose:
       - GET /templates
       - GET /templates/:templateId
       - POST /trips/:tripId/templates/publish
       - POST /templates/:templateId/clone
    3. In apps/api/src/templates/templates.service.ts implement publishTemplate(tripId, leaderUserId) so it is leader-only, serializes the current itinerary into CommunityTemplate.sanitizedSnapshot, stores only planning content (title, dayIndex, startMinute, location fields, shortNote), and strips member data, votes, proposal history, joinCode, and any user identifiers.
    4. Implement cloneTemplate(templateId, userId, dto) so the body requires name, destination, startDate, endDate, and timeZone. The clone flow must create a brand-new Trip, add the caller as LEADER, remap the template's relative days onto the new trip dates, and deep-copy every itinerary item into the new trip with no foreign keys pointing back to the source trip or template.
    5. Increment CommunityTemplate.cloneCount on successful clone and return the new trip's joinCode so the web can route into the copied trip immediately.
  </action>
  <verify>npm run test -w apps/api -- --runInBand</verify>
  <acceptance_criteria>
    - apps/api/src/app.module.ts contains "TemplatesModule"
    - apps/api/src/templates/templates.controller.ts contains "@Controller('templates')"
    - apps/api/src/templates/templates.controller.ts contains "@Controller('trips/:tripId/templates')"
    - apps/api/src/templates/templates.service.ts contains "sanitizedSnapshot"
    - apps/api/src/templates/templates.service.ts contains "cloneCount"
    - apps/api/src/templates/templates.service.ts contains "joinCode"
    - apps/api/src/templates/templates.service.ts contains "role: 'LEADER'"
  </acceptance_criteria>
  <done>The API can publish a sanitized template and clone it into a brand-new leader-owned trip with a deep-copied itinerary.</done>
</task>

<task type="auto">
  <name>Task 2: Build the template library, preview, publish, and clone UI flows</name>
  <files>apps/web/src/lib/api-client.ts, apps/web/app/dashboard/page.tsx, apps/web/app/templates/page.tsx, apps/web/app/templates/[templateId]/page.tsx, apps/web/src/components/templates/TemplateLibraryPage.tsx, apps/web/src/components/templates/TemplatePreviewCard.tsx, apps/web/src/components/templates/CloneTemplateDialog.tsx, apps/web/src/components/templates/PublishTemplateDialog.tsx</files>
  <read_first>
    apps/web/src/lib/api-client.ts
    apps/web/app/dashboard/page.tsx
    apps/web/app/trip/[joinCode]/page.tsx
    .planning/phases/02-the-itinerary-engine/02-CONTEXT.md
    .planning/phases/02-the-itinerary-engine/02-RESEARCH.md
  </read_first>
  <action>
    Create the web entry points for browsing and using community templates.
    1. Extend apps/web/src/lib/api-client.ts with templatesApi methods list(), get(templateId), publish(tripId), and clone(templateId, body).
    2. Add a library entry point from apps/web/app/dashboard/page.tsx and create apps/web/app/templates/page.tsx plus apps/web/app/templates/[templateId]/page.tsx.
    3. Create TemplateLibraryPage.tsx and TemplatePreviewCard.tsx to render template cards from sanitized snapshot data, including destination label, day count, and top itinerary highlights.
    4. Create PublishTemplateDialog.tsx for leaders only. It must call publish(tripId), explain that personal/member data will be stripped, and only be visible when the current user is the trip leader.
    5. Create CloneTemplateDialog.tsx that collects name, destination, startDate, endDate, and timeZone, submits POST /templates/:templateId/clone, and routes to `/trip/{joinCode}` on success.
    6. The template detail page must preview relative Day 1, Day 2, and onward from sanitizedSnapshot rather than reading live source-trip data.
  </action>
  <verify>npm run lint && npm run check-types && npm run test:ui -w apps/web</verify>
  <acceptance_criteria>
    - apps/web/src/lib/api-client.ts contains "export const templatesApi ="
    - apps/web/app/templates/page.tsx contains "TemplateLibraryPage"
    - apps/web/app/templates/[templateId]/page.tsx contains "CloneTemplateDialog"
    - apps/web/src/components/templates/PublishTemplateDialog.tsx contains "Du lieu ca nhan se duoc loai bo"
    - apps/web/src/components/templates/CloneTemplateDialog.tsx contains "timeZone"
    - apps/web/src/components/templates/CloneTemplateDialog.tsx contains "/trip/${joinCode}"
    - apps/web/app/dashboard/page.tsx contains "/templates"
  </acceptance_criteria>
  <done>The web app exposes a discoverable template library, leader-only publishing, and one-click clone flow into a new trip.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests that prove templates are sanitized and cloned as detached copies</name>
  <files>apps/api/src/templates/templates.service.spec.ts, apps/api/test/phase-02-itinerary.e2e-spec.ts, apps/web/src/components/templates/__tests__/template-library.test.tsx</files>
  <read_first>
    apps/api/src/templates/templates.service.ts
    apps/web/src/components/templates/CloneTemplateDialog.tsx
    .planning/phases/02-the-itinerary-engine/02-VALIDATION.md
  </read_first>
  <action>
    Add automated validation for the template lifecycle.
    1. Create apps/api/src/templates/templates.service.spec.ts with cases that prove publish strips member/joinCode data, clone creates a new trip and leader membership, and cloned itinerary items do not retain sourceTripId or source item IDs.
    2. Extend apps/api/test/phase-02-itinerary.e2e-spec.ts so the phase-wide API spec also covers publishing a template and cloning it into a new trip.
    3. Create apps/web/src/components/templates/__tests__/template-library.test.tsx with cases for rendering sanitized preview cards, showing publish only to leaders, and submitting the clone dialog payload with startDate/endDate/timeZone.
  </action>
  <verify>npm run test -w apps/api -- --runInBand && npm run test:ui -w apps/web</verify>
  <acceptance_criteria>
    - apps/api/src/templates/templates.service.spec.ts contains "describe('TemplatesService'"
    - apps/api/src/templates/templates.service.spec.ts contains "sanitizedSnapshot"
    - apps/api/src/templates/templates.service.spec.ts contains "role: 'LEADER'"
    - apps/api/test/phase-02-itinerary.e2e-spec.ts contains "/templates/:templateId/clone"
    - apps/web/src/components/templates/__tests__/template-library.test.tsx contains "PublishTemplateDialog"
    - apps/web/src/components/templates/__tests__/template-library.test.tsx contains "timeZone"
  </acceptance_criteria>
  <done>Template publishing and cloning are covered by automated tests that verify detachment and sanitization.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `npm run test -w apps/api -- --runInBand`
- [ ] Run `npm run lint`
- [ ] Run `npm run check-types`
- [ ] Run `npm run test:ui -w apps/web`
- [ ] Manually confirm a published template preview does not show member names, join codes, or vote/proposal data
</verification>

<success_criteria>
- Templates are stored as sanitized snapshots, not live forks
- Cloning creates a new trip with remapped relative days and deep-copied itinerary items
- Only leaders can publish templates
- Template browsing and cloning are discoverable from the web app without leaving the Phase 2 trip flow
</success_criteria>

<output>
After completion, create `.planning/phases/02-the-itinerary-engine/02-04-SUMMARY.md`
</output>
