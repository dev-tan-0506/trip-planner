---
phase: 02-the-itinerary-engine
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - apps/web/package.json
  - apps/web/src/lib/api-client.ts
  - apps/web/app/trip/[joinCode]/page.tsx
  - apps/web/src/app/trip/[joinCode]/page.tsx
  - apps/web/app/trip/[joinCode]/map/page.tsx
  - apps/web/app/globals.css
  - apps/web/src/components/trip/TripWorkspaceShell.tsx
  - apps/web/src/components/trip/TimelineDaySection.tsx
  - apps/web/src/components/trip/ItineraryComposerSheet.tsx
  - apps/web/src/components/trip/ItineraryItemEditor.tsx
  - apps/web/src/components/trip/DeleteItineraryItemDialog.tsx
  - apps/web/src/components/trip/ProposalComposerSheet.tsx
  - apps/web/src/components/trip/ProposalInboxPanel.tsx
  - apps/web/src/components/trip/TripMapScreen.tsx
  - apps/web/src/components/trip/LocationPicker.tsx
  - apps/web/src/components/trip/__tests__/trip-workspace.test.tsx
  - apps/web/vitest.config.ts
  - apps/web/src/test/setup.ts
autonomous: true
requirements:
  - PLAN-01
  - PLAN-02
user_setup: []
must_haves:
  truths:
    - Leaders can add, edit, delete, and reorder itinerary items from a dedicated trip workspace while members only see read-only structure plus proposal actions.
    - The timeline visibly distinguishes sap toi, dang di, da di, and chua chot gio, and opens to the current region by default.
    - The map is a separate projection mode that follows itinerary ordering, remembers its day filter, and never becomes a second source of truth.
  artifacts:
    - path: apps/web/app/trip/[joinCode]/page.tsx
      contains: TripWorkspaceShell
      min_lines: 80
    - path: apps/web/app/trip/[joinCode]/map/page.tsx
      contains: TripMapScreen
      min_lines: 40
    - path: apps/web/src/components/trip/ProposalComposerSheet.tsx
      contains: UPDATE_TIME
      min_lines: 80
    - path: apps/web/src/components/trip/ProposalInboxPanel.tsx
      contains: proposal
      min_lines: 80
    - path: apps/web/src/components/trip/__tests__/trip-workspace.test.tsx
      contains: TripWorkspaceShell
      min_lines: 60
  key_links:
    - from: apps/web/src/lib/api-client.ts
      to: apps/web/src/components/trip/TripWorkspaceShell.tsx
      via: Workspace components use typed itinerary and proposal client methods.
      pattern: itineraryApi
    - from: apps/web/app/trip/[joinCode]/page.tsx
      to: apps/web/src/components/trip/TripWorkspaceShell.tsx
      via: The trip route hands authenticated members into the workspace shell.
      pattern: TripWorkspaceShell
    - from: apps/web/app/trip/[joinCode]/map/page.tsx
      to: apps/web/src/components/trip/TripMapScreen.tsx
      via: The dedicated map route renders the map projection component.
      pattern: TripMapScreen
---

<objective>
Build the member-facing itinerary workspace on top of the canonical REST backend.

Purpose: Deliver the actual Phase 2 trip-planning surface where leaders manage the timeline, members follow it in real time, and map/proposal flows stay subordinate to the timeline.
Output: A trip workspace shell, timeline CRUD UI, proposal inbox UI, dedicated map screen, and frontend interaction tests.
</objective>

<execution_context>
@C:/Users/quoct/OneDrive/Desktop/code/inan/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/quoct/OneDrive/Desktop/code/inan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-the-itinerary-engine/02-CONTEXT.md
@.planning/phases/02-the-itinerary-engine/02-RESEARCH.md
@.planning/phases/02-the-itinerary-engine/02-VALIDATION.md
@.planning/phases/01-foundation-onboarding/01-CONTEXT.md
@apps/web/app/trip/[joinCode]/page.tsx
@apps/web/src/app/trip/[joinCode]/page.tsx
@apps/web/src/store/useAuthStore.ts
@apps/web/src/components/ui/AuthHydration.tsx
@apps/web/src/lib/api-client.ts
@apps/web/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade the web API client and route shell for authenticated trip workspaces</name>
  <files>apps/web/src/lib/api-client.ts, apps/web/app/trip/[joinCode]/page.tsx, apps/web/src/app/trip/[joinCode]/page.tsx, apps/web/src/components/trip/TripWorkspaceShell.tsx</files>
  <read_first>
    apps/web/src/lib/api-client.ts
    apps/web/app/trip/[joinCode]/page.tsx
    apps/web/src/app/trip/[joinCode]/page.tsx
    apps/web/src/store/useAuthStore.ts
    apps/web/src/components/ui/AuthHydration.tsx
    .planning/phases/02-the-itinerary-engine/02-CONTEXT.md
    .planning/phases/02-the-itinerary-engine/02-RESEARCH.md
  </read_first>
  <action>
    Convert the trip join-code route into a guest-preview plus member-workspace shell without breaking the viral read-only entry flow.
    1. Extend apps/web/src/lib/api-client.ts with typed interfaces and methods for itineraryApi and proposalsApi. Include methods getSnapshot(tripId), createItem(tripId, body), updateItem(tripId, itemId, body), deleteItem(tripId, itemId), reorder(tripId, body), listProposals(tripId), createProposal(tripId, body), acceptProposal(tripId, proposalId), rejectProposal(tripId, proposalId).
    2. Canonicalize apps/web/app/trip/[joinCode]/page.tsx as the only active Next App Router route. It must fetch the public trip preview first, preserve the unauthenticated guest preview, and render TripWorkspaceShell only when the current user is a joined trip member. Retire apps/web/src/app/trip/[joinCode]/page.tsx in the same task by deleting it or replacing it with an inert deprecation/forwarding stub that contains no trip workspace logic and points maintainers to the canonical route. Do not split route behavior across both file trees.
    3. Create apps/web/src/components/trip/TripWorkspaceShell.tsx as the main authenticated shell with tabs or segmented controls for "Lich trinh", "Ban do", and a proposal inbox entry point. Use the existing auth hydration pattern and resolve tripId from the public join-code response before calling itinerary endpoints.
    4. Treat leaders and members differently in the shell: leaders receive structural controls, drag handles, create/edit/delete actions, and proposal moderation; members receive read-only timeline cards plus proposal submission CTA buttons only.
  </action>
  <verify>npm run lint && npm run check-types</verify>
  <acceptance_criteria>
    - apps/web/src/lib/api-client.ts contains "export const itineraryApi ="
    - apps/web/src/lib/api-client.ts contains "export const proposalsApi ="
    - apps/web/app/trip/[joinCode]/page.tsx contains "TripWorkspaceShell"
    - apps/web/app/trip/[joinCode]/page.tsx contains "isMember"
    - apps/web/src/app/trip/[joinCode]/page.tsx is deleted or reduced to a no-logic deprecation/forwarding stub that points to apps/web/app/trip/[joinCode]/page.tsx
    - apps/web/src/components/trip/TripWorkspaceShell.tsx contains "\"Lich trinh\""
    - apps/web/src/components/trip/TripWorkspaceShell.tsx contains "\"Ban do\""
  </acceptance_criteria>
  <done>The join-code route still works for guests, the canonical route file is unambiguous, and implementers are not left with a shadow copy to edit by mistake.</done>
</task>

<task type="auto">
  <name>Task 2: Implement timeline CRUD, progress-state rendering, and proposal review UI</name>
  <files>apps/web/src/components/trip/TripWorkspaceShell.tsx, apps/web/src/components/trip/TimelineDaySection.tsx, apps/web/src/components/trip/ItineraryComposerSheet.tsx, apps/web/src/components/trip/ItineraryItemEditor.tsx, apps/web/src/components/trip/DeleteItineraryItemDialog.tsx, apps/web/src/components/trip/ProposalComposerSheet.tsx, apps/web/src/components/trip/ProposalInboxPanel.tsx, apps/web/app/globals.css</files>
  <read_first>
    apps/web/app/globals.css
    apps/web/src/lib/api-client.ts
    apps/web/src/components/trip/TripWorkspaceShell.tsx
    .planning/phases/02-the-itinerary-engine/02-CONTEXT.md
    .planning/phases/01-foundation-onboarding/01-CONTEXT.md
  </read_first>
  <action>
    Build the actual timeline interaction layer with the locked Phase 2 decisions.
    1. Create TimelineDaySection.tsx to render each day expanded by default, with an empty-state CTA when a day has no items. The component must visually distinguish "sap toi", "dang di", "da di", and "chua chot gio" using consistent chips and timeline card styling.
    2. Create ItineraryComposerSheet.tsx with quick mode as the default path. Quick mode must capture title, dayIndex, insertAfterItemId, startTime, locationName, and shortNote. Detailed mode must additionally capture locationAddress, placeId, lat, and lng.
    3. Create ItineraryItemEditor.tsx as a dedicated modal or drawer for edits. Do not use inline editing. Show overlap warnings from the API in the form but allow save.
    4. Create DeleteItineraryItemDialog.tsx so deletion always requires an explicit confirmation click before calling deleteItem.
    5. Create ProposalInboxPanel.tsx so leaders can review proposals one by one with explicit accept and reject buttons, while timeline cards show proposal badges for associated items.
    6. Create ProposalComposerSheet.tsx as the explicit member-side proposal composer/CTA surface and wire it from TripWorkspaceShell.tsx and the relevant timeline/day-empty-state entry points. It must submit UPDATE_TIME, UPDATE_LOCATION, UPDATE_NOTE, and ADD_ITEM through proposalsApi.createProposal, showing the correct fields and payload shape for each mode instead of reusing leader CRUD forms.
    7. In TripWorkspaceShell.tsx automatically scroll the current item region into view after snapshot load, add a clear current-area highlight, keep untimed items visually neutral instead of looking delayed or upcoming, and expose member CTA buttons that open ProposalComposerSheet rather than structural edit controls.
    8. Use apps/web/app/globals.css to add the Phase 2 state tokens and motion needed for the new timeline states without replacing the existing playful Vietnamese-first style.
  </action>
  <verify>npm run lint && npm run check-types</verify>
  <acceptance_criteria>
    - apps/web/src/components/trip/TimelineDaySection.tsx contains "\"sap toi\""
    - apps/web/src/components/trip/TimelineDaySection.tsx contains "\"dang di\""
    - apps/web/src/components/trip/TimelineDaySection.tsx contains "\"da di\""
    - apps/web/src/components/trip/TimelineDaySection.tsx contains "\"chua chot gio\""
    - apps/web/src/components/trip/ItineraryComposerSheet.tsx contains "insertAfterItemId"
    - apps/web/src/components/trip/ItineraryItemEditor.tsx contains "overlapWarnings"
    - apps/web/src/components/trip/DeleteItineraryItemDialog.tsx contains "Xoa hoat dong nay"
    - apps/web/src/components/trip/ProposalComposerSheet.tsx contains "createProposal"
    - apps/web/src/components/trip/ProposalComposerSheet.tsx contains "UPDATE_TIME"
    - apps/web/src/components/trip/ProposalComposerSheet.tsx contains "UPDATE_LOCATION"
    - apps/web/src/components/trip/ProposalComposerSheet.tsx contains "UPDATE_NOTE"
    - apps/web/src/components/trip/ProposalComposerSheet.tsx contains "ADD_ITEM"
    - apps/web/src/components/trip/ProposalInboxPanel.tsx contains "acceptProposal"
    - apps/web/src/components/trip/ProposalInboxPanel.tsx contains "rejectProposal"
    - apps/web/src/components/trip/TripWorkspaceShell.tsx contains "ProposalComposerSheet"
    - apps/web/src/components/trip/TripWorkspaceShell.tsx contains "scrollIntoView"
  </acceptance_criteria>
  <done>The workspace supports leader CRUD, member read-only viewing plus explicit proposal submission CTAs, visible state chips, explicit delete confirmation, and proposal moderation.</done>
</task>

<task type="auto">
  <name>Task 3: Add a dedicated map projection route with filter memory and focused item launch</name>
  <files>apps/web/package.json, apps/web/app/trip/[joinCode]/map/page.tsx, apps/web/src/components/trip/TripMapScreen.tsx, apps/web/src/components/trip/LocationPicker.tsx</files>
  <read_first>
    apps/web/package.json
    apps/web/src/lib/api-client.ts
    apps/web/src/components/trip/TripWorkspaceShell.tsx
    .planning/phases/02-the-itinerary-engine/02-CONTEXT.md
    .planning/phases/02-the-itinerary-engine/02-RESEARCH.md
  </read_first>
  <action>
    Implement the dedicated map mode as a projection and place-selection helper, not as a structural editor.
    1. Add the map dependencies needed for a robust web map in apps/web/package.json: leaflet and react-leaflet. If clustering is implemented, keep it light and only activate when pin count exceeds 12.
    2. Create apps/web/app/trip/[joinCode]/map/page.tsx as the dedicated route that renders TripMapScreen.
    3. Create apps/web/src/components/trip/TripMapScreen.tsx to fetch the same itinerary snapshot, render markers only for items with lat/lng, draw a line in timeline order, apply a persisted day filter with localStorage key `trip-map-day-filter:${joinCode}`, and respect an optional `focusItemId` query parameter by centering and highlighting the selected item.
    4. Create apps/web/src/components/trip/LocationPicker.tsx so leaders can either search for a place or click the map to stage a candidate point. A map click must only populate a candidate location and still require explicit confirmation in the form before save.
    5. Keep structural save actions in the itinerary forms. The map must never reorder items or persist changes on its own.
  </action>
  <verify>npm run lint && npm run check-types</verify>
  <acceptance_criteria>
    - apps/web/package.json contains "\"leaflet\""
    - apps/web/package.json contains "\"react-leaflet\""
    - apps/web/app/trip/[joinCode]/map/page.tsx contains "TripMapScreen"
    - apps/web/src/components/trip/TripMapScreen.tsx contains "trip-map-day-filter:"
    - apps/web/src/components/trip/TripMapScreen.tsx contains "focusItemId"
    - apps/web/src/components/trip/TripMapScreen.tsx contains "Polyline"
    - apps/web/src/components/trip/LocationPicker.tsx contains "pendingLocation"
    - apps/web/src/components/trip/LocationPicker.tsx contains "confirmLocation"
  </acceptance_criteria>
  <done>The trip has a separate map mode that follows itinerary order, remembers its day filter, and supports explicit place confirmation.</done>
</task>

<task type="auto">
  <name>Task 4: Seed the frontend test harness and interaction coverage for timeline and map flows</name>
  <files>apps/web/package.json, apps/web/vitest.config.ts, apps/web/src/test/setup.ts, apps/web/src/components/trip/__tests__/trip-workspace.test.tsx</files>
  <read_first>
    apps/web/package.json
    apps/web/src/components/trip/TripWorkspaceShell.tsx
    apps/web/src/components/trip/TripMapScreen.tsx
    .planning/phases/02-the-itinerary-engine/02-VALIDATION.md
  </read_first>
  <action>
    Add the missing web test harness required by VALIDATION.md and cover the core itinerary interactions.
    1. Add Vitest and React Testing Library wiring in apps/web/package.json with a script `test:ui` set to `vitest run`.
    2. Create apps/web/vitest.config.ts and apps/web/src/test/setup.ts with the minimum jsdom setup needed for Next/React component tests.
    3. Create apps/web/src/components/trip/__tests__/trip-workspace.test.tsx with cases that verify member read-only rendering, leader-only structural controls, visible status chips, proposal badge rendering, and map launch links with focusItemId.
  </action>
  <verify>npm run lint && npm run check-types && npm run test:ui -w apps/web</verify>
  <acceptance_criteria>
    - apps/web/package.json contains "\"test:ui\": \"vitest run\""
    - apps/web/vitest.config.ts contains "defineConfig"
    - apps/web/src/test/setup.ts contains "@testing-library/jest-dom"
    - apps/web/src/components/trip/__tests__/trip-workspace.test.tsx contains "read-only"
    - apps/web/src/components/trip/__tests__/trip-workspace.test.tsx contains "focusItemId"
  </acceptance_criteria>
  <done>The web app has a runnable unit-test harness and interaction coverage for the itinerary workspace and map launch behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `npm run lint`
- [ ] Run `npm run check-types`
- [ ] Run `npm run test:ui -w apps/web` once the harness is added
- [ ] Manually confirm a member sees no structural edit controls and a leader sees quick-create, edit, delete, reorder, proposal moderation, and map links
</verification>

<success_criteria>
- Leaders can manage the itinerary without inline editing or ambiguous delete behavior
- Members see the current itinerary in real time but cannot perform structural edits
- Map mode is clearly separate, remembers its filter, and reflects timeline order only
- Frontend tests exist for timeline state rendering and map launch interactions
</success_criteria>

<output>
After completion, create `.planning/phases/02-the-itinerary-engine/02-02-SUMMARY.md`
</output>
